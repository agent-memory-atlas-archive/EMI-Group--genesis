# EvoGit Agent Behaviour & Tools

## Intent
Contains the `EvoGit.Agent` behaviour module, its LLM tool definitions, data structs for agent state and results, and extracted helper modules for context compression and subagent processing. Each agent is a transient Elixir module that `use EvoGit.Agent` and provides a system prompt, optional tool overrides, and subagent delegation configuration. The `use` macro injects thin defaults and overridable callbacks; the actual agent loop logic lives in the shared `EvoGit.Agent.Runner` module (extracted from the macro).

**Note:** Agent type implementations (Manager, Executor, etc.) live in `../agents/`.

## Routing Table
- `./tools/` → LLM tool modules (17+ tools for file I/O, context, search, shell, etc.)
- `./runner.ex` → `EvoGit.Agent.Runner` — shared agent loop runner (run/3, do_run/2, loop/1, do_turn/1, effective_tools/1, trigger_recovery/2, enter_grace/3, maybe_enter_cancel_grace/1, etc.)
- `./subagent_schemas.ex` → `EvoGit.Agent.SubagentSchemas` — shared subagent tool/schema generation (tools/1, schemas/1) + the subagent-type → implementation-module resolver `subagent_module_for/2`, parameterized by agent_module
- `./context_compression.ex` → Context compression helper (compresses chat history when token threshold exceeded)
- `./subagent_processing.ex` → Subagent call processing (builds specs, spawns subagents, merges results) + foreign-repo spawn starting-commit resolution (see "Foreign-Repo Commit Propagation")
- Foreign-repo spawn starting commits & the stale-pointer hazard → this file ("Foreign-Repo Commit Propagation") + `../agent_scheduler/CONTEXT.md` (roll-up in `Subagents.store_sub_result/3,4`) + `../runtime/CONTEXT.md` (per-repo report branches)
- `./loop_state.ex` → `LoopState` struct — agent loop state threaded through every turn
- `./result.ex` → `Result` struct — structured output of a completed agent run
- `./output_sanitizer.ex` → Tool output sanitization pipeline (ensure_utf8 → ANSI stripping → progress bar removal → truncation) + `./truncation_feedback.ex` (appended ⚠️ feedback block). Truncation thresholds (3-level config): `[:truncation, :tool_output_max_bytes]` = 131072 (128 KB global ceiling), `[:truncation, :tool_output_default_max_bytes]` = 16384 (16 KB for `@high_output_tools`: run_bash, run_powershell, read_file, rg, curl, run_git, search_web, search_context, search_history), `[:truncation, :tool_output_truncate_size]` = 8192 (8 KB keep size); a per-call `max_bytes` tool arg overrides the default, capped at the global ceiling. Cuts are UTF-8-boundary-safe via `String.byte_slice/3` (Elixir 1.17+) with a defensive `EvoGit.UTF8.ensure_utf8/1` pass on the final assembled string; kept-budget clamping (`min(truncate_size, original_size)`) prevents head/tail overlap and negative omitted counts when output exceeds the threshold but is smaller than the keep size (whole content retained, nothing omitted). On truncation the LLM sees a short factual inline header (original size, effective max, kept first X + last X bytes, N bytes omitted, remediation "Narrow the pattern/path or raise max_bytes (up to 131072)") followed by head+tail content (+ omitted marker), then `TruncationFeedback.append_truncation_feedback/3` appends a single concise ⚠️ line (original/kept sizes + remediation). `truncation_info.truncated_size` = the retained CONTENT bytes (head+tail, excluding header/marker text); `truncation_info.original_size` = the pre-truncation byte size. The truncation call site (tool_dispatch.ex:995-1002) runs `sanitize_and_truncate/3` then `append_truncation_feedback/3`; the sandbox helpers (`EvoGit.Sandbox.Helpers.truncate_output`) are a separate, lower-level truncation with their own wording.

## API Surface

### Data Structs
| Struct | Purpose |
|---|---|
| `EvoGit.Agent.LoopState` | Agent loop state (enforced keys: `agent_id`, `agent_module`, `depth`, `node_path`, `context`; remaining fields have defaults). Threaded through every turn. |
| `EvoGit.Agent.Result` | Structured result of a completed run (`result`, `commit_sha`; optional `tag`, `branch`, `base_commit`, `repo_id`, `foreign_repo_commits`). `repo_id` identifies which repo the result belongs to (`"primary"` or a foreign repo string), populated from the process dictionary. `foreign_repo_commits` = `%{repo_id => latest commit SHA}` for work done in foreign repos (default `%{}`; the scheduler stream injects values, the runtime stream reads it for the per-repo report). Produced by `CompleteTask`. |

### Helper Modules
| Module | Purpose |
|---|---|
| `EvoGit.Agent.ContextCompression` | Compresses chat history when `total_tokens` exceeds threshold |
| `EvoGit.Agent.SubagentProcessing` | Spawns subagents, resolves cross-repo paths, merges results via octopus merge, formats results for LLM context. Foreign-repo subagent phylo nodes (`build_subagent_phylo_node/7`): starting-commit precedence = per-repo `base_sha` (task-level starting commit, non-nil wins) → tracked commit from the `foreign_repo_commits` map (previous subagent's latest commit in that foreign repo) → foreign repo HEAD; an unresolvable `base_sha` or a missing/non-git foreign repo root returns a descriptive `{:error, msg}` (surfaced as `{:error, {call, index, msg}}`) — never a MatchError crash. `format_subagent_result/1` surfaces foreign-repo spawn-gate rejection hints verbatim: `{:error, {:foreign_repo_read_only, msg}}`, `{:error, {:foreign_repo_write_not_root, msg}}` (write-capable spawn into a foreign repo by a nested agent — root-agent-only), and `{:error, {:foreign_repo_write_serialized, msg}}` (parallel writable-foreign-repo spawns in one batch — one-at-a-time) all render as `"Error: #{msg}"` |
| `EvoGit.Agent.TurnWarning` | Adaptive turn-budget warnings — 3 positional categories (beginning/end/critical) scaling with max_turns, plus a periodic middle reminder based on turns since last subagent delegation. `:beginning` delegation-strategy warning fires at ~25% of budget (min turn 6); `:middle` reminder fires every 15 turns (`:high`) / 45 turns (`:low`). Low-level agents (Executor, TaskScheduler, etc.) skip `:beginning` and have a 3x longer middle reminder interval |
| `EvoGit.Agent.SubagentSchemas` | Shared subagent tool/schema machinery (tools/1, schemas/1, and `subagent_module_for/2` — the subagent-type → implementation-module resolver used by subagent spawning and subagent-call detection in tool_dispatch). Subagent tool/module resolution always goes through this schema machinery — the `EvoGit.Agent` behaviour injects NO default `subagent_tools/0` callback (the macro-injected default `available_tools/0` is `Tools.schemas() ++ SubagentSchemas.schemas(__MODULE__) ++ [CompleteTask.schema()]`) |

### Tool Library (`EvoGit.Agent.Tools`)
| Component | Role |
|---|---|
| `tools.ex` | Central dispatch: `schemas/0` returns the STANDARD tool schemas (every `use EvoGit.Agent` coding agent's default `available_tools/0`), `execute/5` dispatches by name. `run_command` — the self-reflective agent's single task-control shell tool — is dispatch-registered ONLY, never in `schemas/0` or `read_only_schemas/0` (scope/security; see Repo-less Agents) |
| Standard tools | `read_file`, `create_files`, `write_file`, `edit_file`, `make_dir`, `read_context`, `write_context`, `edit_context`, `run_bash`, `rg`, `glob`, `list_dir`, `search_web`, `search_context`, `search_history` |
| `run_command` | The self-reflective agent's single task-control shell tool (`EvoGit.Agent.Tools.RunCommand`) — dispatches command strings through the declarative `EvoGit.CommandShell` registry (10 commands + built-in `help`: `ListTasks.list_tasks`/`GetTask.get_task`/`StartTask.start_task`/`CancelTask.cancel_task`/`ForceKillTask.force_kill_task`/`DeleteTask.delete_task`/`SpawnInvestigator.spawn_investigator`, `GuideUser.guide_user`, `ListRecentProjects.list_recent_projects`, `SystemInfo.system_info`); the 10 former per-function tool modules in `agent/tools/` are now command handlers (no `schema/0`, invoked by the shell) — dispatch-registered only, never in `Tools.schemas/0` or `read_only_schemas/0` |
| `CompleteTask` | Special completion tool injected by `use` macro; returns `%Result{}` |

### Delegation Hinting

Two independent hinting mechanisms, both per-child-directory counter + fire-once:

- **Write-tool hint**: agent repeatedly *edits* files below its `node_path`; after the write-tool count per child dir exceeds `delegation_hint_threshold` (default 5, `[:scheduler, :delegation_hint_threshold]`), a nudge is appended to tool output suggesting a subagent for that child directory.
- **Read-tool hint**: a `:high`-level agent repeatedly reads/investigates a child dir (via `read_file`, `rg`, `glob`, `list_dir`); after the count (`read_delegation_hints`) exceeds `read_delegation_hint_threshold` (default 8, `[:scheduler, :read_delegation_hint_threshold]`), a nudge suggests `subagent_investigator`. `:low` agents are expected to read directly.
- Both hints show once per child dir (`hint_shown` flag); suppressed during merge-conflict resolution (`filter_child_paths_if_conflicts/2`). Implemented in `EvoGit.Agent.ToolDispatch` + `EvoGit.Agent.DelegationHints`: `batch_execute_tools/4`, `extract_child_paths/4` (target child dir from write-tool args), `maybe_append_delegation_hint/4`. Hints stored in `LoopState.delegation_hints`, threaded via process dictionary.

## Design Decisions

### Foreign-repo subagent starting-commit chaining (per-agent scope — can go stale)

`build_specs_and_errors/2` (`subagent_processing.ex:418-420`) reads `AgentScheduler.get_foreign_repo_commits(state.agent_id)` — the SPAWNING agent's OWN SchedMeta row — so foreign-repo commit tracking is **per-agent**, not per-task.
`build_subagent_phylo_node/7` has two clauses: the `"primary"` clause (`:584-617`) starts a child at the parent's live `parent_state.phylo_node.current_commit` (`:608`), whereas EVERY non-primary target repo id goes through the foreign clause (`:619-708`), which ignores the parent's own commit entirely.
`resolve_foreign_phylo_commit/4` (`:673-708`): a non-nil task-level `base_sha` wins unconditionally (`:681`); otherwise the spawning agent's `foreign_repo_commits[repo_id]` (`:692`); otherwise the foreign repo's HEAD (`:694`).
Consequence: a child placed in the SAME foreign repo the parent is already working in does NOT inherit the parent's commit — it starts at `base_sha`/foreign HEAD, even at depth > 0, and even when the parent's own children already committed ahead.
`collect_mergeable_results/2` (`:453-471`) merges a child's commit into the parent's worktree ONLY when `spec.repo_id == "primary"` (`:459`) — children in a foreign repo (including same-repo children of a Manager running INSIDE that foreign repo) are classified as cross-repo and never merged, so the parent's HEAD does not contain them.
`delete_same_repo_branches/3` (`:502-512`) likewise deletes branches only for `"primary"` children — foreign children's per-agent branches are deleted by WorktreeManager on agent exit (`agent_scheduler/worktree_manager.ex:289`/`:574-582`).

### Module sizes (cohesive-by-design)

`agent/tool_dispatch.ex` is ~1100 lines — the tool-dispatch orchestrator (`EvoGit.Agent.ToolDispatch`, extracted from the agent macro): per-turn LLM call with retry, tool-call routing (complete vs regular vs subagent; standard-tool batching through the write-gated `Tools.execute/5` dispatch), timeout management, the output sanitization/truncation-feedback call site (tool_dispatch.ex:995-1002), and subagent-spawn plumbing. Deliberately kept cohesive — do not split without a plan.

### Parallel standard tool call execution (bounded by scheduler tool slots)

Standard (non-subagent) tool calls in a batch execute **in parallel**, governed by the scheduler's tool-slot pool (`max_tool_concurrency`, default = detected CPU thread count, set via `[scheduler] max_tool_concurrency` TOML — no CLI flag). Subagent calls also run in parallel (`SubagentProcessing.process_subagent_calls`) — the ENTIRE batch of subagent tool calls from one LLM response is passed in a single blocking `AgentScheduler.spawn_sub_agents/1` call and all specs are registered/dispatched together (`agent_scheduler/subagents.ex` `spawn_validated_subagents/5`, dispatched via `Enum.reduce(sub_ids, state, &Dispatch.try_dispatch/2)`). **Fan-out WIDTH has NO code cap**: there is no min/max on the number of subagent tool calls per response and no agent-count limit in the scheduler spawn path — the only code-level bounds are `max_depth` (recursion DEPTH), the ignored-path / spatial-contract validation, and the one-at-a-time writable-foreign-repo gate; execution is throttled only by the tool/LLM slot pools. Width is therefore at the LLM's discretion (prompt-driven), and the prompts explicitly invite wide parallelism (`agents/architect.ex` "there is no concurrency limit"; `agents/manager.ex` "Spawn subagents for ALL independent tasks simultaneously"). Turn caps bound TOTAL turns, not subagent count — one LLM response = one turn regardless of how many subagents it spawns. `batch_execute_tools/4` uses `Task.async_stream`: `max_concurrency` = batch size (positive integers only — the scheduler, not a local cap, is the binding constraint), `ordered: true`, `timeout: :infinity` (per-tool timeout stays inside `execute_tool_with_timeout/7`, which wraps its own `Task.yield(task, tool_timeout) || Task.shutdown(task)` — an outer stream timeout would kill long-running tools). Tool-slot acquisition (`AgentScheduler.with_tool_slot/2`) happens per call inside `execute_tool_with_timeout/7`. Only raw tool execution runs in the parallel tasks; delegation hints are threaded in index order in the parent via `Enum.reduce` (`apply_tool_output_tracking/3`), keeping hint accumulation deterministic — same for the once-per-run redundant-cd warning (`:redundant_cd_warned` process-dict flag).

**Caveat — parallel committing tools:** `write_context`/`edit_context`/`make_dir` with `commit: true` run git add/commit in the same worktree; parallel execution may contend on git's `index.lock`. `max_tool_concurrency` (default = detected CPU thread count) bounds this; lower it if contention appears.

**No cap on subagent SPAWN COUNT (spawn-count is prompt-driven, not code-enforced).** Nothing in this directory limits how many subagent tool calls one LLM response may carry, or how many subagents may exist at once:
- `process_regular_tool_calls/3` (`tool_dispatch.ex:786-831`) `Enum.split_with`s the batch's subagent calls and passes the FULL list to `SubagentProcessing.process_subagent_calls/3` — no `Enum.take`/`Enum.slice`/batch limit anywhere in this directory (grep-verified).
- `SubagentProcessing.process_subagent_calls/3` (`subagent_processing.ex:50-110`) → `build_subagent_specs/3` (`:139` `Enum.map`) builds a spec for EVERY call, then one `AgentScheduler.spawn_sub_agents/2` call spawns them all; `split_valid_subagent_calls/1` only rejects calls missing `path` (per-call error feedback, not a count cap).
- `subagent_schemas.ex:29-77` emits ONE tool per declared subagent module with a SINGLE `path`+`objective` (+ optional `commit_id`) parameter — to spawn N subagents the model emits N tool calls; there is NO batch/multi-path schema.
- The only code-enforced recursion limit is DEPTH, not count: `[:scheduler] :max_agent_depth` (default 8, `config/schema/definitions.ex:76-85`), enforced by `runner.ex:566-582` `at_max_depth?/1` (drops subagent tool schemas once `depth >= max_depth`) and `agent_scheduler/subagents.ex:181-191` `validate_subagent_depth/3` (`subagent_depth > state.max_depth`). This bounds the DEPTH of each delegation *branch*, NOT the per-turn or task-wide subagent count.
- Running-subagent concurrency is throttled only by the shared LLM/tool slot pools; queued subagents are still registered and each holds its own worktree (no per-task subagent-count / batch-size / concurrency config key exists — the scheduler schema has no `max_subagents`/`max_children` key).
- **The parent BLOCKS for the whole batch** — `spawn_sub_agents/2` is a blocking `GenServer.call` with `timeout: :infinity` (`agent_scheduler.ex:101-109`; `subagent_processing.ex:63` passes no timeout), replying only when the LAST child returns. The parent agent process therefore stays ALIVE for the whole child subtree, holding its worktree (reclaimed only on `:DOWN`) but NOT an LLM slot (`with_llm_slot/2` wraps only the LLM call) and NOT a tool slot (standard tools released theirs earlier in the same batch). Within a batch, standard tool calls complete FIRST (`process_regular_tool_calls/3` runs `process_standard_calls` at `tool_dispatch.ex:797` before `process_subagent_calls` at `:800`) — sequential phases, not concurrent.
- Consequences for any future GLOBAL cap on agents/worktrees (deadlock surface, child-side mechanics incl. eager registration + worktree-before-LLM-slot): see `../agent_scheduler/CONTEXT.md` → "Subagent Spawn Fan-Out Bounds & Deadlock Surface".
So a "wide fan-out" (5-10 children per level) is bounded by DEPTH × turn budget (`max_turns`) and by prompt guidance — e.g. `agents/manager.ex` states "There is no limit on concurrency" and urges parallel batches — NOT by any spawn-count cap.

### Grace Period & Graceful Cancellation (runner-side)

Two grace kinds, both entered via `Runner.enter_grace/3` (`runner.ex`, `@doc false`): (1) `maybe_recovery_auto_commit/1` FIRST (best-effort auto-commit of uncommitted work — for cancel this is "save the changes"), (2) optionally appends a recovery message, (3) sets `in_grace_period: true` + the `grace_turns_remaining` budget, then re-enters `loop/1`.

- **Turn-limit recovery** (`trigger_recovery/2`, call site `loop/1`: `"max turns (#{max_turns}) exceeded"`): budget **1** — one continue attempt → hard-stop. Appends the "exceeded the execution limit" warning (`message: :default`).
- **Cancel-grace** (`maybe_enter_cancel_grace/1`, called in `loop/1` immediately AFTER `drain_and_inject_user_messages/1`): when the ETS `cancel_requested` flag is set (scheduler-side graceful cancel), the flag is cleared and the agent enters grace with budget **3** and `message: nil` — NO extra recovery message (the cancel message was already injected via the `pending_user_messages` drain). Up to 3 turns to wrap up and call `complete_task`.
- **Budget semantics**: `LoopState.grace_turns_remaining` defaults to `0` (not in grace). Each turn ending WITHOUT `complete_task` decrements it via `consume_grace_turn/1` at the three call sites in `tool_dispatch.ex` (`handle_protocol_violation`, the `process_llm_response` `{:error, :protocol_violation}` branch, `continue_after_tools`). `grace_period_continue_failed?/1` (`agent.ex`): `in_grace_period: true, grace_turns_remaining: n` → `n <= 1` (counter 0 — the struct default — hard-stops); true → call sites return `{:error, :recovery_failed}`.
- **complete_task during grace**: `handle_complete_call`'s dirty-workspace check skip (`not state.in_grace_period and ...`) applies to BOTH kinds — completing with a dirty workspace is allowed during any grace period.
- **Turn counts**: cancel-grace 3 → 3→2, 2→1, 1 → hard-stop (3 grace turns). Turn-limit 1 → hard-stop on the first continue (1 grace turn). `trigger_turn_limit_recovery?/1` returns `false` during grace.

### Lifecycle ordering — worktree allocation BEFORE the first LLM slot

The per-agent allocation order is FIXED and explains wide-tree worktree/IO bursts:

- `Runner.run/3` (runner.ex:41-53) calls `setup_dispatch_context/1` (runner.ex:199-289) FIRST; for non-repo-less agents this BLOCKS on `WorktreeManager.create_worktree_for_agent/6` (runner.ex:265-278; raises on `{:error, reason}`), and only afterwards runs `do_run/2` (runner.ex:49) → `loop/1` (runner.ex:350) → `ToolDispatch.do_turn/5` → `call_llm_with_retry/5` → `AgentScheduler.with_llm_slot/2` (tool_dispatch.ex:250). So the worktree is always allocated BEFORE any LLM slot is requested.
- **`ToolDispatch.current_model/0` / `current_generation_params/0` raise contract**: both resolve the calling process's agent state via `current_agent_id()` + `Store.get_agent_state/1`; a nil agent id (process not a scheduled agent) or a missing ETS row (agent purged/cancelled mid-call or scheduler restarted — e.g. while blocked in `with_llm_slot/2`) raises a DESCRIPTIVE `ArgumentError` naming the caller function + agent id (shared helper `fetch_current_agent_state!/1`), never a bare MatchError. The raise propagates — do not add default/nil fallbacks; pinned by regression tests in `tool_dispatch_retry_slot_test.exs`.
- Worktree creation is admission-bounded and concurrent across agents up to a cap: WorktreeManager offloads each create to an independent `Task.start` (worktree_manager.ex) under a 1h `@worktree_call_timeout`, but admits create pipelines through a FIFO admission queue capped at `Application.get_env(:evo_git, :max_concurrent_worktree_creation, 4)` — at most 4 create pipelines run at once and extra requests queue (queuing only DELAYS the reply, never drops it). The scheduler never serializes creates (dispatch.ex:207-211); the scheduler's `queue` is the PAUSED-scheduler dispatch queue only (`Dispatch.process_queue`, drained on resume), not a worktree limiter. The cap bounds CREATE-pipeline concurrency only, never live worktrees/agents (detail: `agent_scheduler/CONTEXT.md`).
- Consequence: every dispatched agent — including every subagent in a fan-out batch, all registered+dispatched in one reduce (`Subagents.spawn_validated_subagents` → `Enum.reduce(sub_ids, state, &Dispatch.try_dispatch(&2, &1))`, subagents.ex:126) — allocates its worktree FIRST and only THEN queues for an LLM slot. A wide tree therefore performs a full worktree-creation burst even when most agents end up merely blocked on LLM slots.
- Subagent wait semantics: the parent BLOCKS in `AgentScheduler.spawn_sub_agents/2` (subagent_processing.ex:63) → `GenServer.call(__MODULE__, {:spawn_sub_agents, ...}, timeout \\ :infinity)` (agent_scheduler.ex:101-108) — no timeout; the scheduler holds `from` and replies via `GenServer.reply` when all subagents finish (`maybe_resume_parent/2` → `dispatch_ready_parent/3`, subagents.ex:441-483). The parent's status flips to `:waiting` (subagents.ex:48) but its worktree is NOT reclaimed while waiting (its process is alive; reclaimed only on process exit — subagents.ex:453-477). The parent holds NO LLM slot (released in `with_llm_slot`'s `after` right after the stream) and NO tool slot (subagent calls never enter `execute_tool_with_timeout/7`/`with_tool_slot/2`) during the wait.
- Spawn breadth is UNLIMITED: no per-call subagent cap exists in this subtree (`process_regular_tool_calls` partitions ALL subagent calls into one `process_subagent_calls/3`; no `Enum.take`/count clamp anywhere). Only DEPTH is capped: `state.max_depth` (default 8, `[:scheduler, :max_agent_depth]`, definitions.ex:76-85) checked in `Subagents.validate_subagent_depth/3` (subagents.ex:181-191) and used runner-side by `at_max_depth?/1` (runner.ex:579-582) to strip subagent tool schemas at max depth.

## Update-Timing Safety (in-flight state vs BEAM restart)

- **No turn-level wall-clock timeout.** A turn = LLM call(s) + tool executions + subagent waits, with NO overall cap. Per-piece bounds: per-LLM-attempt stream receive timeout 30s default (`:req_llm` `receive_timeout`/`stream_receive_timeout`, thinking models 300s via `thinking_timeout`) + pool checkout `stream_pool_timeout` 300s (runtime.exs) + ReqLLM inner retries (3× for 429/transport) + the outer `call_llm_with_retry` backoff (max_retries default 15, delays 0→60s cap ≈ 9 min worst-case); per-tool timeout `min(requested, max_tool_timeout)` with `default_tool_timeout` 10s / `max_tool_timeout` 30 min hard cap (definitions.ex:121-139); subagent waits unbounded (parent blocks). **Consequence: the 3-turn cancel-grace budget is bounded in TURNS, not wall-clock** — a slow LLM stretches a graceful cancel arbitrarily long. Hard stops: `max_turns` (config default 100, applies to subagents) / `max_turns_root` (config default 1000, root only — see `apps/evo_git/lib/evo_git/config/schema/definitions.ex` and `agent_scheduler/dispatch.ex` `resolve_max_turns/3`) and the grace budget.
- **Per-turn ETS checkpoint, in-memory only.** `context.messages` lives in the agent process heap (`%LoopState{context}`); a snapshot is written to ETS (`AgentState.context`) each turn at `batch_update_agent` (tool_dispatch.ex:389-394) and after compression/warnings (runner.ex:281-284) and the pending-message drain (runner.ex:260). Named ETS dies with the VM — **there is NO durable checkpoint of the conversation**; on BEAM restart the whole conversation is lost. The dashboard's `get_agent_history` reads this ETS snapshot.
- **Worktree wipe at next boot.** Uncommitted worktree changes survive the kill on disk, but `WorktreeManager.maybe_init_repo/1` (worktree_manager.ex:236-283) — on the FIRST `create_worktree_for_agent` request for a repo after restart — does `rm_rf .genesis/workers` → `Git.prune_worktrees` → `clean_orphaned_branches` (deletes all `evogit-agent-*` branches, worktree_manager.ex:376-390). Commits made only on an agent branch (never merged/archived) are destroyed at next boot's first worktree creation. Only commits protected by `refs/genesis/archive/T<task>-A<local>-start/-final` refs (written by `CompleteTask.complete/4` when `archive` is enabled AND the agent completed) survive.
- **`Runner.run/3`'s `after` block** (runner.ex:48-52) runs `Dispatch.commit_pending_in_worktree/0` ("Agent: auto-commit fallback", dispatch.ex:265-306) on ANY agent-process exit — normal completion, `{:error, :recovery_failed}` hard-stop, and crashes (raises). It does NOT run on `:brutal_kill` (force kill) or VM shutdown — a graceful-cancel hard-stop salvages the worktree via this after-block commit, a killed BEAM loses it.
- **Task status on restart**: orphaned `:running` tasks are untouched until lease expiry (`:lease_sweep` → `:failed` "Lease expired; owning instance no longer renewing"); orphaned `:finalizing`/`:cancelling` are reconciled at TaskRegistry init (`:failed` / `:cancelled` with "Runtime restarted during ..." results). See `task_registry/CONTEXT.md`.

## LLM Request Retry Mechanism (3 layers)

**Layer 1 — EvoGit outer loop (`call_llm_with_retry/5`, `tool_dispatch.ex:187-225`):** `retry with: exponential_backoff(1_000) |> randomize() |> cap(60_000) |> Stream.take(max_retries)` (tool_dispatch.ex:189-193) with `AgentScheduler.with_llm_slot(agent_id, fn -> ... end)` INSIDE the retry block — each attempt acquires the LLM slot for ONE `ReqLLM.stream_text` + `process_stream` and releases it (via `with_llm_slot`'s `after`) before the backoff sleep runs. Retry-library semantics (`deps/retry`): `delays_from/1` prepends `[0]`; `Enum.reduce_while` sleeps then runs the block per element; `{:ok, _, _}` halts, `{:error, _}` continues (`atoms: [:error]`). Effective delays: 0, ~1s, ~2s, ~4s, ~8s, ~16s, ~32s, then 60s×N, randomized ±10% (`randomize/1`, delay_streams.ex:101-111). **Total attempts = `max_retries + 1`**. `max_retries` = `agent_state.max_retries` (tool_dispatch.ex:102), set from scheduler `state.max_retries` at dispatch (`dispatch.ex:79`), config default **15** (`[scheduler] max_retries`, definitions.ex:63-71). **No error-type discrimination** — every `{:error, reason}` retries identically (429 == 5xx == timeout == connection error). After the stream is exhausted, `case` at tool_dispatch.ex:213-223 checks `EvoGit.Agent.TruncationFeedback.is_rate_limit_error?(reason)` (string-substring match on `inspect(reason)` for `"rate_limit"`/`"quota"`/`"429"`/`"resource_exhausted"`, truncation_feedback.ex:16-23) → `AgentScheduler.report_llm_error(agent_id, :rate_limit)`; then `{:error, reason}` propagates to `prompt_until_tools_or_limit/5` which **RAISES** `"LLM request failed after N retries"` (tool_dispatch.ex:177) → agent crash → scheduler crash-retry (`agent_max_retries`, default 3, lifecycle.ex:148-186). Retried agents get a fresh worktree + new turn; their next slot request is queued behind the backoff if one is active. A model with 0 LLM slots (peak hard-pause) does NOT raise: its slot request BLOCKS (queued in the scheduler — agent status `:blocked`) until capacity returns (peak exit → `update_config` → `grant_pending_on_resume` grants queued waiters) or a force-kill purge replies `{:error, :cancelled}` (raises in `with_llm_slot` → agent crash → scheduler crash-retry/cancel machinery). `rescue_only: []` stays because the remaining raise paths must propagate immediately, not be retried through exponential backoff.

**Layer 2 — ReqLLM inner retries (inside each `ReqLLM.stream_text/3`):** `ReqLLM.Streaming.Retry` (`deps/req_llm/lib/req_llm/streaming/retry.ex`, wired at `finch_client.ex:188`). `max_retries` default **3** (retry.ex:31; `finch_client.ex:238` — overridable via `[[llm.models]]` generation params `max_retries`). Only retried: (a) **HTTP 429** — delay = parsed `Retry-After` header (seconds→ms; missing/unparseable/zero → 1000ms; retry.ex:194-196, 211-245); (b) **transport errors before any body data** (`data_received?: false`) — `pool_not_available` → 250ms, `closed`/`timeout`/`econnrefused` → 0ms (retry.ex:86-92, 198-209; failure.ex:8). **5xx and other HTTP statuses are NOT retried at this layer** (`%ReqLLM.Error.API.Request{}`; retry.ex:76-108), and no retry once data has started streaming (avoids duplicating partial output). Inner retries use blocking `Process.sleep` (retry.ex:123-125). Non-streaming Req-based paths (e.g. `openai_codex`) use `ReqLLM.Step.Retry` (`step/retry.ex`) — same 429 Retry-After honoring, transient transport reasons → 0ms, `max_retries` default 3.

**Layer 3 — scheduler per-model backoff (fires only AFTER the outer loop exhausts):** `AgentScheduler.report_llm_error/2` (agent_scheduler.ex:286-289; handler 880-886) → `Slots.handle_report_llm_error(agent_id, :rate_limit, state)` (slots.ex:162-182): sets per-model `llm_backoff_until[model_id] = now + 60_000`, re-queues ALL waiting agents of that model with the backoff timestamp, schedules `:retry_llm_waiting` in 65s (slots.ex:179 → slots.ex:194-198 → `grant_pending_llm_slots`). New slot requests during backoff are queued (slots.ex:105-109). Non-`:rate_limit` reports are no-ops (slots.ex:184-186). Expired backoffs clear on any grant path (`maybe_clear_model_backoff/2`, slots.ex:492-508). **Scoped per model_id — NOT global** (a rate-limited model doesn't block other models).

**Slot acquired/released PER ATTEMPT — free during backoff sleeps:** `AgentScheduler.with_llm_slot/2` (agent_scheduler.ex:322-332) acquires via blocking `GenServer.call` (`request_llm_slot`, `timeout: :infinity`, agent_scheduler.ex:262-265) and releases via `after` → `release_llm_slot/1` cast (agent_scheduler.ex:277-280). Each attempt holds the slot only for its single `stream_text` + `process_stream`; the outer exponential-backoff sleep (and any inner Retry-After sleep) happens BETWEEN attempts while the slot is FREE. **Pause-scheduler implication:** a retrying agent's next attempt blocks at slot re-acquisition when `AgentScheduler.pause/0` is active (queued `:blocked`, granted on `resume/0`) — pause takes effect within one backoff delay. A raise inside the LLM call propagates immediately: `with_llm_slot`'s `after` releases the slot; the retry macro only retries `{:error, _}` tuples, never raises. `ContextCompression` also uses `with_llm_slot` (context_compression.ex:79-103) but WITHOUT the outer retry loop — single `stream_text` (inner 3-retry only).

## First Message Construction (objective → LLM) — single choke point

The objective (`AgentState.objective`, a plain `String.t()`) enters the conversation ONLY inside the first **user** message, built in `Runner.do_run/2` (`runner.ex:55-196`). Exact construction: `combined_prompt` is assembled at `runner.ex:128-134`; the two initial messages are produced by the pure `ContextBuilder.build_initial_messages/4` at `runner.ex:142-148` (helper in `context_builder.ex`, directly unit-tested — see `agent/context_builder_test.exs`):

- `objective_body = if blank?(objective), do: "", else: to_string(objective)` (`runner.ex:125-126`)
- `combined_prompt` = `ContextBuilder.context_block(context_body)` (context tree + delegation-authority section + foreign-repos section + repo-notes section) joined with `ContextBuilder.objective_block(objective_body)` via `"\n\n---\n\n"` (`runner.ex:128-134`; wrappers at `context_builder.ex` — `context_block/1`/`objective_block/1` emit `<context>...</context>` / `<objective>...</objective>`)
- **`runner.ex:142-148`**: `ContextBuilder.build_initial_messages(system_prompt, combined_prompt, parent_id, attachments)` returns `[system, user]` (wrapped via `ReqLLM.Context.new/1` at `runner.ex:150`) — the ONLY place the objective is turned into an LLM message. The system message is `agent_module.system_prompt()` only; the objective is never a system message. No attachments (or non-root agent / repo-less) → the legacy plain-binary `user(combined_prompt)` fast path (byte-identical shape); a ROOT agent with attachments → `user([ContentPart.text(combined_prompt) | image/file parts…])` (multimodal — see below).

**Delegation-authority section**: built by `ContextBuilder.build_authority_section/1` (`context_builder.ex`, pure — takes `%{parent_id, repo_less, foreign_repos}`, never reads the process dictionary), present ONLY for non-repo-less agents whose task has non-primary foreign repos (else `""` and the blank-filter drops it — single-repo tasks stay token-neutral).
The section states the agent's role as **ROOT** (`AgentState.parent_id == nil` — exactly equivalent to `SchedMeta.depth == 0` by construction in `Dispatch.register_agent/7`) or **NESTED**, and pins the foreign-repo spawn rules for that role (root: writable foreign-repo `:read_write` spawns allowed one-at-a-time; nested: read-only foreign-repo spawns only, report writable needs back up the delegation chain).
The root signal at assembly is `agent_state.parent_id == nil` — never the process-dict depth (`Process.get(:evogit_agent_depth)` / `AgentScheduler.current_depth()`), which may not be populated yet in `do_run`.
Repo-less agents (self-reflective chat, `Process.get(:repo_less)`) never get the role statement; Genesis Mode B's two sequential depth-0 roots (Architect, then Manager) both have `parent_id` nil and both correctly get the ROOT statement — no special-casing.
The section is added in the runner-level `context_body` assembly ONLY — `ContextBuilder.build_dynamic_context/1` is untouched (tests pin its small-repo output length).

**Repo-notes section (git-submodules awareness)**: when the repo has git submodules, the `<context>` block includes a `## Git Submodules` note — the rendered markdown block in `AgentState.repo_notes` (set at root spec build by `EvoGit.Runtime.Helpers.load_repo_notes/2`, inherited by subagents), passed through `ContextBuilder.build_repo_notes_section/1` (`context_builder.ex`). It lists the gitlink paths, explains they arrive as empty placeholder dirs in agent worktrees, tells the agent to populate them with `git submodule update --init [--recursive]` (network required; shared clone in `.git/modules`), and warns never to delete the placeholder dirs, not to create filler files inside them, and not to commit inside submodules. When `repo_notes` is nil (no gitlinks or detection failed) the section renders `""` and the blank-filter drops it — prompts for repos without submodules stay noise-free.

The per-turn LLM call receives the whole `%ReqLLM.Context{}` by value — `ToolDispatch.do_turn/5` → `prompt_until_tools_or_limit(context, ...)` (`tool_dispatch.ex:131`) → `call_llm_with_retry(context, tools, llm_gen_opts, agent_id, max_retries)` (`tool_dispatch.ex:195`) → `ReqLLM.stream_text(current_model(), context, Keyword.merge([tools: tools], llm_gen_opts))` (`tool_dispatch.ex:204-208`). Messages are never re-assembled per call; all other construction sites (turn warnings `runner.ex:221,234`, pending-user-message drain `runner.ex:253`, grace/recovery messages `runner.ex:368,377` — which re-inject the objective text via `recovery_warning_message/2` at `runner.ex:414-435` — no-tool-call nudges `tool_dispatch.ex:246-260`, tool results, compression `context_compression.ex:74,87-90`) only APPEND. Context compression preserves `[system_msg, initial_user_msg | _rest]` verbatim (`context_compression.ex:69,90`), so anything injected into the first user message survives compression but permanently occupies the context window — for multimodal `:attachments` this means the media parts (raw decoded bytes) are re-sent to the provider EVERY cycle after a compression, at full token/bandwidth cost.

**Multimodal attachments (`:attachments` — implemented)**: images/audio attached at task start ride the ROOT agent's FIRST user message as real req_llm content parts (see `EvoGit.Attachments`, `../attachments.ex` — the single source of truth for the opt contract). The opt is a list of STRING-keyed maps `%{"type" => "image"|"audio", "name" => basename, "media_type" => IANA type, "data" => base64-encoded bytes}`; `RuntimeOpts.build_common_runtime_opts` validates it (`Attachments.validate/1`, spec-error style — malformed payloads fail task start with a descriptive `ArgumentError`; caps ≤4 per task and ≤15 MiB raw each) and `Helpers.build_root_agent_spec/7` forwards it into the root spec's `spec_opts` (omit-when-nil/[]). `Runner.setup_dispatch_context` surfaces the spec opt via `Process.put(:attachments, ...)` (`runner.ex:231`). `ContextBuilder.build_initial_messages/4` gates on `parent_id == nil` AND a non-empty list — repo-less agents pass `nil` (a stray reflect key degrades to the plain-text path, never a crash) — then materializes base64 → raw bytes (`Base.decode64!/1`) as `image` → `ContentPart.image(raw, media_type)` and `audio` → `ContentPart.file(raw, name, media_type)` (audio rides a `:file` part — req_llm has no audio part type; `message/content_part.ex:69-79`), in stable input order, after a leading `ContentPart.text(combined_prompt)`. Base64 is mandatory in the opts layer (Store.Codec Jason-encodes whole opts; raw non-UTF-8 binaries abort Jason) — raw bytes exist only inside the LLM-message content parts. Subagents (non-nil `parent_id`) never inherit; resume/merge/auto-resolve follow-ups do not re-carry the key (fresh text objectives; the user can re-attach). Deliberately NO model-capability check (a provider erroring on the modality is the user's concern). The objective stays strictly `String.t()` in `AgentSpec`/`AgentState` — attachment bytes travel as a spec opt + base64 in persisted task opts, never in an `AgentState` field or the objective string. No objective-size truncation exists anywhere (CLI `get_input/2` reads `-f file` verbatim); the only conversation-level limit is `[:llm, :compression_threshold_tokens]` (default 180_000, `config/schema/definitions.ex:168-177`).

## Constraints
- Every agent MUST `use EvoGit.Agent` and implement `system_prompt/0`.
- System prompts MUST NOT contain dynamic state, objectives, or context trees — those are injected as user prompts.
- Read-only agents (`agent_type: :read`) should restrict `available_tools/0` to read-only tools.
- The `complete_task` tool is always injected by the macro; agents need not include it.
- Tool schemas use `ReqLLM.tool/2` format.
- Agents commit before delegating subagents (enforced by auto-commit fallback in scheduler).
- Context compression and subagent processing are extracted to dedicated modules but invoked from the agent loop via callbacks.
- **LoopState discipline**: Agent loop state must always be a `%LoopState{}` struct. Update syntax preserves the struct type.
- **Result discipline**: Agent completion produces `%Result{}` structs. Consumers pattern-match on struct fields rather than using `Map.get/2`.

## Repo-Path Assumptions (agent-loop crash points on a missing repo)

The agent loop hard-assumes a real git worktree, set once by `Runner.setup_dispatch_context/1` (`runner.ex:158-211`): `Process.put(:repo_path, worktree_path)` (`runner.ex:179`), `:genesis_repo_root`, `:evogit_repo_id`, `:evogit_agent_id`, `:evogit_agent_depth`, `:evogit_started_at`, `:custom_agent_id` (from `spec.opts`). The same function UNCONDITIONALLY requests a fresh worktree via `WorktreeManager.create_worktree_for_agent/6` and RAISES `"Failed to create worktree..."` on `{:error, reason}` (`runner.ex:188-201`) — a repo-less agent type must branch here (the worktree-creation and `resolve_agent_repo_root` seams live on the scheduler side; see `../agent_scheduler/CONTEXT.md` → "Repo-path assumptions").

Within the agent loop, the following raise on nil/missing `:repo_path` (all `|| raise "repo_path not in process dictionary"` or git-error raises) — relevant to a future repo-less agent:
- `sync_and_get_current_commit/1` (`tool_dispatch.ex:68-85`) — `Git.rev_parse(repo_path)` raises on git failure; a missing repo/worktree directory (`{:error, {:enoent, _}}` from `Git.run/2`'s pre-check) raises a clear "Worktree vanished while agent was running (removed mid-run): ..." message (shared `raise_rev_parse_error/1` helper, same hardening as `sync_current_commit_after_tools/1`); all other error shapes raise `"Git rev_parse failed (#{code}): #{msg}"`. Called from `do_complete/2` (`tool_dispatch.ex:703`) — **so `complete_task` unavoidably crashes a repo-less agent at completion** — and from `sync_current_commit_after_tools/1` (`tool_dispatch.ex:50-65`), which runs after EVERY standard-tool batch (`process_standard_calls`, `tool_dispatch.ex:804`) and after subagent processing (`sync_commit_fn`).
- `batch_execute_tools/4` (`tool_dispatch.ex:812` `repo_path` raise; `process_standard_calls` `tool_dispatch.ex:793` raises on nil `:genesis_repo_root`) — any standard tool call requires BOTH keys present.
- `do_run/2` validates `Path.join(repo_path, node_path)` exists (`runner.ex:62-70`) → `{:error, :path_not_exist}` if not.
- `do_commit_pending/1` in `AgentScheduler.Dispatch.commit_pending_in_worktree/0` (scheduler side, dispatch.ex:271-312) is the ONE repo-dependent path that is already repo-less-safe: nil `:repo_path` → no-op `:ok`; non-git dir → `Git.status` returns `{:error, {128, _}}` → logged warning, returns `:ok`. Called from `Runner.run/3`'s `after` block (`runner.ex:51`) and `SubagentProcessing.process_subagent_calls/3` (`subagent_processing.ex:60`).
- `CompleteTask.complete/4` (`tools/complete_task.ex:128-190`): `%Result{}` construction sets `commit_sha` (enforced key), `branch` (derived from ETS `lookup_task_info/1` — repo-free, `{:error, :not_found}` → `{0, nil, 0}`), `repo_id` (`Process.get(:evogit_repo_id)`), `usage`; `tag/base_commit/agent_count/archive_records` stay nil. Git-note (`add_metadata_note`) and archive-ref writing (`write_archive_refs`) are both gated on `base_commit` (from `phylo_node`), so a repo-less agent with a nil phylo_node skips them naturally. The dirty-workspace check `CompleteTask.check_workspace_dirty/1` is tolerant (`_ -> {:clean, nil}` on git errors).

## Repo-less Agents

A repo-less agent runs WITHOUT a git worktree (chatbot-style). It is marked via `Process.get(:repo_less)` (set by the runner) and its `Process.get(:repo_path)` points at the real Genesis source root or a placeholder binary (not a worktree). Cardinal rule: **repo-less agents must never touch git or write files.**

- **Write guard**: `EvoGit.Agent.Tools.execute/5` (`tools.ex`) blocks all write tools for repo-less agents, returning `"Error: this agent has read-only access to the system — the <name> tool is disabled."`. The blocked set is the `@write_tools` module attribute (`create_files`, `write_file`, `edit_file`, `make_dir`, `write_context`, `edit_context`, `run_bash`, `run_powershell`, `run_git`, `curl`, `skill_add`, `skill_edit`, `skill_remove`, `skill_enable`, `skill_disable`). Both `execute/5` argument paths (map args and the JSON-string decode fallback) route through the shared `maybe_block_repo_less/5` helper, so there is no bypass. Chained AFTER the repo-less guard is the **foreign-repo role gate** `maybe_block_read_only_foreign_repo/5` (same `tools.ex`): it resolves the agent's repo role from the process dict (`:foreign_repos` — populated by `Runner.setup_dispatch_context/1` from `AgentSpec.foreign_repos` — via `:evogit_repo_id` id-match with a `ForeignRepo.resolve_path/2` root-path fallback) and blocks every `@write_tools` entry when the agent operates inside a foreign repo whose `writable` is not literally `true` (read-only foreign repos are for investigation only). `Shared.validate_file_scope/3` (`tools/shared.ex`) adds a path-level defense-in-depth check: a write target path that resolves to a read-only foreign repo is rejected before the scope check. `read_only_schemas/0` no longer includes `Curl.schema()` (no legitimate read-only role); it keeps `Context.write_schema`/`edit_schema` + `ShellTool` by design — read-only agents' write usage in read-only FOREIGN repos is blocked by the role gate, while CONTEXT.md updates and `mix test` in their own repo remain allowed.
- **One `run_command` shell tool**: the SelfReflective agent exposes a SINGLE `run_command` tool (dispatch-registered only in `tools.ex`). `EvoGit.CommandShell` (declarative compile-time registry, whitelist-only dispatch — no dynamic apply/eval, no `String.to_atom` on input, length guardrails) dispatches the command string to the 10 former tool modules in `agent/tools/` as command handlers (no `schema/0`); each registry entry carries a `level:` and level-2/3 commands block on user approval before their handler runs (see "Command-approval gate" below). `run_command` must NOT be added to `Tools.schemas/0` or `read_only_schemas/0` — every `use EvoGit.Agent` coding agent would otherwise receive task-control, a scope/security violation. `SpawnInvestigator.spawn_investigator` runs a bounded READ-ONLY investigation probe of a given codebase path (no subagent spawn, no LLM — a deterministic pure-filesystem probe; detail in `agent/tools/CONTEXT.md`); the old tool-name collision with the `SubagentSchemas` subagent tool is gone (no per-function schemas remain). Regression-pinned by `tools_test.exs` (asserts `run_command` is absent from `schemas/0`/`read_only_schemas/0` and present in the SelfReflective tool list, with the 10 old names absent).

## Foreign-Repo Commit Propagation (spawn-time resolution + staleness hazard)

Subagents NEVER get a live foreign-repo commit snapshot: the starting commit for a foreign target is resolved at SPAWN time from the task-level `foreign_repos` list plus the parent's scheduler-side roll-up map.

- **Advance authority — the write side lives in the scheduler, not here**: `agent/subagent_processing.ex` only ever READS the tracked map (`AgentScheduler.get_foreign_repo_commits(state.agent_id)` at `:419`) to seed spawns; it never records a foreign-repo commit. A subagent's `Result.foreign_repo_commits` is NOT populated here either (`CompleteTask` leaves the struct default `%{}`) — the scheduler injects it from the agent's own `SchedMeta` at completion (`Lifecycle.handle_task_result/3` → `inject_foreign_repo_commits/2`) before rolling it up into the parent. A foreign repo's tracked commit is advanced ONLY by a completing child that is ITSELF a writable agent running in that repo (its `spec.repo_id` is a task-level `writable: true` foreign repo AND its `agent_type() == :read_write`) — enforced in `agent_scheduler/subagents.ex` (`Subagents.store_sub_result/3,4`); a local/`"primary"` child or a read-only foreign probe never advances it.
- `AgentSpec.foreign_repos` is a struct FIELD (`agent_spec.ex:29`) that also rides inside `opts` (`agent_spec.ex:66`); the list is built ONCE per task by `Runtime.Helpers.build_root_agent_spec/7` (`runtime/helpers.ex:567-597`, after `load_foreign_repos/2` validation at `:306-346`) with the ORIGINAL per-repo `base_sha` (nil = HEAD, never resolved to a concrete sha at load time).
- Registration copies it verbatim into `%AgentState{}.foreign_repos` (`agent_scheduler/dispatch.ex:94`) — there is NO update path afterwards (only the change-detection field list, `agent_scheduler/store.ex:540`).
- `Runner.setup_dispatch_context/1` surfaces it twice: `Process.put(:foreign_repos, spec.foreign_repos || [])` (`runner.ex:220` — the role gate read by `tools.ex:248-276` and `tools/shared.ex:357-368`) and `LoopState.foreign_repos` (`runner.ex:186`).
- A child spec INHERITS the parent's list verbatim — `subagent_processing.ex:132` (`state.foreign_repos`) → `:190` (`foreign_repos: foreign_repos`) — so a child's `base_sha` is the frozen task-level value, never refreshed from completed foreign work.
- The child's phylo node for a foreign target is resolved by `resolve_foreign_phylo_commit/4` (`subagent_processing.ex:673-708`), precedence: (1) non-nil `base_sha` of that inherited entry (`:681-690`); (2) the PARENT's tracked map `AgentScheduler.get_foreign_repo_commits(parent_id)` fetched at spawn-batch time (`:419`, `agent_scheduler.ex:306-312`) (`:692-705`); (3) foreign HEAD (`:694-702`). Precedence (1) over (2) is pinned by `test/evo_git/agent/subagent_processing_test.exs:632-667`.
- A child's OWN `SchedMeta.foreign_repo_commits` starts EMPTY (`dispatch.ex:99-108`; `agent_scheduler/sched_meta.ex:57`) — the tracked map is NOT inherited, so a NESTED agent's foreign spawns can only see `base_sha` or foreign HEAD.
- The LLM `commit_id` arg is silently IGNORED for foreign targets (`subagent_processing.ex:620` binds `_commit_id`), even though the shared schema advertises it as "the commit SHA to spawn the subagent on" (`subagent_schemas.ex:65-70`); it is honored only for `"primary"` targets.
- The child's first-user context carries NO commit/branch info for foreign repos — only the id/root/description table (`context_builder.ex:32-52`) and the ROOT/NESTED authority line (`:71-86`); the worktree is created at `spec.phylo_node.current_commit` (`agent_scheduler/worktrees.ex:107,134`, phylo built at `:312-325`).
- Feedback loop: the parent's in-memory `foreign_repos` is NEVER updated — the only channels are the scheduler roll-up into the parent's `SchedMeta.foreign_repo_commits` (`agent_scheduler/subagents.ex` — `store_sub_result/3,4`) and the parent's textual cross-repo note `"  - <repo_id>: <sha>"` (`subagent_processing.ex:477-484`), which the LLM sees but cannot act on (no API to set a spawn's starting commit).
- STALENESS HAZARD (known issue, READ-SIDE): a non-nil task-level `base_sha` wins unconditionally in `resolve_foreign_phylo_commit/4`, masking all intra-task progress — every later foreign spawn (root OR nested) restarts there, and a NESTED agent's spawn (its own map starts empty, line above) falls back to foreign HEAD. The final per-repo report branch is created from the tracked map (`runtime/helpers.ex:148-160`, writable repos only) while the producing agent's own `evogit-agent-*` branch is deleted when its worktree is reclaimed (`agent_scheduler/worktree_manager.ex:280-298` → `:574-602`), so a spawn seeded from a stale commit can leave an advanced foreign commit unreachable from the reviewed/reported branch.

## Tool Dispatch Flow & Turn-Loop Contract (incl. the run_command approval seam)

- **Schema lists** (`tools.ex`): `schemas/0` (tools.ex:66-108) = the standard coding-agent set (`FileRead`…`SkillWhere` + `WebSearch` when `tools_search_enabled?/0`); `read_only_schemas/0` (tools.ex:127-147) = read set + `Context.write/edit` + `ShellTool` (no `Curl`); `@write_tools` (tools.ex:42-58) = the 15 write tools (`create_files write_file edit_file make_dir write_context edit_context run_bash run_powershell run_git curl skill_add skill_edit skill_remove skill_enable skill_disable`). **`run_command` is in NONE of the three** — dispatch-only (`execute_tool/5` clause tools.ex:351-353), not blocked by the repo-less guard, exposed only via the SelfReflective agent's own `available_tools/0` override.
- **Per-agent whitelist = schema-granting ONLY — there is NO runtime membership filter.** The tool list handed to the LLM each turn is `effective_tools/1` (runner.ex:527-542) = `agent_module.available_tools()` `++ skill_schemas` (hierarchical skills from `runner.ex:129-139`), minus subagent tools at max depth (subagent schemas are injected by the macro default `available_tools/0` = `Tools.schemas() ++ SubagentSchemas.schemas(__MODULE__) ++ [CompleteTask.schema()]`, agent.ex:98-102). At execute time a called tool is NEVER checked against that list; unknown names fall to `execute_tool/5`'s catch-all (tools.ex:355-370) → `"Error: Unknown tool '<name>'"` or a dynamic skill. Protocol violations are only: zero tool calls after 3 nudges (`prompt_until_tools_or_limit/5`, tool_dispatch.ex:159-207), or an empty `process_tool_calls` list (tool_dispatch.ex:662) — both → `handle_protocol_violation/3` (tool_dispatch.ex:309-322). **Consequence for repo-less agents (SelfReflective): a hallucinated tool name NOT in its `available_tools/0` still dispatches by literal name — write tools are blocked by the repo-less guard (`@write_tools`, tools.ex:42-58), but non-write tools with dispatch clauses that are NOT in `@write_tools` and NOT in its list WOULD silently run if the LLM hallucinated them: `skill_list`/`skill_read`/`skill_where` (tools.ex:317-323, 345-347) and `web_search` (tools.ex:301-303 — `WebSearch.execute/3` at web_search.ex:169 has NO runtime `search_enabled` gate; the config gate is schema-level only, tools.ex:146-152, so a hallucinated `search_web` call executes even when search is disabled). `complete_task` is likewise handled by NAME special-case in `process_tool_calls` (tool_dispatch.ex:672-675) regardless of schema membership. Dynamic-skill fallback is inert here (the Genesis source root has no `.agents/skills/`).
- **Execute-time gate chain** (`Tools.execute/5`, tools.ex:167-240; sole call site tool_dispatch.ex:981-987): map-args (or JSON-string-decoded) → **shell-tool alias normalization** (`normalize_tool_name/1`) → `maybe_block_repo_less/5` (tools.ex:194-200 — blocks `@write_tools` when `Process.get(:repo_less)`; message `"Error: this agent has read-only access to the system — the <name> tool is disabled."`) → `maybe_block_read_only_foreign_repo/5` (tools.ex:212-240) → `execute_tool/5`. **Alias normalization runs FIRST, before the write guards, deliberately**: LLMs sometimes call the shell by a familiar external name ("Bash", "Shell", ...) instead of the platform tool name; the well-known aliases (`bash shell sh execute_bash bash_command run_shell shell_command`, plus the platform tool names `run_bash`/`run_powershell` themselves so case variants like "RUN_BASH" normalize) are matched case-insensitively and mapped to the compile-time platform `@shell_tool_name`, so the call actually runs instead of failing with "Unknown tool". It must precede the guards because `run_bash`/`run_powershell` are in `@write_tools` — normalizing AFTER them would let a repo-less (or read-only-foreign-repo) agent bypass the write block by calling "Bash". `Process.get(:repo_less)` is set ONLY in `Runner.setup_dispatch_context/1` (runner.ex:198-219, `Process.put(:repo_less, true)` at runner.ex:212) when the dispatch ctx carries `repo_less: true` (scheduler side, from `spec.opts[:repo_less]`).
- **Flow**: `do_turn` (tool_dispatch.ex:119-146) → `prompt_until_tools_or_limit` → `process_llm_response` (tool_dispatch.ex:422-461: `dedupe_and_sync` on tool calls, turn-tag context, `batch_update_agent` ETS sync) → `process_tool_calls` (tool_dispatch.ex:662-673: `"complete_task"` → `handle_complete_call`/`do_complete`, else `process_regular_tool_calls`) → partition subagent vs standard via `subagent_module_for` (tool_dispatch.ex:780-825) → `batch_execute_tools/4` parallel `Task.async_stream` (tool_dispatch.ex:847-918) → results re-sorted by index, appended to context in `continue_after_tools` (tool_dispatch.ex:528-588).
- **One LLM response = one blocking batch — the ONLY wait-for-user seam is the `run_command` approval gate.** `Runner.loop/1` (runner.ex:315-358) → `do_turn` → `prompt_until_tools_or_limit` (LLM call, tool_dispatch.ex:159-207): ALL `tool_calls` in the assistant message are then executed synchronously in `process_llm_response` → `process_tool_calls` — standard calls in parallel via `Task.async_stream` inside `batch_execute_tools/4` (per-call timeout in `execute_tool_with_timeout/7`, tool_dispatch.ex:964-1011), subagent calls via `AgentScheduler.spawn_sub_agents` (subagent_processing.ex:62 — a blocking call). The loop does NOT return to the LLM until every call has produced a result; results are appended as `:tool`-role messages in `continue_after_tools` (tool_dispatch.ex:570-577) before `loop_fn.(state)` (586) re-enters for the next LLM call. The one deliberate exception — a "wait for a user decision" implemented INSIDE a tool's execution — is the **`run_command` security gate**: `EvoGit.CommandShell` registry entries carry `level:` (1 = read-only, executes immediately; 2 = needs the user's attention, `GuideUser.guide_user`; 3 = real side effects, `StartTask`/`CancelTask`/`ForceKillTask`/`DeleteTask`); at the shell's dispatch choke point (`run_command/3`, command_shell.ex) a level-2/3 command routes through `EvoGit.CommandApproval.request/5` and BLOCKS the tool task (holding its tool slot) until the user approves in the /help chat, the window expires, or the owning task dies — see "Command-approval gate" below.
- **Every tool call gets a result** (string, success or `"Error: ..."`): `execute_tool_with_timeout` maps `{:error, reason}` → `"Error: #{inspect(reason)}"`, crash → `"Error: Tool execution crashed: ..."`, timeout → `"Error: Tool execution timed out after ..."` (tool_dispatch.ex:990-1009); unknown tool → an actionable `"Error: Unknown tool '<name>'. Did you mean '<closest>'? Available tools: ..."` (tools.ex catch-all → `unknown_tool_error/1` + `closest_tool_name/1`: closest name from the tool names DERIVED at runtime from `schemas/0` — private `available_tool_names/0` = `schemas() |> Enum.map(& &1.name)` — by `String.jaro_distance/2` when the score ≥ 0.7, else no suggestion, followed by the full available-tool list, so the LLM can self-correct next turn instead of repeating the bad call; the list can never drift — `run_command` is absent by construction); guards return `"Error: ..."` strings (tools.ex:196, 234). The only call-without-result case is deliberate: a batch CONTAINING `complete_task` executes ONLY `complete_task` (tool_dispatch.ex:664-673) — the task is finishing anyway (dirty-workspace path instead returns a warning result per call, 719-723).
- **The ONLY external message-injection point** is `drain_and_inject_user_messages/1` at the TOP of `loop/1` (runner.ex:337; function 292-313, drains `AgentScheduler.Store.drain_pending_user_messages` runner.ex:293, appends turn-tagged user messages, syncs to ETS at 309). It fires once per turn boundary BEFORE the LLM call — a dashboard decision arriving mid-turn is only visible on the NEXT turn. Injection does not interrupt an in-flight turn.
- **Per-message correlation is limited**: EvoGit attaches only `metadata[:turn]` (integer) + `metadata[:timestamp]` (Unix seconds) — `ContextBuilder.tag_message_turn/2` (context_builder.ex:127-131), `tag_message_timestamp/2` (138-141); tool-result messages carry the provider's `tool_call_id` (built via `ReqLLM.Context.tool_result/3`, tool_dispatch.ex:814) and assistant messages carry `tool_calls: [%ReqLLM.ToolCall{id: ...}]`. `%ReqLLM.Message{}` has NO EvoGit message UUID (fields: role, content, name, tool_call_id, tool_calls, metadata, reasoning_details — deps/req_llm/lib/req_llm/message.ex:108-121). All of it survives verbatim to the dashboard's `get_agent_history` read (ETS `AgentState.context`, raw structs).
- **`message_count` is NOT computed in this node**: it is added only to the `:agent_updated` broadcast kwlist in the sibling `EvoGit.AgentScheduler.Store` — `batch_update_agent/2` (`apps/evo_git/lib/evo_git/agent_scheduler/store.ex:410-436`, `[message_count: length(messages)]` at 426) and the `put_agent_state` insert path (139-150). This node only calls `AgentScheduler.batch_update_agent/2` / `update_agent_context/1`; the ETS write itself is verbatim.
- **Tool events — NO per-tool broadcast.** Tool outputs live only in the conversation context (tool_result messages). The agent's only dashboard-visible emissions are the ETS syncs → `AgentScheduler.Store.batch_update_agent/2` (store.ex:410-436) → `PubSub.broadcast_agent_updated` with `{:agent_updated, agent_id, changed_fields, node}` where changed_fields = `[message_count: length(messages)] ++ strip_heavy_fields(fields)` (`:context` stripped). Sync points around tools: tool_dispatch.ex:433-438 (per LLM response, pre-execution: context/turn/usage/total_tokens), tool_dispatch.ex:581 (post-tool-results context append in `continue_after_tools`), tool_dispatch.ex:584 (usage), tool_dispatch.ex:841 `sync_current_commit_after_tools` (phylo commit sync after each standard batch, repo-less skip at tool_dispatch.ex:52-54). Consumers detect "a tool ran" via `message_count` growth + `get_agent_history/1` re-fetch; live streaming text/tool state is rendered from these deltas (see agent_scheduler CONTEXT "Throttled PubSub").
- **Per-tool metadata read at dispatch** (tool-name-keyed): `args["timeout"]` honored per call, `min(default_tool_timeout(), max_tool_timeout())` caps (tool_dispatch.ex:973-976; config `[:scheduler, :default_tool_timeout]` 10s / `[:scheduler, :max_tool_timeout]` 30min, delegation_hints.ex:31-36); output cap via `@high_output_tools` MapSet (output_sanitizer.ex:24-34 — run_bash/run_powershell/read_file/rg/curl/run_git/search_web/search_context/search_history get `[:truncation, :tool_output_default_max_bytes]` 16KB vs global `:tool_output_max_bytes` 128KB; per-call `args["max_bytes"]` overrides, capped at global, output_sanitizer.ex:221-233); `@write_tools` classification (both guards); delegation-hint write/read tool classes `@write_tools_for_delegation`/`@read_tools_for_delegation` (delegation_hints.ex:15,17); redundant-cd warning only for run_bash/run_powershell (tool_dispatch.ex:1086-1100).

### Command-approval gate (run_command level-2/3, EvoGit.CommandApproval)

The command shell's security levels are enforced INSIDE the `run_command` tool call (`EvoGit.CommandShell.execute/1` — the `:chat` approval mode) — the tool task blocks until the user decides. (`CommandShell.execute/2` also supports `approval: :auto`, used by the CLI `run` subcommand, which bypasses the gate for all levels — the terminal user is the human authorizer.) Facts:

- **Levels**: `EvoGit.CommandShell.security_level/1` maps each command path to 1|2|3 (`"help"`/`"Help"` → 1; unknown/non-binary → 1). Level 1 executes immediately; levels 2 (`GuideUser.guide_user`) and 3 (`StartTask.start_task`, `CancelTask.cancel_task`, `ForceKillTask.force_kill_task`, `DeleteTask.delete_task`) do NOT run until the user approves. Parse/validation errors still surface BEFORE the gate (the args parse first).
- **Gate mechanics**: at the shell choke point (`EvoGit.CommandShell.run_command/3`), level ≥ 2 calls `EvoGit.CommandApproval.request(path, human_args, level, Process.get(:evogit_agent_id), Process.get(:evogit_task_id))` — a blocking `GenServer.call(:infinity)`. `:approved` → the handler runs; `:denied` → `{:error, "Action denied by the user: ..."}`; `:timeout` → `{:error, "The user did not confirm ... in time ..."}` — the handler NEVER runs on anything but `:approved` (fails closed, including when the approval service is not running).
- **Approval window**: bounded per request by app env `[:evo_git, :command_approval_timeout]` (default `120_000` ms), read at request time. When the window expires the caller is replied `:timeout` and a `{:approval_resolved, request_id, :timed_out}` broadcast goes out.
- **Task-lifecycle auto-deny**: `EvoGit.CommandApproval` subscribes to the `"tasks"` PubSub topic in init; a `{:task_updated, task_id, status, node}` with `node == node()` and `status in [:cancelling, :completed, :failed, :cancelled]` auto-resolves `:denied` every pending request carrying that `task_id` (a graceful Stop/cancel never stalls on an approval wait). Caller death (each waiting caller is monitored) cleans the request up and resolves it `:timed_out`.
- **PubSub contract** (topic `"approvals"`): `{:approval_requested, request}` where `request = %{request_id: "aprv-…", command, args, level, agent_id, task_id, node}` and `{:approval_resolved, request_id, decision}` (`:approve | :deny | :timed_out`). Every requested card eventually resolves (invariant: one requested → one resolved).
- **ToolDispatch timeout extension** (`execute_tool_with_timeout/7`): an approval-requiring `run_command` call is detected up front (`approval_window_tool_call?/2` peeks `CommandShell.security_level/1` of the first whitespace token) and gets a per-call timeout of `max(configured, EvoGit.CommandApproval.timeout() + @approval_execution_buffer 30_000)`, capped by the scheduler's `max_tool_timeout` (30 min) — otherwise the 10s default tool timeout would kill the task mid-wait. Level-1 `run_command` calls keep their exact current behavior.
- **Agent/task context**: spawned tool tasks do NOT inherit the caller's process dictionary, so `execute_tool_with_timeout/7` re-establishes `:evogit_agent_id`/`:evogit_task_id` inside the task for `run_command` calls (`AgentScheduler.task_id_for_agent/1`); direct/test callers have neither key and the request registers with `nil` ids (still gated by request_id and its own task lifecycle).
- **RPC responders**: `EvoGit.AgentScheduler.RemoteAPI.respond_approval/2` (→ `EvoGit.CommandApproval.respond/2`; `{:error, :approval_not_started}` when the service is down) and `EvoGit.RemoteNode.respond_approval/3` (local direct call / remote `:erpc`).
- **Dispatch plumbing**: `run_command` is dispatch-registered ONLY in `tools.ex` — never in `schemas/0` or `read_only_schemas/0` (giving every coding agent task-control access would be a scope/security violation); the approval gate is defense-in-depth layered on top of the dispatch whitelist.
