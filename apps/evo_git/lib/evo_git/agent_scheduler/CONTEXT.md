# Agent Scheduler Data Structures and Helper Modules

## Intent

Contains data structs and extracted helper modules used internally by `EvoGit.AgentScheduler` GenServer. The data structs back two ETS tables (`:evogit_agent_state` and `:evogit_sched_meta`) tracking agent execution state and scheduling metadata. Helper modules encapsulate slot management, worktree lifecycle, agent dispatching, subagent management, and agent lifecycle logic.

## Routing Table

None — leaf directory (modules: `state.ex`, `agent_state.ex`, `sched_meta.ex`, `slots.ex`, `store.ex`, `worktrees.ex`, `worktree_manager.ex`, `worktree_retry.ex`, `dispatch.ex`, `subagents.ex`, `lifecycle.ex`, `pubsub.ex`, `throttle.ex`, `remote_api.ex`).

## Throttled PubSub (PubSub + throttle.ex)

- `EvoGit.AgentScheduler.PubSub` — throttled `{:agents_updated, node}` bulk broadcast plus delta broadcasts `{:agent_registered, agent_id, summary, node}` / `{:agent_updated, agent_id, changed_fields, node}` / `{:agent_removed, agent_id, node}`. Every broadcast carries a trailing `node` element (the emitting BEAM node), added by the helpers themselves.
- **Registered-summary key set** (`store.ex:67-74`): the `:agent_registered` `summary` map has EXACTLY `status, depth, parent_id, task_id, task_number, objective` — it does NOT include `message_count` or `agent_module` (those live only in the `list_agents/0` RPC summary, `RemoteAPI.build_agent_summary/3`, remote_api.ex:1009-1078 — a different shape, 20 keys). Dashboard code must not dot-access `summary.message_count` on registration events (KeyError). `message_count` arrives only via `:agent_updated` deltas (prepended as `[message_count: n]` when a context is written — `store.ex:143-149, 420-430`).
- `EvoGit.AgentScheduler.PubSub.Throttle` (throttle.ex) — small GenServer, standard child of the application supervision tree: declared in `EvoGit.Application`'s `children` immediately after `{Phoenix.PubSub, name: EvoGit.PubSub}`, runs under `EvoGit.Supervisor` (one_for_one, default `:permanent` restart) — a crash restarts it instead of silently degrading to unthrottled broadcasts forever.
- `broadcast_agents_updated/0` casts `:schedule` to the Throttle (coalesces rapid calls into one flush `@throttle_ms` = 200ms after the last schedule). When `Process.whereis(EvoGit.AgentScheduler.PubSub.Throttle)` is nil (tests, supervisor restart window), broadcasts fall back to **immediate dispatch**. If the Throttle exhausts the supervisor's max_restarts, `EvoGit.Supervisor` dies and takes the app down — standard OTP.

## Defensive ETS lookups in Subagents

`Store.get_sched_meta/1` / `Store.get_agent_state/1` return `{:ok, x} | :error`. Four sites in `subagents.ex` use `case`-guarded lookups with graceful fallbacks (log warning + safe return) instead of bare `{:ok, x} = Store.get_*(id)` matches — a missing ETS entry (e.g. a parent's entries reaped by `cancel_agent/2` while a subagent spawn/result is in flight) never crashes the scheduler GenServer with a `MatchError`:

1. `spawn_validated_subagents/5` — missing parent agent_state → replies `[{:error, :parent_recycled}]` per spec to the blocked `from`, spawns nothing, returns `{:noreply, state}` (bails BEFORE the `:waiting` status mutation).
2. `store_sub_result/3,4` — missing parent sched_meta → `:ok` (result dropped; parent gone).
3. `maybe_resume_parent/2` — missing parent sched_meta → state unchanged.
4. `dispatch_ready_parent/3` — missing agent_state (only needed for the resume log's commit SHA) → still replies/resets meta, logs `"unknown"` SHA.

Normal-path behavior is identical for all four sites. Error paths are covered by `./apps/evo_git/test/evo_git/agent_scheduler/subagents_test.exs` (e.g. the `:parent_recycled` reply) and the Throttle by `pubsub_test.exs`.

## Agent identity: parent_id & depth (root ⇔ parent_id nil ⇔ depth 0, by construction)

- `Dispatch.register_agent/7` (dispatch.ex:42-124) is the ONLY writer of both identity fields, from two call sites: ROOT via `handle_call({:run_agent, spec}, ...)` (agent_scheduler.ex:688-697 — `_parent_id = nil, _depth = 0`), NESTED via `spawn_validated_subagents/5` → `Dispatch.register_agent(..., parent_id, parent.depth + 1, ...)` (subagents.ex:86-95). So the invariants hold: root ⇔ `AgentState.parent_id == nil` ⇔ `SchedMeta.depth == 0`; every nested agent has `parent_id == <its parent's integer id>` and `depth == parent.depth + 1`.
- `%AgentState{}` (agent_state.ex:41-66) carries `parent_id` (line 50, `pos_integer() | nil`) but has NO `depth` field — only `max_depth` (the recursion LIMIT, not current depth). `%SchedMeta{}` (sched_meta.ex:38-58) carries BOTH: `depth` is an `@enforce_keys` field (line 38/41, `non_neg_integer()`) and `parent_id` (line 43). Agent depth is therefore stored in the `:evogit_sched_meta` ETS row, never on AgentState; it is not derived by walking a parent chain.
- Depth reaches the agent process as a separate argument: dispatch_ctx `depth: meta.depth` (dispatch.ex:215) → Runner `depth = Keyword.fetch!(ctx, :depth)` (runner.ex:172) → `LoopState.depth` (`@enforce_keys`, loop_state.ex:35/43) + `Process.put(:evogit_agent_depth, depth)` (runner.ex:181). Crash-retry resets preserve identity: `%AgentState{agent_state | phylo_node: nil, context: nil}` (lifecycle.ex:162).
- The writable-foreign-repo spawn gate's ROOT check is `parent_depth == 0` (subagents.ex:274), where `parent_depth` = the parent's `SchedMeta.depth` — passed as a plain integer through `validate_single_subagent/5` (subagents.ex:171: `validate_spatial_contract_for_spec(parent_id, parent_agent_state, spec, parent.depth)`); the gate never reads `AgentState.parent_id`. Because depth and parent_id are written together from the same two call sites, `parent_depth == 0` is exactly equivalent to `parent_id == nil` for real runs. The `/3` form (subagents.ex:244-246) hardcodes `0` (parent treated as root) — backward-compat only; `validate_single_subagent/5` always uses `/4`.
- dispatch.ex's root-only custom-agent logic (resolve_max_turns, dispatch.ex:464-500) uses the SAME equivalence the other way: `if is_nil(parent_id)` (dispatch.ex:475) selects the custom `max_turns` for roots; subagents (parent_id set) always use `state.max_turns`.

## Task Spawn & Starting-Commit Flow (run_agent)

The **starting commit is NOT decided in this directory** — it arrives pre-resolved as `spec.phylo_node.current_commit` (a `%PhyloGraphNode{repo, base_commit, current_commit}` built by the runtime phase). Upstream resolution: `Runtime.Evolution.run/2` (`runtime/evolution.ex`) and `Runtime.Genesis` (`runtime/genesis.ex`) call `Runtime.Helpers.resolve_starting_commit/2` (`runtime/helpers.ex:402-447`, the single choke point): nil → `PhyloGraphNode.current_head/1` (HEAD; unborn-HEAD repos fail with a structured error); a ref → `Git.rev_parse(repo_path, ref <> "^{commit}")` (commit-peeling: valid SHA/branch/lightweight tag resolve, an annotated tag peels to its commit, a ref to a tree/blob or an unknown ref fails). Failures return the structured `{:error, {:invalid_starting_commit, ref, repo_path, git_output}}` — never the raw git tuple — which the evolution/genesis phase else-arms map to a human-readable message string via `Helpers.format_invalid_starting_commit_error/3` (persisted as the failed-task result). Ref sources: CLI evolve `--starting-commit` (parser switch `starting_commit: :string`, `cli/parser.ex` — the CLI passes it through as task opt `:starting_commit`); task resume (`TaskRegistry.ResumeContext.apply_resume_context/3`, `task_registry/resume_context.ex:43-54` — sets `:starting_commit` to the previous task's `commit_sha`); Genesis Mode B's second root starts from `architect_output.commit_sha || base_sha` (`runtime/genesis.ex:143-144`).

Spawn flow: `AgentScheduler.run_agent/2` (`agent_scheduler.ex:80-82`) → `handle_call({:run_agent, spec}, ...)` (agent_scheduler.ex:649-712 — two rejection clauses at :649-662 reply `{:error, :llm_not_configured}`, main handler at :664-712): derives `task_id` from `spec.opts[:task_id]` (else random hex, :665-666); refuses `{:error, :cancelled}` when the task is in the `:evogit_cancelling_tasks` marker (blocks Genesis Mode B's second root during cancel, :673-678); resets per-task archive (`Lifecycle.clear_archive_records`, :683); resolves repo root (`Dispatch.resolve_agent_repo_root/2`, dispatch.ex:523-538); computes `task_number = Dispatch.next_task_number/1` (scans `.genesis/workers/worker_T*`, dispatch.ex:131-148); then `Dispatch.register_agent/7` (dispatch.ex:42-124: `id = state.next_agent_id` at :51; `task_local_id` from `state.task_local_counters` starting at 1, :54-59; writes `%AgentState{}` with `phylo_node: nil` + `%SchedMeta{}` carrying the full original `spec`, :79-108). Paused → queued; else `Dispatch.try_dispatch/2` (dispatch.ex:174-247): computes `worktree_path = <repo_root>/.genesis/workers/worker_T#{task_number}_A#{task_local_id}`, stores it in SchedMeta BEFORE spawning, then `Task.Supervisor.async_nolink(EvoGit.TaskSupervisor, spec.agent_module, :run, [spec.objective, dispatch_ctx])` (:225-231); the agent's Runner requests the fresh worktree from `WorktreeManager.create_worktree_for_agent/6` (runner.ex:265-278, inside `setup_dispatch_context/1` at runner.ex:199-289).

**Checkout to the starting commit** happens in `Worktrees.create_worktree/6` (private, worktrees.ex:106-147): `commit_sha = spec.phylo_node.current_commit` goes to `EvoGit.Adapters.CowWorktree.create_worktree/5` (or fallback `Git.add_worktree(repo_root, worktree_path, commit_sha, branch_name)`), then `assign_and_prepare_worktree/3` (worktrees.ex:295-345) — after the registered-linked-worktree guard (`ensure_linked_worktree/2`, worktrees.ex:347-388: asserts `wt/.git` is a FILE with `gitdir: <path>` under `<repo_root>/.git/worktrees/`; refuses `{:error, {:worktree_prepare_failed, :not_a_linked_worktree}}` WITHOUT running git against a plain dir — see "Worktree Safety" below) — runs `Git.clean` + `Git.checkout(wt, branch_name)` and writes the worktree-bound phylo_node (`repo: wt`, `base_commit: spec.phylo_node.base_commit`, `current_commit: commit_sha`) into AgentState ETS — this is where `AgentState.phylo_node` transitions from nil to the live node. During the run the agent advances it via `ToolDispatch.sync_current_commit_after_tools/1` / `sync_and_get_current_commit/1` (agent/tool_dispatch.ex:50-84) → `AgentScheduler.update_phylo_node/2` (agent_scheduler.ex:226-230). Subagent phylo nodes: `build_subagent_phylo_node/6` (agent/subagent_processing.ex:569-601) — explicit `commit_id` validated via `Git.rev_parse`, else parent's `current_commit`; cross-repo uses tracked `foreign_repo_commits` or foreign HEAD (:603-614). Both creation paths (CoW and `Git.add_worktree` fallback) handle gitlink/submodule entries like `git worktree add`: submodule dirs arrive in worktrees as **empty placeholders** (never CoW-copied — `Git.ls_tree_names/2` excludes gitlinks); `Git.clean/1` (`git clean -fd`) neither deletes the tracked placeholder dirs nor fails; populate via `git submodule update --init`.

**No reuse / no resume-checkout special-casing in the scheduler layer**: `prepare_new_worktree/5` always destroys leftovers (dir, branch) and creates fresh (worktrees.ex:61, 139-170) — a branch that already has commits is deleted first, then re-created from the spec's starting commit. The only exception is `Subagents.dispatch_ready_parent/3` (subagents.ex:355-390): a waiting parent keeps its LIVE worktree (no new checkout). A resumed TASK's "existing commits" are handled entirely upstream by setting `:starting_commit` in opts (see `task_registry/resume_context.ex`).

## Subagent Spawn Fan-Out Bounds & Deadlock Surface

**Spawn is a blocking batch.** `AgentScheduler.spawn_sub_agents/2` (agent_scheduler.ex:101-109) is `GenServer.call(__MODULE__, {:spawn_sub_agents, parent_id, specs}, timeout)` with default `timeout \\ :infinity`; the parent agent process blocks inside it (called from `SubagentProcessing.process_subagent_calls/3` at subagent_processing.ex:63 with no timeout arg ⇒ `:infinity`). The reply arrives only when ALL subagents complete (`Subagents.dispatch_ready_parent/3` via `maybe_resume_parent/2`, subagents.ex:442-518). The scheduler GenServer itself is NOT blocked — the handler returns `{:noreply, state}` (subagents.ex:126-136).
**Batch size is UNBOUNDED.** `spawn_validated_subagents/5` `Enum.reduce`s over all `specs` (subagents.ex:57-111) with no cap; the spec list = the subagent tool calls in ONE LLM response (`build_subagent_specs/3`, subagent_processing.ex:139 `Enum.map`).
**No spawn-side total caps exist.** There is no agent cap / per-parent child cap / batch cap / live-worktree cap. The only spawn-time gates are: `max_depth` (depth, subagents.ex:181-191), `:path_ignored`, the spatial contract, one-writable-foreign-repo-per-batch serialization (subagents.ex:73-83), scheduler-paused `{:error, :scheduler_paused}` (agent_scheduler.ex:928-929), and `:llm_not_configured` (agent_scheduler.ex:649-662). Worktrees are fresh-per-run (no pool). The ONE cap in this area is on worktree CREATION concurrency — WorktreeManager's bounded FIFO admission queue (`:max_concurrent_worktree_creation`, default 4, see "Worktree Allocation & I/O Concurrency Model") — which bounds concurrently-running create PIPELINES only, never live worktrees/agents or spawn breadth.
**Registration is EAGER at accept.** Each accepted spec → `Dispatch.register_agent/7` writes `:evogit_agent_state` + `:evogit_sched_meta` (dispatch.ex:79-108, status `:pending`), then `Dispatch.try_dispatch/2` (subagents.ex:126) sets status `:running` and spawns the child Task immediately (dispatch.ex:225-231). There is NO global spawn queue (the `state.queue` is only used when the scheduler is paused — agent_scheduler.ex:703-706); children pile up as `:running` tasks blocked on worktree/LLM-slot.
**Worktree is created EAGERLY at child start, NOT on slot grant.** The child Task's Runner calls `WorktreeManager.create_worktree_for_agent/6` in `setup_dispatch_context/1` (runner.ex:265-278) BEFORE `loop/1`; the first LLM-slot request happens later inside `do_turn` → `call_llm_with_retry` (tool_dispatch.ex:250). So a child HOLDS its worktree while blocked waiting for an LLM slot — the LLM-slot throttle does NOT bound the number of live worktrees/agents. `WorktreeManager` create work is offloaded to a spawned Task per agent_id, admitted through the bounded creation queue (`:max_concurrent_worktree_creation`, default 4); that caps concurrently-running create PIPELINES (plus per-agent serialization) but does NOT bound live worktrees/agents.
**Deadlock surface for a naive worktree/agent cap.** A WAITING parent keeps its LIVE worktree (its process is alive, blocked on the `:infinity` `spawn_sub_agents` call); reclaim is `:DOWN`-only (WorktreeManager), so the parent's worktree is not freed until all its children complete (subagents.ex:453-455, 473-478). A waiting parent holds NO LLM slot (`with_llm_slot` wraps only the LLM call; slot released per attempt — agent_scheduler.ex:396-411, tool_dispatch.ex:237-250) and NO tool slot (standard tools run BEFORE the subagent spawn and each releases its slot — tool_dispatch.ex:797-804, 998). Consequence: a cap on LIVE worktrees/agents with QUEUEING that a parent's children must acquire can deadlock — a waiting parent occupies a worktree slot its own (transitively required) descendants need, so with enough simultaneously-waiting ancestors all slots are held, descendants never start, and ancestors never resume. Any such cap must guarantee every live ancestor's children can proceed, or the parent must release/checkpoint its worktree while waiting (it already commits before spawning via `commit_pending_in_worktree/0`, subagent_processing.ex:61, but the design keeps it live). The IMPLEMENTED cap instead bounds CREATE-pipeline concurrency and is deadlock-free — a permit is held only for one pipeline's duration and a queued request holds none (see "Worktree Allocation & I/O Concurrency Model" → "Why a creation-concurrency cap is deadlock-free").

**`max_depth`** — config key `[:scheduler, :max_agent_depth]` (definitions.ex:77-84), type `:pos_integer`, default `8`, validation `[min: 1]` (no max). Scheduler state default `max_depth: 8` (state.ex:70). Enforced at spawn: `subagent_depth = parent.depth + 1` (subagents.ex:166), `validate_subagent_depth` errors `{:error, :max_depth_exceeded}` when `subagent_depth > state.max_depth` (subagents.ex:181-191). Also stored on AgentState (`max_depth`, dispatch.ex:86), used by Runner `at_max_depth?` (runner.ex:579-581), and surfaced to the LLM via the subagent schema (`max_agent_depth`, tool_dispatch.ex:775). It bounds DEPTH only, never fan-out.
**`max_turns`/`max_turns_root`** — `[:scheduler, :max_turns]` default 100 (subagents), `[:scheduler, :max_turns_root]` default 1000 (root only) (definitions.ex:96-119, both `:pos_integer` `[min: 1]`; state.ex:74-75). `resolve_max_turns/3` (dispatch.ex:474-500): roots use `max_turns_root` (or a custom root agent's positive-integer `max_turns` override), subagents use `max_turns`.
**`delegation_level`** is NOT used in this subtree — it is an `EvoGit.Agent` behaviour callback (`:high`/`:low`, macro default `:high`, agent.ex:152) consumed by the turn-budget warning / delegation-hint system (runner.ex:188, turn_warning.ex, delegation_hints.ex); it never gates spawning.

## Agent-facing submodule awareness (repo_notes)

`AgentSpec.repo_notes` (`String.t() | nil`) carries the **already-rendered** git-submodules note block — rendered ONCE at root spec build by `EvoGit.Runtime.Helpers.load_repo_notes/2` (calls `Git.ls_tree_gitlinks/2`; nil when the repo has no gitlinks or detection fails — graceful degradation, no `try/rescue`). Registration copies it into `AgentState.repo_notes` (`Dispatch.register_agent/7`, dispatch.ex:95, alongside `foreign_repos:`); the Runner copies it into `LoopState.repo_notes` (runner.ex:152) and renders it into the agent's `<context>` block via `ContextBuilder.build_repo_notes_section/1` (blank when nil — dropped by the blank-filter, so repos without submodules get no noise). **Subagents inherit the rendered text** — `SubagentProcessing.build_subagent_specs/3` reads `state.repo_notes` (subagent_processing.ex:131) and passes `repo_notes:` to the subagent `AgentSpec.new(...)` (no re-detection; a foreign-repo subagent still carries the PRIMARY repo's note). Detection happens at the primary-repo root specs only: `Runtime.Evolution.run_resolved_root_agent/7` (evolution.ex:118) and `Runtime.Genesis` Mode A / Mode B architect / Mode B implementation roots (genesis.ex:49, 114, 192) — Mode B's second root detects against the architect's final commit tree (`architect_output.commit_sha || base_sha`).

## Repo-Nil Tolerance & Repo-Less Agent Plumbing

Facts about which scheduler paths tolerate `nil` repo/worktree fields and which hard-dereference them. The repo-less self-reflective agent (`EvoGit.Agents.SelfReflective`, `spec.opts[:repo_less] = true`; phylo_node nil, repo root = Genesis source root or `"[system]"`) runs on this plumbing — nothing in this subtree runs git or creates worktrees for repo-less agents.

**Nil-tolerant:**
- `AgentState.phylo_node` defaults nil (agent_state.ex:44), written nil at registration (dispatch.ex:81) and on crash-retry reset (lifecycle.ex:162) — a transient nil phylo_node in ETS is the NORMAL state.
- `AgentState.repo_root` defaults nil (agent_state.ex:60); `Dispatch.resolve_agent_repo_root/2` returns nil when a foreign repo id is missing from `spec.foreign_repos` (dispatch.ex:525).
- `RemoteAPI.get_agent_state/1` (remote_api.ex:125-130) only drops `:context`; `get_agent_history/1` (remote_api.ex:102-113) reads only `state.context`.
- `Dispatch.commit_pending_in_worktree/0` is repo-less-safe: `Process.get(:repo_less)` → immediate `:ok` (never git-commits the Genesis source root); otherwise `if wt = Process.get(:repo_path)` → `:ok` when unset.
- Runner skill loading guards nil repo_root (`if repo_root && is_binary(repo_root)`, runner.ex:119).
- `RemoteAPI.build_agent_summary/3` nil-guards `phylo.current_commit`/`phylo.base_commit`/`context_node.path` (`safe_phylo_commit/2`/`safe_context_path/1`): repo-less agents emit `current_commit: nil`/`base_commit: nil` instead of crashing the dashboard's `list_agents` RPC.
- `Subagents.dispatch_ready_parent/3` logs `"unknown"` commit for a repo-less parent (nil phylo_node) instead of raising KeyError.

**Hard-dereferences (crash on nil — repo-less agents must guard/avoid):**
- `Dispatch.resolve_agent_repo_root/2` primary branch: `String.split(spec.phylo_node.repo, "/.genesis/workers/", parts: 2)` — nil `spec.phylo_node` → FunctionClauseError for NORMAL agents; repo-less agents short-circuit to a binary BEFORE touching `spec.phylo_node`. Called from `register_agent` (dispatch.ex:69) and `do_try_dispatch` (dispatch.ex:204 — the latter runs in the scheduler GenServer process, so a crash takes the scheduler down).
- `Dispatch.do_try_dispatch`: `worktree_path = Path.join([agent_repo_root, ".genesis/workers", ...])` — nil root raises ArgumentError; repo-less agents get a binary from `resolve_agent_repo_root/2`. Their `worktree_path` is synthetic (stored in SchedMeta for cancel lookup, NEVER created on disk — the Runner skips `create_worktree_for_agent`).
- `WorktreeManager.maybe_init_repo/2` → `Worktrees.workers_dir/1` (`Path.join(repo_root, ".genesis/workers")`, worktrees.ex:26) — nil root raises ArgumentError inside the WorktreeManager GenServer.
- `Runner.setup_dispatch_context/1` (runner.ex:158-211) UNCONDITIONALLY calls `WorktreeManager.create_worktree_for_agent(agent_id, repo_root, worktree_path, spec, meta, self())` and raises `"Failed to create worktree..."` on `{:error, reason}` (runner.ex:188-201) — the agent Task crashes → scheduler crash-retry. `Process.put(:repo_path, worktree_path)` (runner.ex:179) is what `commit_pending_in_worktree` reads. `Runner.do_run/2` also requires `Process.get(:repo_path)` (`Path.join(repo_path, node_path)` + `File.exists?` validation, runner.ex:62-70).
- `Worktrees.assign_and_prepare_worktree/3` (worktrees.ex:295-345): bare matches `{:ok, meta} = Store.get_sched_meta(agent_id)` / `{:ok, %AgentState{}}`, reads `spec.phylo_node.current_commit`, FIRST runs the registered-linked-worktree guard `ensure_linked_worktree/2` (worktrees.ex:347-388 — `.git`-file check, see "Worktree Safety"; refuses `{:error, {:worktree_prepare_failed, :not_a_linked_worktree}}` without running git), then runs `Git.clean`+`Git.checkout`, then writes the worktree-bound phylo_node (`repo: wt`) into AgentState. `Worktrees.branch_name/2` (worktrees.ex:31-33) is repo-agnostic — only used for branch naming.
- `SchedMeta` (sched_meta.ex:39-58) carries `worktree` (nil default, set at dispatch), `task_number`, `spec` (full `%AgentSpec{}` incl. phylo_node/repo_id/foreign_repos), `foreign_repo_commits`; no `repo_root` field. `Slots` has NO repo-specific state (pure per-model_id LLM/tool pools).
- `commit_pending_in_worktree/0` is fully safe on missing/non-git paths (dispatch.ex:271-312): nil `:repo_path` → skips, `:ok`; missing path → `Git.run/2`'s `File.dir?` guard returns `{:error, {:enoent, ...}}` (git.ex:64-66); bare non-git dir → `git status --porcelain` exits 128 → `{:error, {128, "fatal: not a git repository..."}}` (git.ex:89-95); both fall into the `with`'s `else error -> Logger.warning(...)` arm → `:ok`. Never raises. Call sites: `Runner.run/3` `try/after` (runner.ex:48-52 — cannot mask a `do_run` exception) and `SubagentProcessing.process_subagent_calls` (subagent_processing.ex:60, inline before `spawn_sub_agents`).
- `dispatch_ctx` keys (dispatch.ex): `[agent_id:, depth:, repo_root:, repo_id:, worktree_path:, retries:, spec:, meta:]` — all `Keyword.fetch!`'d in `setup_dispatch_context` (runner.ex:159-166); `meta` = full `%SchedMeta{}` (status `:pending | :running | :waiting | :ready | :blocked`). Repo-less agents additionally carry `repo_less: true` + `repo_less_repo_path: <resolved root>`. `SchedMeta.worktree` defaults nil (sched_meta.ex:48), set in `do_try_dispatch` BEFORE the task spawn.
- Child AgentState gets `repo_id` + `repo_root` only — no worktree_path field (dispatch.ex:79-95): `phylo_node` starts nil, bound to the worktree later by `Worktrees.assign_and_prepare_worktree/3`. Each subagent gets its OWN fresh worker dir under the repo root (computed in `do_try_dispatch`), never the parent's path.
- Subagent validation never checks repo existence (`validate_single_subagent/5`, subagents.ex:130-144): only depth, `ContextNode.is_ignored?`, and the spatial contract (which reads `spec.context_node.path` — nil context_node would raise via `.path` access). A nil/phantom-repo spec passes validation and crashes later in dispatch/worktree creation.

**Repo-less plumbing specifics:**
- `Dispatch.resolve_agent_repo_root/2` resolves repo-less roots FIRST via the SHARED chain `EvoGit.SelfReflectiveSource.reference_path/0` (`:self_reflective_source_root` app env → `GENESIS_SOURCE_ROOT` env var → valid managed clone at `<data_dir>/genesis-source`), terminal fallback `File.cwd!()` (dev = the genesis repo) → `"[system]"`. `EvoGit.Runtime.SelfReflective.source_root/0` delegates to the same `reference_path/0` (with `File.cwd!()` fallback) so both always agree.
- `Dispatch.do_try_dispatch` appends `repo_less: true` + `repo_less_repo_path: <resolved root>` to the dispatch_ctx (the Runner uses `repo_less_repo_path` as its `Process.get(:repo_path)` and SKIPS `create_worktree_for_agent`).
- `Dispatch.commit_pending_in_worktree/0` no-ops for repo-less agents (guarded on `Process.get(:repo_less)`) — never `git add/commit` against the real Genesis source root.
- Runner-side (outside this subtree, `agent/`): `Runner.setup_dispatch_context/1` skips `create_worktree_for_agent` and sets `:repo_path` from `repo_less_repo_path`; `ToolDispatch.sync_current_commit_after_tools` is skipped (no commits to track).

## API Surface

| Module | Description |
|---|---|
| `EvoGit.AgentScheduler.State` | GenServer state struct — configuration, agent lifecycle queues, slot holder sets |
| `EvoGit.AgentScheduler.AgentState` | Live agent state in `:evogit_agent_state` ETS — context_node, phylo_node, objective, repo_id, foreign_repos, `repo_notes` (already-rendered git-submodules note block, `String.t() \| nil`; set at registration from `spec.repo_notes`, rendered into the agent's `<context>` block, inherited by subagents via LoopState — see "Agent-facing submodule awareness") |
| `EvoGit.AgentScheduler.SchedMeta` | Scheduler-private metadata in `:evogit_sched_meta` ETS — status, depth, worktree, subagent tracking |
| `EvoGit.AgentScheduler.Slots` | Pure-function LLM/tool slot management using holder MapSets with FIFO queuing and rate-limit backoff |
| `EvoGit.AgentScheduler.Worktrees` | Pure helper functions for the worktree create pipeline (no own state) — **the single source of truth for worker-dir and repo-root path derivation**: `workers_dir/1` and `repo_root_from_worktree/1` (new code deriving worker-dir or repo-root paths should use these helpers instead of re-deriving the paths); `assign_and_prepare_worktree/3` (registered-linked-worktree guard via `ensure_linked_worktree/2` — `.git`-file check against `<repo_root>/.git/worktrees/`, refuses `{:error, {:worktree_prepare_failed, :not_a_linked_worktree}}` without running git — then git clean + checkout agent branch + bind `phylo_node.repo = worktree` in ETS; `repo_root` is an explicit param, resolved by `Dispatch.resolve_agent_repo_root/2`, NOT `spec.phylo_node.repo` which for subagents is the parent's worktree path), `run_init_script/3` (genesis.toml `[worktree]` script; env `SOURCE_REPO_PATH`/`SOURCE_WORKTREE_PATH`/`TARGET_WORKTREE_PATH`), `prepare_new_worktree/5` (fresh-create pipeline: destroy leftover dir/branch via `destroy_leftovers/3` — rm_rf failure ESCALATES to `{:error, {:worktree_create_failed, msg}}` and skips the prune (dir+registration consistency, see "Worktree Safety"); same retried `WorktreeRetry` helpers, never raises (runs in the offloaded create task) → CoW create via `EvoGit.Adapters.CowWorktree.create_worktree` with `Git.add_worktree` fallback → assign_and_prepare → init script for primary repo only), `branch_name/2` (single source of truth: `"evogit-agent-T#{task_number}-A#{task_local_id}"`), `delete_branch_tolerant/2` ("branch not found" AND repo-gone outputs — "Repository path does not exist" / "not a git repository" via `WorktreeRetry.repo_gone_output?/1` — treated as `:ok`: a vanished repo implies the branch is gone with it; "used by worktree at" / "checked out at" is RECOVERABLE — prune stale registrations + retry once, see "Worktree Safety"). No fast/slow path — every agent run gets a FRESH worktree. |
| `EvoGit.AgentScheduler.WorktreeManager` | Stateful GenServer owning the entire worktree lifecycle (supervised in `EvoGit.Application`, same name). Lazy per-repo initialization (`maybe_init_repo/2`) on the FIRST create request for a repo — never from the scheduler — gated on a **persistent per-repo marker** in the app-owned ETS table `:evogit_worktree_repos` (created in `EvoGit.Application.start/2`, same pattern as `:evogit_cancelling_tasks`; read/written defensively via `:ets.whereis` — a missing table means "uninitialized"). **Marker present** (WorktreeManager-only restart) → destructive wipe SKIPPED (no rm_rf, no orphaned `evogit-agent-*` cleanup, no prune), only the workers dir ensured via `WorktreeRetry.mkdir_p_retry/2` — live agents' worktrees preserved. **Marker absent** (first request, or genuine BEAM restart with no live agents) → full wipe (rm_rf workers dir → `Git.prune_worktrees` → orphaned branch cleanup → mkdir_p) then marker recorded (`repo_root` => init timestamp) — **the destructive steps (rm_rf + orphaned `evogit-agent-*` branch cleanup) run for the PRIMARY repo only**: `maybe_init_repo/3` takes a `primary?` flag derived at the call sites from `spec.repo_id` (`EvoGit.Core.ForeignRepo.primary?/1`); foreign repos (possibly writable per-task, holding real task branches) get only the non-destructive prune + workers-dir ensure + init marker. All init steps via `WorktreeRetry` (`rm_rf_retry` log-and-continue on exhaustion; `retry_on_transient` around prune; `mkdir_p_retry` raises `File.Error` on exhaustion — safe because the marker is written before the first worktree exists). Request API: `create_worktree_for_agent(agent_id, repo_root, worktree_path, spec, meta, agent_pid)` — `GenServer.call` with named 1-hour timeout (`@worktree_call_timeout` 3_600_000), called by the agent Runner; the handler offloads blocking create I/O to a spawned `Task` (reply via `GenServer.reply/2` + `{:create_finished, agent_id}` cast), so the loop stays responsive for `:DOWN` handling. **Bounded admission**: `accept_request/8` registers each request as `:queued` and pushes it onto a FIFO `admission_queue`; `admit_next/1` drains the queue while `creating_count < max_concurrent_creation/0` (app env `:max_concurrent_worktree_creation`, default 4, read at admission time), flipping each admitted entry to `:creating` and spawning its offloaded task — a permit is held only for one pipeline's duration and always released by the `{:create_finished, ...}` cast, so the queue always drains; this bounds CREATE-pipeline concurrency only, never live worktrees/agents, and is deadlock-free (a `:waiting` parent holds its worktree but no permit). Agent entries carry an explicit `status: :queued | :creating | :live`. Per-agent serialization: a second create request for a `:creating` OR `:queued` agent is stashed in `pending_requests` and re-enters the SAME admission path (`accept_request/8`) when the previous request finishes; agents dying mid-create are tracked in `pending_cleanup`, cleaned up when the create task finishes; an agent dying while still `:queued` is filtered out of `admission_queue` (no create started, no permit consumed). Create-fun seam: `Application.get_env(:evo_git, :worktree_create_fun) || &Worktrees.prepare_new_worktree/5` (arity 5 `(agent_id, repo_root, worktree_path, spec, meta)`). `Process.monitor(agent_pid)` per agent; on `:DOWN` for ANY reason (normal exit, crash, cancel kill) the worktree is reclaimed + destroyed (`destroy_worktree/3`): `File.rm_rf(worktree_path)` → only AFTER the dir is gone `Git.prune_worktrees(repo_root)` → `Worktrees.delete_branch_tolerant(repo_root, branch_name)` (this order — git refuses to delete a branch checked out in another worktree; the tolerant delete recovers stale "used by worktree at" registrations). On rm_rf failure it logs at ERROR and skips BOTH prune and branch delete — a dir that survives rm_rf WITH its `.git` file + registration stays a safe registered worktree; pruning it would create the plain-unregistered-dir leak precondition (see "Worktree Safety"). Each step through `WorktreeRetry` (never raises — idempotent; the GenServer never crashes on agent exits). **Crash-restart re-monitoring**: one_for_one child of `EvoGit.Supervisor` — a crash restarts IT without killing the running agent Tasks (which keep running in their live worktrees); `init/1` calls `rebuild_monitors/1` to re-monitor live agents from `:evogit_sched_meta` rows (`worktree`, `task_ref.pid`, `task_number`) + `:evogit_agent_state` rows (`repo_root`, `task_local_id`); missing/stale rows skipped, never crashes init. Kept: `workers_dir/1` (delegates to `Worktrees.workers_dir/1` — single source), `start_link/1`. |
| `EvoGit.AgentScheduler.WorktreeRetry` | Pure retry helpers for transient filesystem failures in the worktree lifecycle (Windows file locking, anti-virus scans) — single shared implementation (worktree_retry.ex). `rm_rf_retry/2` → `{:ok, files} \| {:error, reason, file}` — retries `File.rm_rf/1` on transient reasons; `:enoent` normalized to `{:ok, []}` (dir already gone = success); non-transient fails fast. `mkdir_p_retry/2` → `:ok \| {:error, reason}`. `retry_on_transient/2` — generic wrapper: success shapes (`:ok`, `{:ok, _}`) halt immediately; retryable = transient POSIX reasons (`:eacces`, `:eperm`, `:eexist`, `:enotempty`, `:ebusy`, `:again`, `:eintr`) in 2-/3-tuple fs error shapes, git `{:error, {tag, output}}` tuples, and binary error shapes — EXCEPT repo-gone outputs ("Repository path does not exist" / "not a git repository", `repo_gone_output?/1`) which fail fast. Policy: 4 total attempts (first immediate — a `0` delay is prepended), exponential backoff 50ms → 100ms → 200ms via `Retry.DelayStreams` (the `retry` hex lib; `opts[:delays]` overridable for hermetic tests — `[0,0,0]`/`[]` never sleep). Design: `Enum.reduce_while` over `Retry.DelayStreams` — the `retry` macro's `atoms:` predicate cannot express the mixed return-shape classification and its default `rescue_only: [RuntimeError]` would retry exceptions that must fail fast. Never raises on its own; exceptions from the wrapped fun propagate (fail fast). |
| `EvoGit.AgentScheduler.Dispatch` | Agent registration, dispatching, agent-process git commit, repo root resolution, queue processing |
| `EvoGit.AgentScheduler.Subagents` | Subagent validation/spawning, spatial contract checks, result tracking, parent resumption |
| `EvoGit.AgentScheduler.Lifecycle` | Agent recycling (cleanup) and crash handling (retry logic, permanent failure) |
| `EvoGit.AgentScheduler.PubSub` | Centralized broadcast helpers — throttled `{:agents_updated, node}` (via `Throttle` GenServer supervised under `EvoGit.Supervisor`, see "Throttled PubSub") plus delta broadcasts `{:agent_registered, agent_id, summary, node}` / `{:agent_updated, agent_id, changed_fields, node}` / `{:agent_removed, agent_id, node}`, and config broadcast `{:scheduler_config_updated, node}` — every broadcast carries a trailing `node` element computed inside the helpers. Agent-state broadcast kwlists have the heavy `:context` field stripped, with `message_count` added when a context is present; ETS writes stay verbatim (see `EvoGit.AgentScheduler.Store`) |
| `EvoGit.AgentScheduler.PubSub.Throttle` | GenServer (throttle.ex) — coalesces `:schedule` casts into one flush broadcast 200ms after the last schedule; supervised child of `EvoGit.Supervisor` (`:permanent` restart), declared in `EvoGit.Application`'s children after `Phoenix.PubSub` |
| `EvoGit.AgentScheduler.RemoteAPI` | The `:erpc` target for the dashboard (`remote_api.ex`) — thin pass-throughs returning native BEAM terms; no guards and no fallback shapes here (transport failures are normalized by `EvoGit.RemoteNode`). Review ops mirror `EvoGit.Review` function-for-function and each takes an **arbitrary `repo_path`** (primary AND foreign repos): `get_file_content/3`(642) `list_commits/2`(657) `load_review_data/2`(674) `load_review_metadata/2`(690) `load_file_diff/4`(705) `load_file_diff/5`(722, `when is_list(opts)`) `load_review_metadata_from_shas/3`(737) `list_commits_from_shas/3`(752) `load_commit_files/2`(766) `load_commit_file_diff/3`(781) `merge_branch/2`(798) `merge_branch/3`(815) `check_merge/3`(834) `default_merge_target/1`(849) `list_branches/1`(863) `reject_branch/2`(877) `create_github_pr/4`(892) `branch_exists?/2`(959). Review persistence is TASK-LEVEL, never per-repo: `set_review_status/2`(469) / `set_review_metadata/3`(482) are `EvoGit.TaskRegistry` casts keyed on `task_id` only (single `review_status` / `base_sha`+`commit_sha` task-row columns). `get_task/1`(456) is the only SINGLE-task full read, returning the whole `%TaskInfo{}` (→ `result` `"repos"` map + STRING-keyed `opts[:foreign_repos]`); `list_tasks/0`(320) and `list_tasks_paginated/1`(335) also return full `%TaskInfo{}` structs (full decode incl. `result`) but have NO dashboard caller; the summary RPCs deliberately exclude `result`; no per-repo-commits read exists over RPC. See "RemoteAPI review surface" below. |

### RemoteAPI review surface (arbitrary repo_path; task-level review status)

- Every review RPC is a `repo_path`-first pass-through to `EvoGit.Review` (line numbers in the API Surface row above) — the CALLER supplies the repo path, so ONE RPC serves the primary repo and every writable foreign repo; nothing in this layer resolves "the task's primary repo" (the dashboard's `ReviewLive` loops over `@review_repos` and picks the path).
- Return shapes are the `EvoGit.Review` shapes verbatim — most review RPCs are `@spec ... :: term()` because nothing is rewritten (full contract: `../../CONTEXT.md` → "Review / RemoteNode Return-Shape Contract"). Notable: `check_merge/3` → `{:ok, :clean} | {:ok, {:conflict, files}} | {:error, ...}` (non-mutating `git merge-tree` dry-run), `merge_branch/2,3` → `{:ok, sha} | {:conflict, details} | {:error, ...}`, `list_commits/2` always `{:ok, [%CommitInfo{}]}` (git errors yield `[]`), `default_merge_target/1` → `{:ok, name} | {:error, :no_branch_found}`, `reject_branch/2` → `:ok | {:error, {code, output}}`.
- `set_review_status/2` + `set_review_metadata/3` key on `task_id` ONLY (no `repo_id`), so a multi-repo task carries ONE review status and ONE SHA pair: "repo X merged/rejected" is NOT recordable through RemoteAPI (the dashboard keeps per-repo merge outcomes in LiveView assigns; it calls `set_review_status(:merged)` only after ALL repos merged).
- Task reads: `get_task/1` is the single-task full read (whole `%TaskInfo{}` incl. `result`, whose `"repos"` map is `%{repo_id => %{"commit_sha", "branch_name"}}` with STRING keys); `list_tasks/0` + `list_tasks_paginated/1` also decode full `%TaskInfo{}` structs incl. `result` (no dashboard caller — dead surface); the summary RPCs (`list_tasks_summary/1`, `list_tasks_summary_by_path/2`, `list_tasks_changed_since/1`) exclude `result`. `AgentScheduler.get_foreign_repo_commits/1` is NOT exposed over RPC — per-repo commit SHAs are only reachable via `get_task/1`'s `result`.
- Guards: the review/task RPCs have NONE (they run local filesystem/git I/O and work without a started scheduler). The only guards in the file are `send_agent_message/2` (`:ets.whereis(:evogit_agent_state)` → `{:error, :scheduler_not_started}`), `get_recent_system_samples/0` (`Process.whereis` → `{:error, :sampler_down}`) and `respond_approval/2` (`Process.whereis` → `{:error, :approval_not_started}`).

### Slot Management (Slots module)

Two independent slot pools tracked as `MapSet`s of agent IDs. Available capacity is derived: `capacity - map_size(holders)` — slot leaks impossible by construction: when an agent dies, `release_agent_slots/2` removes it from both holder sets, restoring the slot automatically.

| Pool | State Keys | Capacity | Backoff |
|------|-----------|----------|---------|
| LLM slots | `llm_holders`, `llm_waiting`, `llm_backoff_until` | **per-model** `model_concurrency[model_id]` (from `[[llm.models]]` profile `concurrency`, default 3); fallback `default_llm_max_concurrency` (3) for unknown model_ids via `State.concurrency_for/2` | **per-model** 60s cooldown on `:rate_limit` errors (NOT global — one model's rate limit doesn't block others) |
| Tool slots | `tool_holders`, `tool_waiting` | `max_tool_concurrency` (default = detected CPU thread count, via `EvoGit.Platform.cpu_threads/0`) | None |

Key functions: `handle_request_llm_slot/3`, `handle_request_tool_slot/3` (grant if capacity available, else enqueue); `handle_release_llm_slot/2`, `handle_release_tool_slot/2` (remove from holder set, grant pending — called via `handle_cast`, fire-and-forget; return `{state, status_updates}`); `release_agent_slots/2` (on agent death `:DOWN`: removes from both holder sets, purges queues, grants pending); `purge_agents_from_queues/2` (removes agents from waiting queues, replies `{:error, :cancelled}` to each); `grant_pending_on_resume/1` (grants all available slots when resuming from pause; also runs at the END of every config update (state.ex), so a 0→N capacity increase (peak exit) grants queued 0-capacity waiters automatically).

Slot functions return `{:reply, :ok, state, status_updates}` or `{:noreply, state, status_updates}` for synchronous operations, and `{state, status_updates}` for asynchronous releases (`handle_cast`). `status_updates` contains `{agent_id, :blocked | :running}` tuples applied to ETS SchedMeta for dashboard visibility.

### `scheduler.default_llm_max_concurrency` precedence

`scheduler.default_llm_max_concurrency` is the **default per-LLM concurrency**, NOT a global cap:
- **Effective LLM limit for an agent = `model_concurrency[model_id]` if present, else `state.default_llm_max_concurrency`** (`State.concurrency_for/2`, state.ex:205-207). Both slot enforcers read it: `Slots.do_handle_request_llm_slot/4` (slots.ex:103) and `Slots.grant_llm_from_queue/2` (slots.ex:399).
- `model_concurrency` is populated at config-load time ONLY by `State.from_model_profiles/2` (state.ex:146-151) from `[[llm.models]]` profile `concurrency` fields (default 3), at `AgentScheduler.init/1` (agent_scheduler.ex:419) and `State.do_update_config`'s `:model_profiles` branch (state.ex:299-301) — the `reload_config` path (`RemoteAPI.build_reload_opts`, remote_api.ex:192-222). At RUNTIME it can additionally be replaced wholesale via `update_config(model_concurrency: map)` (the dynamic wall-clock engine path — see "Slot-Pool Sizing & Concurrency").
- **Legacy (no `[[llm.models]]`) path**: `scheduler.default_llm_max_concurrency` is live — it becomes the synthesized "default" profile's `concurrency` via `Schema.LLM.build_legacy_default_profile/1` (schema/llm.ex:123), then flows into `model_concurrency["default"]`. Config-load time only (init or reload), NOT runtime.
- **When `[[llm.models]]` profiles exist**: each profile's own `concurrency` wins for its pool; `scheduler.default_llm_max_concurrency` does NOT cap or override it — it remains only as (a) the fallback for agents whose `model_id` is not in `model_concurrency` (state.ex:206) and (b) the value surfaced in `get_config/0` as `default_llm_max_concurrency` (agent_scheduler.ex:656).
- **Runtime `default_llm_max_concurrency` override is a floor (no CLI path)**: the CLI makes NO scheduler overrides (removed flags; concurrency is config.toml-only). Runtime overrides reach the scheduler only via `update_config(default_llm_max_concurrency: N)` (dashboard settings save / `RemoteAPI.reload_config`) → `State.do_update_config`'s `maybe_apply_default_llm_concurrency` step → `State.apply_default_llm_concurrency_override/2`, which sets `state.default_llm_max_concurrency = N` AND raises every `model_concurrency` entry to `max(c, N)`. Raising the default takes effect immediately for ALL live pools — with AND without `[[llm.models]]` profiles (an explicit profile concurrency above the default still wins and is never lowered). The floor applies to runtime `update_config` only — at init the file semantics remain "profile wins". (`update_config(max_tool_concurrency: N)` also takes effect — tool slots read `state.max_tool_concurrency` directly, slots.ex:219.)

### Slot-Pool Sizing & Concurrency

Coordinated behaviors in `state.ex` + `slots.ex` (do not regress):

- **Config updates preserve live LLM pools**: `State.do_update_config/2`'s `:model_profiles` branch calls `from_model_profiles(profiles, opts)` (opts pass-through so a `:default_llm_max_concurrency` override in the same update is honored) and merges the existing `llm_holders`/`llm_waiting`/`llm_backoff_until`/`llm_last_granted` maps OVER the fresh ones (existing entries win; new profile ids keep fresh empty entries; stale pool ids keep live entries), then `prune_empty_pools/1` drops empty-value entries. Trailing `Slots.grant_pending_on_resume(state)` (state.ex:413) grants under the new capacity. Queued waiters' `GenServer.from`s survive the update; live holders stay counted (no over-grant beyond capacity).
- **Stale-model pools are visible to all sweeps**: `State.all_model_ids/1` returns the UNION of keys of `model_concurrency`, `llm_holders`, `llm_waiting`, `llm_backoff_until` — pools for model_ids absent from `model_concurrency` (stale ETS `model_id` after a profiles update; unknown models) are swept by `grant_pending_llm_slots` (both Enum.reduce sweeps), `release_agent_slots`'s LLM cleanup, and `purge_llm_waiting_queues`. Capacity for a stale pool = `concurrency_for`'s fallback (`default_llm_max_concurrency`).
- **LLM slot release scans all pools**: `Slots.handle_release_llm_slot/2` scans ALL pool ids (`State.all_model_ids`) rather than resolving the model via ETS at cast-processing time (which picks the wrong pool when the ETS entry is gone), deletes the agent from every pool's holder set (idempotent — an agent holds at most one LLM slot), cleans `llm_last_granted` for the pool it was removed from, then runs `grant_pending_llm_slots`.
- **`default_llm_max_concurrency` override is a floor**: `maybe_apply_default_llm_concurrency/2` → public `State.apply_default_llm_concurrency_override/2` sets the default AND applies a FLOOR to every `model_concurrency` entry (`max(c, N)`; explicit `0` entries — the peak hard-pause sentinel — stay `0`); applies whether or not `:model_profiles` is in the same update (a runtime `default_llm_max_concurrency` override — dashboard default-concurrency setting or `RemoteAPI.reload_config` — raises the effective capacity of ALL live pools immediately; the CLI has no such override).
- **Dynamic `:model_concurrency` override (wall-clock engine path)**: `State.do_update_config/2`'s `maybe_update_model_concurrency/2` step (private, state.ex) replaces `state.model_concurrency` with the given `%{model_id => non_neg_integer()}` map — the runtime entry point for `EvoGit.PeakHourEngine`. After replacement it re-applies the floor via `apply_default_llm_concurrency_override(state, state.default_llm_max_concurrency)` — an engine update below an active floor NEVER drops it (the floor is a LOWER BOUND), **EXCEPT explicit `0` entries (the peak hard-pause sentinel), which are never floored up**; the engine applies the identical rule (`effective 0 → 0`), so the fixed point holds end-to-end. Independent of `from_model_profiles/2` (config loading stays wall-clock agnostic; the engine owns dynamic overrides). Capacity changes take effect immediately via the END-of-update `Slots.grant_pending_on_resume(state)` sweep (runs for EVERY update). The `AgentScheduler` handler (`handle_call({:update_config, opts}, ...)` → `reconcile_pool_after_update`) reconciles the ReqLLM Finch pool from the new `model_concurrency`/`default_llm_max_concurrency`.
- **Config updates never block on sandbox-slice work**: `State.do_update_config/2` (state.ex:313) propagates a `:sandbox_resources` change (Linux only, map-valued — state.ex:403-407) via the FIRE-AND-FORGET `EvoGit.SandboxSlice.update_resources_async/1` (`GenServer.cast`), never a synchronous slice update inside this handler (production crash: a synchronous `systemctl` slice update here blocked every scheduler caller ~7s on a wedged systemd user bus, crashing `SystemSampler` on its `:get_config` call timeout and risking the scheduler's own update call timeout). The cast is enqueued in SandboxSlice's mailbox BEFORE the handler replies (state.ex:424), so later `ensure_slice` callers observe the updated slice in the common case; slice property writes are idempotent last-write-wins (reordering harmless) and SandboxSlice logs its own bounded-update failures (`../CONTEXT.md` → SandboxSlice section).
- **0-capacity LLM slots enqueue and block (peak hard pause)**: `concurrency_for/2` may legitimately return `0` (the engine emits `{model_id, 0}` during peak windows for `peak_concurrency = 0` profiles). A slot request for a 0-capacity model enqueues exactly like the paused-scheduler case — `Slots.handle_request_llm_slot/3` has no 0-capacity pre-check, so flow falls through to the paused branch (or `do_handle_request_llm_slot/4`'s full-queue branch), storing `{agent_id, from, nil}` and replying `{:noreply, state, [{agent_id, :blocked}]}` — the agent blocks until capacity returns. `Slots.grant_llm_from_queue/2` at capacity 0 is a no-op that KEEPS the waiting queue untouched (`{state, []}` — no replies, no drain, no status updates): waiters stay queued, and a 0→N transition (peak exit → `update_config(model_concurrency: %{...})` → end-of-update `grant_pending_on_resume/1` sweep → normal grant branch) grants them automatically. In-flight holders are never evicted (at 0 the release sweep is a no-op). `AgentScheduler.with_llm_slot/2` does not raise on zero capacity — agents wait. Graceful cancel unblocks blocked agents of the cancelling task: `begin_graceful_cancel/1` purges their LLM/tool waiting-queue entries via `Slots.purge_agents_from_queues/2`, replying `{:error, :cancelled}` → the blocked `with_llm_slot` raises → crash-retry → the agent enters cancel-grace at its next loop top (reads `cancel_requested`, saves work, calls `complete_task`). Force-kill's `{:error, :cancelled}` purge path is unchanged and capacity-independent.

### Peak/Off-Peak Slot Diagnostics (logging)

Diagnostic logging for the blocked-after-peak-exit case lives on the SLOT side here (`slots.ex`) plus the scheduler's `model_concurrency` update (`agent_scheduler.ex`); the engine-side wakeup/decision logging is documented in `../CONTEXT.md` ("Peak/Off-Peak Diagnostics"). Read the slot-side lines in this order:

1. **`AgentScheduler: model_concurrency updated — old=... new=... skip_floor=... default_llm_max_concurrency=...`** (info, `agent_scheduler.ex` `log_model_concurrency_update/3` in `handle_call({:update_config, opts}, ...)`) — every `:model_concurrency` update logs the pre-update map vs the POST-application map (post-floor / verbatim for `model_concurrency_skip_floor: true`). `old=%{"m" => 0} new=%{"m" => 5}` ⇒ capacity WAS restored by the engine; `old==new` ⇒ the update was a no-op (or the engine never applied).
2. **`AgentScheduler: LLM slot grant sweep (resume) — model=... capacity=... waiting_before=... granted=... waiting_after=...`** (info when `granted > 0` or `capacity == 0 and waiting_before > 0`, else debug; `slots.ex` `grant_pending_on_resume/1`) — the end-of-update sweep that grants queued 0-capacity waiters. `capacity=5 waiting_before=3 granted=0 waiting_after=3` is the **"applied but queued waiter never granted"** symptom. `grant_pending_llm_slots/1` returns a per-model sweep tuple `{model_id, capacity, waiting_before, granted, waiting_after}` (third return element) to feed this log; the other three callers (`handle_release_llm_slot/2`, `handle_retry_llm_waiting/1`, `release_agent_slots/2`) discard it.
3. **Debug per-agent events** (`slots.ex`): `LLM slot request ENQUEUED — agent=... model=... capacity=0 (peak pause)` at the 0-capacity enqueue point in `do_handle_request_llm_slot/4`; `granted LLM slot — agent=... model=...` at each queue grant in `grant_llm_from_queue/2` (right after `GenServer.reply(from, :ok)`); `purged N LLM waiter(s) for model ... — agent(s) [...]` in `purge_llm_waiting_queues/2` when a cancel/force-kill purge removes queued waiters (the "rejected" counterpart of the sweep's granted/remaining counts).

Logging-only: no behavior, timing, floor, fixed-point, or slot semantics changed; `grant_pending_llm_slots/1`'s return shape grew a third element purely to carry sweep stats.

### LLM-model resolution gotchas (scheduler state vs config file)

- **Init (agent_scheduler.ex:508-640) always produces a non-empty `model_profiles`** — `raw_model_profiles = Schema.model_profiles(config)` (:543); `[]` → `[Schema.LLM.build_legacy_default_profile(config)]` (:545-553).
- **Init `llm_model` has a FLAT-config fallback; runtime `:model_profiles` updates do NOT.** `default_model` (agent_scheduler.ex:560-564) = first-profile `:model` when non-nil, else flat `config[:llm][:model]`; the boot warning "AgentScheduler: LLM model not configured" fires at :577-594 only when BOTH are nil/falsy. By contrast `State.do_update_config/2`'s `:model_profiles` branch → `from_model_profiles(profiles, opts)` (state.ex:317-322) derives `llm_model` purely from `Map.get(first_profile, :model)` (state.ex:181,190) — nil/absent first-profile `:model` silently sets `state.llm_model = nil`, with NO flat-`llm.model` consult, even when a LATER profile or the file's flat `llm.model` has a model. The update is ACCEPTED (`{:reply, :ok, state}`): the nil-rejection in `handle_call({:update_config, opts}, ...)` (agent_scheduler.ex:849-858) checks ONLY for an explicit `:llm_model => nil` OPTS key (:851-852), never the derived state. Reload paths never push `:llm_model` (RemoteAPI.build_reload_opts, remote_api.ex:226-238; evo_dash ConfigIO pushes `:model_profiles` only) — so a first-profile-nil-model file config nils the scheduler's llm_model on every reload. `migrate_llm_models` (config.ex:297-338) explicitly tolerates an existing `[[llm.models]]` list whose first profile lacks `:model` (:319-335, "manually edited config.toml or an incomplete dashboard profile").
- **Dispatch model selection**: `Dispatch.register_agent` (dispatch.ex:42-124) resolves per agent: `effective_model_id/5` (:74; chain: `spec.model_id` / custom-agent def `:model_id` → `spec.opts[:model_id_locked]` short-circuit → ModelSelector script → base, dispatch.ex:407-462) then `resolve_model_for_agent/2` (:76-77, :338-375): unknown id → warn + `List.first(profiles)`; nil id → `List.first(profiles)`; empty profiles → `{State.default_model_id(state), state.llm_model, ...}`. The model string is `Map.get(profile, :model, state.llm_model)` (dispatch.ex:371) — the `state.llm_model` default applies ONLY when the key is ABSENT; a profile with `:model => nil` yields nil even if `state.llm_model` is set. No raise/log on nil; nil flows into `AgentState.llm_model` (:82) and reaches `ReqLLM.stream_text(current_model(), ...)` (tool_dispatch.ex:40-44, 253-257) at the API layer.
- **`run_agent` rejection clauses**: clause (a) `%State{model_profiles: []}` (agent_scheduler.ex:649-652, warn "no model profiles configured") is unreachable at boot but reachable at runtime via `update_config(model_profiles: [])`; clause (b) `%State{llm_model: nil, model_profiles: profiles} when profiles == nil` (:654-662, warn "LLM model not configured") is effectively DEAD (`model_profiles` defaults `[]` in the struct and every constructor produces a list). Both reply `{:error, :llm_not_configured}`; the nil-model-but-nonempty-profiles case passes BOTH clauses unguarded. Tests hit clause (a) by pushing `model_profiles: []` (see `apps/evo_git/test/evo_git/runtime/evolution_test.exs` technique, documented in `test/CONTEXT.md:87`).
- **No scheduler file-watching**: scheduler `state.model_profiles`/`llm_model` change ONLY via `update_config/1` GenServer calls (dashboard save → RemoteAPI.save_user_config → reload_config → build_reload_opts; evo_dash ConfigIO; PeakHourEngine). `get_config` (agent_scheduler.ex:861-868, config_map :1029-1031 over @config_keys :1009-1026) serves the in-memory values. `Config.resolve()`/`config_status()` (config.ex:619-637) are file-fresh — the two views diverge until a reload happens.

### Running Count

No `running_count` field — always derived as `map_size(state.ref_to_agent)`. Every agent lifecycle transition (dispatch, completion, crash) pops or puts from `ref_to_agent`, keeping the count authoritative.

### Multi-Task Repo Root Resolution

The scheduler supports multiple concurrent tasks targeting different repos. Repo root resolution priority:
1. **Per-agent ETS** (`AgentState.repo_root`) — set at registration via `Dispatch.resolve_agent_repo_root/2`, derived from spec data. Used by `Lifecycle` for agent cleanup/retry paths.
2. **Process dictionary** (`Process.get(:genesis_repo_root)`) — set at dispatch time in `try_dispatch/2`. Preferred by `current_repo_root/0` for runtime lookups.

No global `state.repo_root` fallback — always per-agent. Repo-init tracking lives in `WorktreeManager` (lazy per-repo init on first create request); `%State{}` does not carry `initialized`/`initialized_repos` fields.

`resolve_agent_repo_root/2` in Dispatch is self-contained for primary repos (strips worktree suffix from `spec.phylo_node.repo`). For foreign repos it looks up the repo root from the agent's own `spec.foreign_repos` list by `spec.repo_id` — foreign repos are carried per-agent, not in global scheduler state.

### Worktree Lifecycle (WorktreeManager + Worktrees)

Worktrees are **created fresh per agent run** — NO pool, NO persistent-worktree reuse across retries (no fast/slow path, no `File.exists?` reuse branch, no `newly_created` flag). The entire lifecycle is owned by the `WorktreeManager` GenServer:

1. **Lazy per-repo init** — on the FIRST `create_worktree_for_agent/6` request for a repo: rm_rf `.genesis/workers` → `git worktree prune` → orphaned `evogit-agent-*` branch cleanup → mkdir_p. Never called from the scheduler. The destructive wipe is gated on the **persistent per-repo marker** `:evogit_worktree_repos` (app-owned ETS): marker present (WorktreeManager-only restart) → wipe skipped, only the workers dir ensured; marker absent (first request, or BEAM restart with all agents dead) → full wipe + marker recorded. **PRIMARY-ONLY destructive steps**: the rm_rf workers-base wipe and the orphaned-`evogit-agent-*`-branch cleanup run only when `primary?` (derived from `spec.repo_id` via `ForeignRepo.primary?/1` at the two handle_call call sites); foreign repos — which may be writable per-task and carry real `evogit-agent-*` task branches — get only the non-destructive `Git.prune_worktrees` + workers-dir ensure + init marker. (See the WorktreeManager row.)
2. **Runner requests a fresh worktree** — `EvoGit.Agent.Runner.setup_dispatch_context/1` calls `WorktreeManager.create_worktree_for_agent(agent_id, repo_root, worktree_path, spec, meta, self())` with the named 1-hour timeout; the handler offloads the blocking create I/O to a spawned `Task` (reply via `GenServer.reply/2`, `{:create_finished, agent_id}` cast on completion) so the loop stays responsive for `:DOWN` handling. **Create pipelines are admitted through a bounded FIFO admission queue** — at most `Application.get_env(:evo_git, :max_concurrent_worktree_creation, 4)` create pipelines run at once (cap read at admission time); extra requests wait in `admission_queue` and reply later (queuing only DELAYS the reply, never drops it). A permit is held only for one pipeline's duration and always released via the `:create_finished` cast, so the cap bounds CREATE concurrency only (never live worktrees/agents) and is deadlock-free (see "Worktree Allocation & I/O Concurrency Model"). On `{:error, ...}` the Runner raises → agent Task crashes → scheduler crash-retry → fresh worktree.
3. **Create pipeline** (`Worktrees.prepare_new_worktree/5`, inside the create task): destroy leftover dir/branch (rm_rf failure escalates — see "Worktree Safety") → CoW create via `EvoGit.Adapters.CowWorktree.create_worktree` (fallback `Git.add_worktree`; failure → `{:error, {:worktree_create_failed, msg}}`) → `assign_and_prepare_worktree/3` (registered-linked-worktree guard, then git clean + checkout agent branch + bind `phylo_node.repo = worktree` in ETS; `repo_root` passed explicitly) → `run_init_script/3` (genesis.toml `[worktree]` script, primary repo only; env `SOURCE_REPO_PATH`/`SOURCE_WORKTREE_PATH`/`TARGET_WORKTREE_PATH`; Windows scripts run via `Platform.shell_args/1` → PowerShell `-EncodedCommand` — single source of truth: `EvoGit.Powershell`; base64-encoded so no command-line quoting issues, output forced UTF-8). Branch name from `Worktrees.branch_name/2` — `"evogit-agent-T#{task_number}-A#{task_local_id}"`.
4. **Monitor-based reclaim** — WorktreeManager `Process.monitor`s each agent; on `:DOWN` for ANY reason (normal exit, crash, cancel kill) it destroys the worktree (`destroy_worktree/3`): `File.rm_rf(worktree_path)` → only AFTER the dir is gone `Git.prune_worktrees(repo_root)` → `Worktrees.delete_branch_tolerant(repo_root, branch_name)` (this order — git refuses to delete a branch checked out in another worktree; on rm_rf failure it logs at ERROR and skips both prune and branch delete — see "Worktree Safety"). All destroy steps via `WorktreeRetry` with a modest budget (4 attempts, ≤350ms sleep per op) because `destroy_worktree/3` runs in the WorktreeManager GenServer process. Idempotent; the GenServer never crashes on agent exits. `recycle_agent/2`, `cancel_agent/2` (`Task.shutdown(:brutal_kill)` fires the monitor) and the `handle_agent_crash/3` permanent-failure path never call worktree deletion — cleanup is entirely monitor-driven. After a WorktreeManager-only restart, `init/1` calls `rebuild_monitors/1` to re-monitor live agents from scheduler ETS rows (see the WorktreeManager row) so `:DOWN`-driven cleanup keeps working for worktrees that predate the crash.
5. **Per-agent serialization** — a second create request for an agent whose create is still `:creating` OR still `:queued` is stashed in `pending_requests`; when the previous request finishes it re-enters the SAME admission path (`accept_request/8` → `admit_next/1`, never bypassing the cap). Agents that die while their create is in flight are tracked in `pending_cleanup`, cleaned up when the create task finishes; an agent that dies while its request is still `:queued` is filtered out of `admission_queue` (no create started, no permit consumed) and its registration removed.

All blocking filesystem operations (rm_rf, prune_worktrees, delete_branch, mkdir_p) run inside `WorktreeManager` (create I/O offloaded to a spawned Task); the scheduler never touches worktree I/O.

### Worktree Allocation & I/O Concurrency Model (creation is admission-bounded; deletion is serialized)

**Creation is admission-bounded, running in parallel up to the cap.** `WorktreeManager.accept_request/8` registers each request as `:queued` (monitor + `agents`/`monitors` entries) and pushes it onto the FIFO `admission_queue`; `admit_next/1` pops and starts requests while `creating_count < Application.get_env(:evo_git, :max_concurrent_worktree_creation, 4)` (cap read at ADMISSION time), each admitted request flipping its entry to `:creating` and spawning its own independent `Task.start` — so up to 4 full-tree copy pipelines run concurrently instead of one per agent, and the GenServer never blocks (all I/O offloaded; the loop stays responsive for `:DOWN`). The create fun is read through the seam `Application.get_env(:evo_git, :worktree_create_fun) || &Worktrees.prepare_new_worktree/5` (arity 5 `(agent_id, repo_root, worktree_path, spec, meta)`). **A permit is held only for the duration of one create pipeline and always released via the `{:create_finished, agent_id}` cast** (fired from the task's `try/after`, so it fires even on a raise), so the admission queue always drains. **This caps CREATE-pipeline concurrency, NOT live worktrees/agents** — the cap is deadlock-free because a parent parked in `spawn_sub_agents/2` (`GenServer.call :infinity`) already HOLDS its created worktree and does NOT hold a creation permit, so it can never wedge its own children's creation; a request waiting in the admission queue occupies NO permit. Agent entries carry an explicit `status: :queued | :creating | :live`. The only remaining serialization is **per-`agent_id`** (a re-create while one is `:creating`/`:queued` is stashed in `pending_requests` and re-enters the same admission path), and **deletion** (still serialized in the GenServer loop — see below).

**Deletion IS serialized in the WorktreeManager GenServer process** (asymmetric with creation): `handle_info({:DOWN, ...})` calls `destroy_worktree/3` **synchronously** — a slow `rm_rf` blocks the whole manager loop, delaying other agents' create requests and other `:DOWN` cleanups.

**Worktree creation PRECEDES any LLM-slot acquisition** — an agent blocked on its per-model LLM slot ALREADY holds a fully-created worktree: `Runner.setup_dispatch_context/1` runs before `do_run/2`, whereas `AgentScheduler.with_llm_slot/2` is acquired per LLM attempt inside the loop and released immediately after. ⇒ a wide agent tree creates and I/O-pays for worktrees belonging to agents that are merely queued on LLM capacity. (Ordering detail: `../../../agent/CONTEXT.md` → "Lifecycle ordering".)

**Waiting parents keep their worktree.** `subagents.ex:453-477` (`dispatch_ready_parent/3`) documents that a `:waiting` parent KEEPS its live worktree while blocked on `spawn_sub_agents/2` (agent_scheduler.ex:101-108, `timeout \\ :infinity`) — worktrees are reclaimed ONLY on process exit (`Process.monitor` `:DOWN`), so a parked parent's worktree stays allocated for the whole duration of its subtree. Note the stale `@doc` on `spawn_sub_agents/2` (agent_scheduler.ex:86) claiming the worktree "becomes reclaimable" — the parent retains it. (Relation to the `:finalizing` window: "Completion vs Reclaim timing".)

**Why a creation-concurrency cap is deadlock-free (unlike a live-worktree cap).** A parent blocks `:infinity` in `spawn_sub_agents/2` holding NO LLM slot and NO tool slot but HOLDING its worktree until every child finishes (see "Waiting parents keep their worktree"). A global semaphore on concurrently-ALLOCATED worktrees would therefore wedge: if all permits were held by blocked parents, their children could never create a worktree (creation precedes everything the child does) and no parent would ever release → livelock. The implemented cap instead bounds CREATE-pipeline concurrency: a request waiting in the admission queue holds NO permit and an admitted request holds one only for its pipeline's duration, always released when that pipeline terminates (the `:create_finished` cast) — so a parent parked in `spawn_sub_agents/2` holds its worktree but NO permit and can never wedge its own children's creation, and the admission queue always drains.

**Existing admission-control primitive**: `AgentScheduler.pause/0|resume/0|paused?/0` (agent_scheduler.ex:196-217, handlers :886-917) is global, not breadth-scoped — `run_agent` queues while paused and `spawn_sub_agents` replies `{:error, :scheduler_paused}` (agent_scheduler.ex:928-931); it is NOT a per-agent-count limiter.

### Completion vs Reclaim timing (worktree cleanup is NOT in the `:finalizing` window)

Ordering at top-level agent completion (useful for diagnosing a task stuck in `:finalizing` — the agent Task is spawned via `Task.Supervisor.async_nolink`, dispatch.ex:225-231, so its `{ref, result}` message is sent to the scheduler BEFORE the process exits, and per-sender signal ordering puts that message ahead of the scheduler's own `:DOWN`):

1. Agent Task's Runner finishes → Task sends `{ref, result}` → scheduler `handle_info({ref, result})` (agent_scheduler.ex:1075-1077) → `Lifecycle.handle_task_result/3` (lifecycle.ex:222): root branch is ETS-only (put_sched_meta `result_sent` lifecycle.ex:230, archive collect/inject lifecycle.ex:247-248) then **`GenServer.reply(meta.from, result)`** (lifecycle.ex:259) — this reply unblocks the `run_agent` caller (agent_scheduler.ex:80-81).
2. The runtime then broadcasts `:finalizing` + runs `merge_and_report` (runtime/evolution.ex:107-110) — pure Git-adapter `rev_parse`/`create_branch` on the MAIN repo (runtime/helpers.ex:38-91), no worktree ops, never blocks on WorktreeManager.
3. Task process exits → scheduler's own `:DOWN` (agent_scheduler.ex:1081-1083) → `Lifecycle.handle_agent_down/4` → `recycle_agent/2` (ETS-only, lifecycle.ex:303/33-34) — AFTER the reply (step 1), not before.
4. Task process exits → WorktreeManager's `:DOWN` (worktree_manager.ex:225-256) → `destroy_worktree/3` (worktree_manager.ex:241, 450-478: rm_rf → prune → `delete_branch_tolerant`) runs **in the WorktreeManager GenServer process**, concurrently with steps 1-3 and never on the reply/`:finalizing` path.

Consequences: (a) **worktree cleanup (rm_rf/prune/branch delete) is never part of the task's `:finalizing` window** — it cannot delay the runtime's reply/merge; (b) a slow NFS `rm_rf` inside `destroy_worktree/3` blocks only the WorktreeManager GenServer message loop — delaying OTHER agents' `create_worktree_for_agent` calls and other `:DOWN` cleanups (their Runners/agents), never the scheduler or the completing task's runtime; (c) reclaim is triggered ONLY by actual process death — `Process.monitor` (worktree_manager.ex:389) delivers `:DOWN` solely on exit, so a hung-but-alive agent/wrapper never triggers reclaim.

### WorktreeRetry — transient filesystem failure retries

`EvoGit.AgentScheduler.WorktreeRetry` is the single shared implementation of all retry logic for the worktree lifecycle. Purpose: Windows file lockers and anti-virus scanners transiently fail file operations with `:eacces`/`:eperm`/`:eexist` (and friends) — a single immediate failure is usually a false alarm; the operation succeeds on retry. (API details in the API Surface table above.) Transient-reason set: `:eacces`, `:eperm`, `:eexist`, `:enotempty`, `:ebusy`, `:again`, `:eintr`. `:enoent` deliberately absent: "not found" means the goal is already met (`rm_rf_retry/2` normalizes `{:error, :enoent, file}` to `{:ok, []}`). Git repo-gone outputs ("Repository path does not exist" / "not a git repository", `repo_gone_output?/1`) also fail fast. The moduledoc in `worktree_retry.ex` is the authoritative detail.

### Agent Dispatching (Dispatch module)

1. `register_agent/6` — Assigns agent IDs, computes task-local IDs, resolves event sink inheritance, writes both ETS tables.
2. `try_dispatch/2` — Scheduling-only dispatch (no worktree I/O). `with` guard bails cleanly if ETS entries are missing (genuine race). Computes the worktree path (`Worktrees.branch_name/2` + workers dir) and stores it in sched_meta **before** spawning (so `cancel_agent` can find the worktree), spawns the task immediately via the 4-arity `Task.Supervisor.async_nolink/4` **named-function** form (`spec.agent_module, :run, [spec.objective, dispatch_ctx]`), updates `ref_to_agent`. **NO git commands, worktree creation, or init scripts run here** — the scheduler never does blocking I/O and never calls any worktree function (not even init). The named-function form makes the spawned process show up as a meaningful `module.run/2` in process inspection.
   - **Worktree creation happens in the agent Task**: `EvoGit.Agent.Runner.setup_dispatch_context/1` (inside `run/2`, the single true entry point for an agent, before the agent loop) calls `WorktreeManager.create_worktree_for_agent(..., self())` with the 1-hour timeout. The create pipeline (`Worktrees.prepare_new_worktree/5` — destroy leftovers → CoW create → assign_and_prepare → init script; lazy per-repo init on first request) runs offloaded inside WorktreeManager, admitted through the bounded admission queue (`:max_concurrent_worktree_creation`, default 4) so it runs concurrently across subagents only up to the cap. The CoW optimization copies unchanged files from a source working tree (parent's worktree for subagents, repo root for top-level) using `cp --reflink=auto --preserve=timestamps` (Linux) or `cp -cp` (macOS), avoiding full git object extraction. Controlled by `[:git, :cow_worktree_creation]` config (`:auto` default). `spawn_validated_subagents`' `Enum.reduce` over `try_dispatch` spawns all tasks immediately.
3. `commit_pending_in_worktree/0` — Best-effort git commit of pending changes, runs in the **agent process** (not the scheduler). Uses `Process.get(:repo_path)` for the worktree path. Handles all git adapter error tuples (`{:ok, _}`, `{:error, _}`, `{:conflict, _}`) explicitly via a `with` block (not a broad `try/rescue`), so failures are logged but never crash. Called via `try/after` in the agent's `run/2` after the agent loop. **The scheduler process NEVER calls git directly.**
4. `resolve_agent_repo_root/2` — Resolves repo root from spec (primary vs foreign repo).
5. `dispatch_queued_agents/1` — Drains queue dispatching agents (used after resume).
6. `process_queue/1` — Processes queue with ready-parent detection (delegates to Subagents).

### Subagent Management (Subagents module)

1. `spawn_validated_subagents/5` — Validates all specs, registers valid ones, replies immediately if all invalid. The parent agent commits its pending changes BEFORE calling `spawn_sub_agents` (done in the agent process via `Dispatch.commit_pending_in_worktree/0`), so the scheduler performs no git operations.
2. `validate_single_subagent/5` — Per-spec validation chain (depth, ignored, spatial).
   **Fan-out WIDTH is NOT code-limited**: the batch is processed wholesale via a single `Enum.reduce` over `specs` (subagents.ex:57-111) and EVERY valid spec is registered + dispatched — there is no max-batch-size, max-children, or per-parent/per-task agent-count check anywhere (the subagent tool schema `EvoGit.Agent.SubagentSchemas.schemas/1` carries `path`/`objective`/`commit_id` only, no `maxItems`; `state.task_agent_counts` is stats-only). The ONLY count-bounding gate is the writable-foreign-repo one-per-batch serialization (item 4); recursion DEPTH is bounded only by `max_agent_depth` (see item 3).
3. `validate_subagent_depth/3`, `validate_subagent_not_ignored/1` — Individual validation checks.
4. `validate_spatial_contract_for_spec/3,4` (4-arity takes the parent's `depth`; the 3-arity treats the parent as ROOT/depth 0 — backward-compatible entry), `validate_spawn_spatiality/4` — Spatial contract enforcement. Cross-repo delegation: `:read` agents are always allowed in foreign repos at ANY depth (unrestricted); `:read_write` agents are allowed ONLY into foreign repos whose TASK-LEVEL entry (resolved from `spec.foreign_repos` — never the path alone) is `%{writable: true}`, AND only from a ROOT parent (`parent.depth == 0`); unknown repo ids and listed-but-read-only repos are blocked with `{:error, {:foreign_repo_read_only, msg}}`; a non-root parent targeting a writable foreign repo is blocked with `{:error, {:foreign_repo_write_not_root, msg}}`. The messages teach the model: read-only unrestricted; writable = root-only + one-at-a-time; nested agents report needed foreign-repo changes back up to the root. Same-repo spawns WITHIN a foreign repo (`spec.repo_id == parent repo_id`) are unrestricted — the foreign-repo Manager's own internal parallelism is never gated. One-per-batch serialization of accepted writable foreign spawns is enforced by the caller `spawn_validated_subagents/5` (2nd+ accepted writable foreign spec in one batch → `{:error, {:foreign_repo_write_serialized, msg}}`).
5. `store_sub_result/3,4` — Stores subagent result at correct index in parent's results map; the 4th arg carries the COMPLETING CHILD's `%AgentSpec{}` (or `nil`) and gates foreign-repo commit recording via `writable_foreign_repo_agent?/1`. ONLY a child that is ITSELF a writable agent running in its own foreign repo may advance a repo's tracked `SchedMeta.foreign_repo_commits` entry: `repo_id` a binary `!= "primary"`, `agent_module.agent_type() == :read_write`, and the task-level `spec.foreign_repos` entry for that id `writable: true`. An authorized child (a) rolls up its result's subtree `foreign_repo_commits` map (injected by `Lifecycle.inject_foreign_repo_commits/2` at completion, may be absent) child-wins via `Map.merge(parent.foreign_repo_commits, child_frc)`, then (b) records its OWN `commit_sha` under its own `repo_id` via `Map.put` (only when `sha` is a binary, `repo_id` non-nil and `!= "primary"`). A LOCAL/PRIMARY or READ-ONLY child leaves the parent's map EXACTLY as-is — its result's foreign commits are IGNORED — so a stale sha can never overwrite an advanced one recorded by a writable foreign agent. `writable_foreign_repo_agent?/1` is public (returns `false` for any non-`%AgentSpec{}` input — never raises) and reuses the same private `foreign_repo_entry/1` id-matching source as the spawn gate.
6. `maybe_resume_parent/2` — Checks if all subagents done, resumes parent if so.
7. `dispatch_ready_parent/3` — Replies to parent's GenServer.call with ordered results.
8. `build_ordered_results/2` — Builds final results list in original spec order.

### Agent Lifecycle (Lifecycle module)

1. `recycle_agent/2` — Deletes both ETS entries (`Store.delete_agent_state` + `Store.delete_sched_meta`, lifecycle.ex:33-34) on normal completion. **Performs NO filesystem/git worktree I/O** — the module's only dependencies are ETS helpers (lifecycle.ex:11-16; no Git/File/Worktrees aliases). `case` guard returns state unchanged if the ETS entry is already gone (genuine race between `:DOWN` completion and another cleanup path). Invoked ONLY from the scheduler's own agent-monitor `:DOWN` (`Lifecycle.handle_agent_down/4`, lifecycle.ex:302-303, `reason == :normal or meta.result_sent`) — see "Completion vs Reclaim timing" below: in the normal flow that is AFTER `handle_task_result` has already replied to the top-level caller. No worktree deletion — cleanup is monitor-driven (WorktreeManager `:DOWN` handler).
2. `cancel_agent/2` — Kills the agent's Task process via `Task.shutdown(meta.task_ref, :brutal_kill)`. `task_ref` stores a full `%Task{}` struct (not a bare reference), enabling proper shutdown. The kill fires the WorktreeManager `:DOWN` monitor → worktree reclaim. Replies to blocked callers, deletes ETS entries. `case` skips ETS deletion if agent_state is missing.
3. `handle_agent_crash/3` — Retry logic (re-dispatch — the retried agent gets a FRESH worktree from WorktreeManager; crash-retry never reuses a worktree) or permanent failure (ETS cleanup, notify parent/reply error). Both paths resolve `repo_root` from per-agent ETS state; neither calls worktree deletion — cleanup is monitor-driven.
4. `handle_task_result/3` — after marking `result_sent`, injects the agent's own `SchedMeta.foreign_repo_commits` into its result via `inject_foreign_repo_commits/2` (`{:ok, %Result{}}` → `Map.put(res, :foreign_repo_commits, commits)` — Map.put, NOT struct update, because the Result field is being added by the agent stream in parallel; non-Result tuples pass through) BEFORE the parent/root split, so the subagent path (`Subagents.store_sub_result` roll-up) and the root reply both carry it.

## Foreign-Repo Writable Spawn Gate & Per-Repo Commit Roll-up

Writable foreign repos (`EvoGit.Core.ForeignRepo.writable`, default `false`) enable `:read_write` agents to work in foreign repos. Scheduler-side support (SchedMeta collection/injection):

- **Spawn gate** (`Subagents.validate_spatial_contract_for_spec/4`): the full conditions/error messages are in "Subagent Management" point 4 above. **Check order** for a CROSS-repo `:read_write` spawn: target writable (resolved from `spec.foreign_repos`, never the path)? else `{:error, {:foreign_repo_read_only, msg}}` → spawner is the ROOT (`parent.depth == 0`)? else `{:error, {:foreign_repo_write_not_root, msg}}` → only accepted writable foreign spec in the batch? else `{:error, {:foreign_repo_write_serialized, msg}}`. Rationale: parallel foreign-repo writes create merge conflicts the spawning agent cannot resolve — the sandbox restricts write access to the agent's LOCAL path, not the foreign repo path.
- **Subtree roll-up** (`Subagents.store_sub_result/3,4`): authority-gated recording — the full rule is in "Subagent Management" point 5 above. `writable_foreign_repo_agent?/1` decides authorization; `Lifecycle.handle_task_result/3` passes `meta.spec` as the 4th arg.
- **Result injection** (`Lifecycle.handle_task_result/3` + `inject_foreign_repo_commits/2`): see "Agent Lifecycle" point 4 above. `Map.put(res, :foreign_repo_commits, commits)` — NOT a struct update, because the `%Result{}` field is added by the `agent/` stream in parallel and struct-update would raise when it is absent; injected BEFORE the parent/root split, so the ROOT's replied result carries the map the runtime reads to build the per-repo report.
- **Getter** (`AgentScheduler.get_foreign_repo_commits/1`, `../agent_scheduler.ex:307-312`): returns the SPAWNING/CALLING agent's OWN SchedMeta `foreign_repo_commits` map (`%{}` when the row is missing); consumed at `agent/subagent_processing.ex:419` to seed the next subagent phylo-node starting commits (`build_subagent_specs/3` → `resolve_foreign_phylo_commit/4`, `agent/subagent_processing.ex:673-708`) — always the SPAWNING agent's own map, never an ancestor's or a sibling's.
- **Per-agent scope, no downward inheritance (known consequence)**: the map lives on ONE agent's SchedMeta row, is seeded `%{}` at registration (`Dispatch.register_agent/7`, dispatch.ex:99-108 — nothing is copied from the parent spec/state) and only ever moves UPWARD (roll-up → root result). `AgentSpec`/`AgentState` carry no foreign-commit field (`agent_spec.ex:21-31`, `agent_state.ex:42-66`); `foreign_repos` (incl. `base_sha`) is copied verbatim `LoopState.foreign_repos` → child spec (`agent/subagent_processing.ex:132,190`) and nothing bumps a `base_sha` at runtime (only next-task `MergeContext`/`ResumeContext`/`PrevTaskRepos` do). A nested agent's foreign spawn therefore resolves through its OWN empty map: `resolve_foreign_phylo_commit/4` precedence is task-level per-repo `base_sha` (non-nil WINS unconditionally — pinned by subagent_processing_test.exs:632) → the agent's own tracked sha → the foreign repo's HEAD (unchanged by the task — foreign work lands on `evogit-agent-*` branches, never the default branch). A sibling subtree never sees another child's (or the root's) advanced commit; it restarts from base_sha/HEAD. Chaining is correct only WITHIN one agent's sequential batches (the map is re-read per batch) and only with an unpinned `base_sha`; Genesis Mode B's two sequential roots (both depth 0) have INDEPENDENT maps merged manager-wins at the end (genesis.ex:190-196). Cross-TASK continuation is handled separately by `MergeContext` overriding per-repo `base_sha` from the previous task's `repos[repo_id].commit_sha` — not through this map.
- **Authority-gated roll-up, no cross-role clobbering**: the ROOT result's map is built by the child-wins `Map.merge(parent.foreign_repo_commits, child_frc)` plus a direct `Map.put(…, repo_id, sha)` (subagents.ex), applied ONLY for a child authorized by `writable_foreign_repo_agent?/1` (a `:read_write` agent running in its own writable foreign repo). A local/primary or read-only child — including a READ-ONLY foreign probe whose `commit_sha` is merely its starting commit (tool_dispatch.ex:111-133) — contributes NOTHING, so it can never overwrite the advanced sha recorded by a writable foreign agent; that advanced sha therefore still reaches `Runtime.Helpers.merge_and_report`'s per-repo `genesis/agent_<hex>` branch (runtime/helpers.ex:148-181, `Git.create_branch` at :165). Among a repo's own authorized writers, application order remains last-writer-wins.
- **Root-spawn chaining only works with an unpinned `base_sha`**: with `base_sha == nil`, the root's sequential one-at-a-time writable spawns chain through the map (F2 starts at F1's sha — healthy path); with `base_sha` pinned, precedence 1 disables chaining entirely — every foreign spawn restarts at the pinned sha, so F2's commit is a sibling of F1's and the map (last writer wins) points at one of them only.
- **WorktreeManager scoping**: the destructive per-repo init (rm_rf workers base + orphaned-`evogit-agent-*`-branch cleanup) runs for the PRIMARY repo only — see the `Worktrees`/`WorktreeManager` API rows and "Worktree Lifecycle" above; foreign repos keep only the non-destructive prune + workers-dir ensure because their `evogit-agent-*` branches carry real task work.

## Graceful Cancellation (cancel_requested + `:evogit_cancelling_tasks` marker)

**`AgentState.cancel_requested`** (`agent_state.ex:27,63,88`) — `boolean() | nil`, default `nil`. Set by the scheduler when the task is being gracefully cancelled; the runner checks/clears it at the top of each turn and enters cancel-grace (save work → `complete_task`). `EvoGit.AgentScheduler.Store` helpers (`store.ex:199-255`): `set_cancel_requested/1` (sets `true`) and `/2` (arbitrary `boolean() | nil`), `cancel_requested?/1` (boolean getter — `false` when the state is missing or the flag is nil/false), `clear_cancel_requested/1` (sets back to `nil`). All return `:ok | {:error, :not_found}` except the getter.

**Delta-broadcast visibility of mid-run signal fields (design-relevant asymmetry).** Only fields in `@agent_state_tracked_fields` (`store.ex:526-547`) produce `:agent_updated` deltas through `put_agent_state`'s struct diff. `:pending_user_messages` IS tracked (store.ex:546) — every append AND every drain (reset to `[]`) emits an `{:agent_updated, agent_id, [pending_user_messages: [...full list...]], node}` delta (nothing renders it; the message itself is invisible until the Runner injects it into context, which fires a separate `[message_count: N]` delta via `batch_update_agent(context:)`). `:cancel_requested` is NOT tracked — `set_cancel_requested`/`clear_cancel_requested` route through `put_agent_state`, so the flag change emits NO `:agent_updated` delta, only the throttled bulk `{:agents_updated, node}` (store.ex:156). `batch_update_agent/2` (store.ex:410-436) is the alternative: it broadcasts the caller-supplied kwlist verbatim (minus `:context`), so a field updated through it IS delta-visible even if untracked. Implication for any new dashboard-visible per-agent signal flag (e.g. a tool-approval state): update it via `batch_update_agent(agent_id, flag: value)` — or add it to `@agent_state_tracked_fields` — or the delta never reaches subscribers.

**`AgentScheduler.begin_graceful_cancel/1`** (public `agent_scheduler.ex:400-402`, handler `:700-731`):
1. Registers `task_id` in the `:evogit_cancelling_tasks` marker — a public `:set` ETS table created in `application.ex:29-34` (owned by the application process, survives scheduler restarts).
2. Scans `SchedMeta.task_id` (same pattern as `force_kill_task_agents`); for each live agent appends the cancel message via **`Store.append_pending_user_message/2`** (NOT `send_user_message` — that's a `GenServer.call` to the scheduler itself, which would self-call deadlock) + `Store.set_cancel_requested/1`.
3. Purges the task's agents from the LLM/tool waiting queues via **`Slots.purge_agents_from_queues/2`**, replying `{:error, :cancelled}` to each blocked `from` — an agent blocked on a zero-capacity (peak-paused) or saturated pool unblocks immediately; the raised error propagates → crash-retry → the agent enters cancel-grace at its next loop top.
4. Exact message text in `cancel_message/0` (`agent_scheduler.ex:375-383`): *"The task is being cancelled by the user. Please immediately save your work: commit any uncommitted changes, then call complete_task with a summary of what was accomplished. You are in a grace period and must call complete_task now."*

**`AgentScheduler.clear_cancelling_task/1`** (public `:412-414`, handler `:734-741`) — deletes the task from the marker, idempotent (defensive against a missing table). Called by TaskRegistry's guarded `clear_cancelling_marker/1` at terminal transitions.

**`AgentScheduler.cancelling_task?/1`** (public, `@doc false`) — the SHARED read of the `:evogit_cancelling_tasks` marker table (nil-safe when the table is undefined — a missing ETS table reads as not-cancelling). Used by both the scheduler's `run_agent` guard and `Dispatch.register_agent`'s new-agent registration guard when checking a task's cancel state; code checking the marker should call it rather than touching the ETS table directly.

**`run_agent` guard** (`agent_scheduler.ex:568-578`) — replies `{:error, :cancelled}` WITHOUT dispatch when the task is in the marker. Blocks Genesis Mode B's second sequential root agent (which shares the first root's `task_id`) from spawning after the first root completes during a cancel.

**New-agent registration guard** in `Dispatch.register_agent` (`dispatch.ex:103-113`) — agents registered while their task is in the marker (subagent spawns, crash-retries, queued agents starting late) immediately get the cancel message + `cancel_requested` flag, so they enter grace at their first loop top.

**`AgentScheduler.force_kill_task_agents/1`** (public `agent_scheduler.ex:367-369`, handler `:615-697`) — the brutal reverse-depth `:brutal_kill` cascade (removes refs from `ref_to_agent` first, purges queues, releases slots, `Lifecycle.cancel_agent` deepest-first), called by `TaskRegistry.force_kill_task/1`.

**RPC:** `RemoteAPI.cancel_task/1` (graceful — delegates to `TaskRegistry.cancel_task/1`, `remote_api.ex:374-377`) + `RemoteAPI.force_kill_task/1` (`:389-392`); `RemoteNode.cancel_task/2` + `RemoteNode.force_kill_task/2` (`remote_node.ex:449-483` — local-direct when `node == node()`, else `:erpc` routing).

## Shutdown & Restart Safety

Facts relevant to restarting the BEAM (e.g. a desktop auto-update) while agent tasks are in flight:

- **No `terminate/2` anywhere in the scheduler ecosystem** — `AgentScheduler`, `Slots`, `State`, `Store`, and `WorktreeManager` have none (the only `terminate` in the app is `TaskRegistry.terminate/2` → `:ok` no-op). **Worktree cleanup is purely monitor-driven**: `WorktreeManager.handle_info({:DOWN, ...})` (worktree_manager.ex:225-256) destroys the worktree when the AGENT process dies. When the app/BEAM stops, `EvoGit.Supervisor` terminates children in reverse order — `AgentGroupSupervisor` (application.ex:107, last child) goes FIRST, its `one_for_all` (agent_group_supervisor.ex:25) kills `EvoGit.TaskSupervisor` (all agent Tasks, brutal-killed per Task.Supervisor's default `shutdown: :brutal_kill`) + `AgentScheduler`; then WorktreeManager is terminated before it can process the queued `:DOWN`s. **Result: `.genesis/workers/worker_T<n>_A<m>/` dirs and `evogit-agent-T<n>-A<m>` branches remain on disk after any BEAM/app stop.**
- **Next-start cleanup is the lazy per-repo init** (`maybe_init_repo/3`, worktree_manager.ex:268-322, on the FIRST `create_worktree_for_agent` per repo): (1) `rm_rf_retry(worker_base)` deletes the whole `.genesis/workers/` dir (log-and-continue on failure — the following `mkdir_p_retry` is then a no-op), (2) `Git.prune_worktrees(repo_root)` prunes the worktree registry, (3) `clean_orphaned_branches/1` (worktree_manager.ex:456-472) deletes every `evogit-agent-*` branch, (4) `mkdir_p_retry` (raises `File.Error` on exhaustion). No cleanup at boot without a create request. **This full wipe runs ONLY when the persistent per-repo marker `:evogit_worktree_repos` is absent** — i.e. a genuine BEAM/app restart (all agents dead) or the first-ever create for a repo — AND **only for the PRIMARY repo** (steps 1 + 3 are gated on the `primary?` flag from `spec.repo_id`; foreign repos skip them to preserve real task branches). On a **WorktreeManager-only restart** (one_for_one child of `EvoGit.Supervisor`; agent Tasks keep running in their live worktrees) the marker survives → wipe skipped, only `mkdir_p` runs — a manager-only restart never re-wipes live worktrees. The marker is app-owned ETS (dies with the app), written before the first worktree exists, so "marker absent ⇒ no live worktrees for this repo" always holds.
- **Task rows survive in SQLite**; `TaskRegistry.init/1` (task_registry.ex:252-289) reconciles orphaned `:finalizing` → `:failed` and `:cancelling` → `:cancelled` synchronously at boot; `:running` rows are left to the one-shot `:lease_sweep` (task_registry.ex:1354-1394, scheduled 150s after init; `@lease_duration 120`/`@heartbeat_interval 60_000`/`@sweep_after (@lease_duration + 30) * 1000` at task_registry.ex:37-42), which marks `:running` + expired lease + no live agents → `:failed` ("Lease expired; owning instance no longer renewing"). In-flight agent ETS state (`AgentState.context`, `pending_user_messages`, `cancel_requested`) is LOST on BEAM death — ETS tables are owned by the application process.
- **Slot pools are memory-only** (`%State{}`: `llm_holders`, `llm_waiting`, `llm_backoff_until`, `llm_last_granted`, `tool_holders`, `tool_waiting` — state.ex:79-88). On scheduler restart the new `%State{}` is fresh (empty pools from `from_model_profiles`, state.ex:194-196) — capacity-derived holder sets can't leak. Blocked slot waiters (`GenServer.call(:infinity)`, agent_scheduler.ex:263-265, 296-298) exit with the scheduler; no explicit "shutting down" gate — the only slot gating is `state.paused` (slots.ex:89-93, 214-216) and per-model capacity/backoff.
- **Graceful-cancel marker `:evogit_cancelling_tasks`** is ETS owned by the application process — survives scheduler restarts but NOT app restarts; on boot the marker is empty (any `:cancelling` task is reconciled to `:cancelled` by TaskRegistry.init as above).

## Worktree Safety — Main-Copy HEAD Protection (foreign repos)

The foreign repo's MAIN working copy (repo root, not a `.genesis/workers/` worktree) must never have its HEAD moved by the framework pre-merge. Empirically verified on git 2.54.0 (Aug 2026 investigation of an intermittent bug where the foreign main HEAD landed on `evogit-agent-T<task>-A<agent>` and the review diff went EMPTY):

- **`git worktree add -b <branch> <path-inside-main> <sha>` never moves the main HEAD** (non-mutating w.r.t. the main copy); neither do `Git.create_branch/3` (`git branch <name> <sha>`) or CoW's `--no-checkout` + `git checkout <sha> -- .` (path-limited). The full CoW lifecycle (create → clean+checkout branch in WT → agent commit → destroy rm_rf+prune+branch -D → `git branch genesis/agent_*` → `merge-base`/`merge-tree` review reads) leaves the foreign main on its original branch with a clean tree.
- **`Git.clean`/`Git.checkout` are only safe in a REGISTERED linked worktree — and `assign_and_prepare_worktree/3` now GUARDS this** (worktrees.ex, `ensure_linked_worktree/2`): before running `Git.clean(wt)`/`Git.checkout(wt, branch_name)` it asserts `wt` is a registered linked worktree of the agent's repo. A registered linked worktree ALWAYS has a `.git` FILE whose content is `"gitdir: <path>"`, where `<path>` (after `EvoGit.Platform.safe_expand/2` — a UNC-preserving `Path.expand` — relative to the worktree, per git's own rule) lies under `<repo_root>/.git/worktrees/`. The repo root for the check is the `repo_root` param threaded from `prepare_new_worktree/5` (resolved by `Dispatch.resolve_agent_repo_root/2` — NOT `spec.phylo_node.repo`, which for subagents is the PARENT'S worktree path, not the root). On failure it returns `{:error, {:worktree_prepare_failed, :not_a_linked_worktree}}` WITHOUT running any git — no clean, no checkout, no rev-parse (a plain `File.read`/`Path` check only), so the main copy can never be touched. **Why the `.git`-file check and NOT `git -C wt rev-parse --git-common-dir`**: from a PLAIN dir inside the repo tree (the dangerous broken-registration state) git walks UP and returns `<repo_root>/.git` — identical to a real worktree's common dir — so a rev-parse check would PASS for exactly the dangerous state. The repo ROOT itself has a `.git` DIRECTORY (not a file) and a plain dir has no `.git` file at all — both fail the check. The guard failure propagates through `prepare_new_worktree/5`'s existing `{:error, {:worktree_prepare_failed, _}}` else clause → the agent's Runner raises → scheduler crash-retry (the retry's fresh create cleans the path first).
- **Leftover-dir removal now ESCALATES** (`destroy_leftovers/3` in `prepare_new_worktree/5`, and `WorktreeManager.destroy_worktree/3`): a failed `rm_rf_retry` after retries is no longer warn-and-continue. `destroy_leftovers/3` returns `{:error, {:worktree_create_failed, msg}}` (propagated through `prepare_new_worktree/5`'s existing else clause — the retried agent crash-retries) and **skips the `Git.prune_worktrees` for that path**: a dir that survives rm_rf WITH its `.git` file + registration is a SAFE registered worktree, but a partially-removed dir that is then pruned becomes a PLAIN unregistered dir — the leak precondition. The dir+registration pair is kept consistent so a later create/cleanup can rm_rf it properly. `destroy_worktree/3` (runs in the WorktreeManager GenServer — must never crash) likewise logs at ERROR level and skips both prune and branch delete on rm_rf failure.
- **`delete_branch_tolerant/2` (worktrees.ex:188-245) RECOVERS git's "cannot delete branch 'X' used by worktree at '<path>'"** (and the older "checked out at" wording — matched robustly via `branch_in_worktree?/1`, verified 2.54.0): the registration may be STALE (worktree dir already removed without a matching prune), so it runs `WorktreeRetry.retry_on_transient(fn -> Git.prune_worktrees(repo_root) end)` to drop stale registrations, then retries the delete ONCE with the same tolerant logic. A branch genuinely checked out in a LIVE worktree survives the prune and the retry still fails — `{:error, output}` (correct: a live checked-out branch is never force-deleted). Existing "branch not found" / repo-gone tolerance unchanged.
- **On git 2.54.0 the scheduler's own crash-retry interleavings FAIL SAFELY**: a stale `:DOWN` destroy racing a retry's fresh create deletes the new worktree's branch, so the retry's `git checkout` errors ("pathspec did not match") and main HEAD stays put (the run crashes and retries instead). The dangerous plain-dir+live-branch state is not reachable through scheduler code alone on 2.54.0 — but the `:not_a_linked_worktree` guard + destroy-leftovers escalation make it structurally unreachable regardless of git version (older git had `git worktree add`/prune corner-case bugs; Windows partial rm_rf can leave a plain dir).
- **LLM cd-escape into the repo root is HARD-BLOCKED at the tool layer**: `do_execute/5` (shell_tool.ex:243-248) first calls `main_copy_mutation_error/3` (shell_tool.ex:258-265) — if ANY resolved cd target equals `Path.expand(repo_root)` AND the command matches the mutating-git regex `\bgit\s+(checkout|switch|reset|merge|pull)\b` (`@mutating_git_regex`, shell_tool.ex:34), it returns an error string ("Error: This command changes directory into the repository's MAIN working copy ... and runs a mutating git command ... blocked ...") and NOTHING executes. Relative cd escapes are caught too: `@cd_regex` (shell_tool.ex:30) matches absolute paths, dot-relative escapes (`..`, `../..`, ...) and plain relative targets, and `cd_targets/2` (shell_tool.ex:534-540) resolves each target via `EvoGit.Platform.safe_expand(target, repo_path)` (the worktree cwd) before the comparison — so the attack shape `cd <foreign_root> && git checkout evogit-agent-*` from inside a writable-foreign-repo worktree resolves to the repo root and is blocked. `detect_cd_warnings/3` (shell_tool.ex:400) remains warning-only for cds that stay inside the worktree or cross between worktrees (no root+mutating-git combo). The block is tool-layer defense — `run_bash`/`run_git`/`run_powershell`/`curl` are in `@write_tools` (tools.ex:47-63) → ALLOWED for writable foreign repos (the read-only gate `maybe_block_read_only_foreign_repo/5`, tools.ex:221-243, only blocks `writable != true`), and the sandbox does not prevent the mutation: under bwrap the foreign repo's common `.git` dir is in the writable binds (bwrap.ex:425-429), and a same-tree checkout (HEAD/index-only) succeeds even with the working tree read-only; under systemd-run the whole filesystem stays writable (only `ReadWritePaths` additions, no `ReadOnlyPaths=/`); `:none` backend unrestricted. Residual risk is defense-in-depth: the block is string-pattern based — an agent that reaches the main copy through a command shape other than `cd`-into-root + `git checkout/switch/reset/merge/pull` could still move the main HEAD, so the prompts still tell agents never to run checkout/switch/reset against the repo root (manager/executor system prompts).

## AgentScheduler GenServer Hub (`agent_scheduler.ex`)

`agent_scheduler.ex` is ~1130 lines — the scheduler GenServer hub: agent lifecycle, per-model LLM/tool slot pools, config state + updates, cancellation markers, startup reconciliation. The sub-logic lives in the `agent_scheduler/` submodules (`state`, `slots`, `dispatch`, `worktree_manager`, `remote_api`, ...).

**Lazy config-read rule (regression-derived — do not regress)**: inside the scheduler GenServer, single-key config reads MUST go through the lazy per-key private helper `config_value/2` — never build the full config map eagerly for a single-key read. `config_map/1` evaluates `Platform.sandbox_backend()` and `Sandbox.capabilities()` on every call; evaluating those inside the GenServer for a single-key read altered blocking timing and deterministically regressed the system_sampler tests. `config_map/1` is used ONLY for the full `:get_config` reply; `@config_keys` (16 keys) enumerates the fields. New single-key config reads inside the scheduler must use `config_value/2`.

## Constraints

- Data structs are plain data with no behaviour or callbacks.
- `Slots`, `Worktrees`, `Dispatch`, `Subagents`, and `Lifecycle` are pure-function modules operating on `State.t()`; they don't maintain their own state.
- `AgentState` is shared (scheduler + agent processes); `SchedMeta` is scheduler-exclusive.
- Both ETS tables are created by **`EvoGit.Application`** (the application process), NOT by the `AgentScheduler` GenServer (see `application.ex:20-22`). Deliberate: the tables have **no heir**, so ownership must outlive a scheduler crash. Because they are owned by the long-lived application process, they **SURVIVE an `AgentScheduler` restart** — stale `SchedMeta` entries from the crashed instance remain. (Restart semantics: `AgentGroupSupervisor` is `strategy: :one_for_all`, so a scheduler crash also kills `EvoGit.TaskSupervisor` and all running agent Tasks. The GenServer `%State{}` is reset fresh on restart, but the ETS tables persist.)
- GenServer state must always be `%State{}`; use struct update syntax, not `Map.put/3`.
- The scheduler process NEVER calls git, does blocking filesystem I/O, or calls ANY worktree function (not even init) — all git operations (auto-commit, sync) happen in the agent (Task) process, and ALL worktree filesystem operations (rm_rf, mkdir_p, prune_worktrees, delete_branch) are owned by the `WorktreeManager` GenServer. The only worktree call is Runner → `WorktreeManager.create_worktree_for_agent/6` (named 1-hour timeout; create I/O offloaded to a spawned Task; reclaim via `:DOWN` monitor).
- Slot availability is derived from holder MapSets, never stored as a counter — this eliminates leak/deadlock bugs by construction.
- `SchedMeta.task_ref` stores a `%Task{}` struct (for `Task.shutdown/2`), NOT a bare reference. The `ref_to_agent` map still keys on `task.ref` (the monitor reference).
- `case`/`with` guards on ETS lookups are used ONLY where a genuine race can cause the entry to be absent (recycle_agent, try_dispatch, cancel_agent). Not used defensively everywhere.
- Cross-module calls: `Dispatch.process_queue/1` calls `Subagents.dispatch_ready_parent/3` for ready parents. `Lifecycle.handle_agent_crash/3` calls `Dispatch.try_dispatch/2`, `Dispatch.process_queue/1`, `Subagents.store_sub_result/3,4`, and `Subagents.maybe_resume_parent/2`.
- **Task archive table**: `:evogit_archive_records` is a **`:set` keyed by `{task_id, agent_id}`** (at most one record per agent per task — `EvoGit.AgentScheduler.Store.put_archive_record/3` writes are idempotent, so crash-retry double-writes are safe). Records are written at agent exit, consumed (collected into `Result.archive_records`) **AND** cleared at successful root completion in `Lifecycle.handle_task_result/3`, and reset defensively at task start in the `run_agent` call. **Archive record shape** (plain map): core fields `agent_id`, `parent_id`, `depth`, `objective`, `result` (ALWAYS a binary string), `base_commit`, `final_commit`, `archive_ref_start`/`archive_ref_final`, `branch_name`, `usage` (PLAIN MAP — **NOT a `%Usage{}` struct** — with exactly 9 canonical keys: `input_tokens`, `output_tokens`, `total_tokens`, `input_cost`, `output_cost`, `total_cost`, `cached_tokens`, `cache_creation_tokens`, and the derived `cache_hit_rate` [computed via `Usage.cache_hit_rate/1`, not a struct field]), `compression_count`, `repo_path`/`repo_id`/`repo_root`, `started_at`/`completed_at` (`%DateTime{}`); plus `:llm_settings` (model, model_id, context_compression_threshold, generation params), `:agent_settings` (`max_turns`, `max_retries`, `max_agent_depth`), and `:foreign_repos` (`%{id, root, description}` maps). **Only writer is `CompleteTask.complete/4`** via its private `write_archive_refs/7` (writes the `refs/genesis/archive/T{task_id}-A{agent_id}-start`/`-final` git refs + the ETS record through `Store.put_archive_record/3`); the per-record `usage` map is built exclusively by the private `format_usage_for_archive/1` — the SINGLE writer of per-agent usage into archive records (its 9 keys above are canonical; pinned by `complete_task_test.exs`). `complete/4` ALSO writes a metadata git note (`add_metadata_note/3`, gated on `base_commit` alone) whose usage shape is DIFFERENT (`input_tokens`/`output_tokens`/`total_tokens` + a legacy `cost` = `total_cost` key — see `format_usage_for_note`) — that note shape is NOT the archive contract, and archive usage NEVER contains a `cost` key. Consumer-side canonical reader: `EvoGit.Agent.Usage.from_archive_map/1` (handles both keyings → `%Usage{}`). A CRASH/FAILURE/ABORT writes NO record (crashed agents are ABSENT from `archive_records`, usage LOST — retry resets to `Usage.zero()`). **Keying caveat (dual keyings — both carry the SAME canonical names above)**: records are ATOM-keyed while live (ETS / pre-persistence `Result.archive_records`); after the Store JSON round-trip they are STRING-keyed maps with STRING timestamps (Jason encodes `%DateTime{}` as ISO strings; inner maps incl. `usage` are NOT re-atomized) — consumers must read either keying, and `Usage.from_archive_map/1` abstracts it.

### Per-message timestamp metadata convention

Every message in an agent's session memory (the `%ReqLLM.Message{}` list inside `%ReqLLM.Context{}.messages`, held in `AgentState.context` / `:evogit_agent_state` ETS) carries `metadata[:timestamp]` — **Unix seconds** (`System.system_time(:second)`), an atom-keyed integer consistent with the existing `:turn` metadata convention. Timestamps are stamped **at message-CREATION time in the agent code** — never in the Store.

**Single choke point — `ContextBuilder.tag_message_turn/2` (agent/context_builder.ex)**: every message produced by the agent loop passes through it, and it sets BOTH `metadata[:turn]` AND `metadata[:timestamp]` in one place. Idempotent via `Map.put_new` — an already-stamped message keeps its original `:turn` and `:timestamp`. `ContextBuilder.tag_message_timestamp/1` stamps just the timestamp (used at the nudge and compression-summary append sites). The agent subtree owns all stamping; see `apps/evo_git/lib/evo_git/agent/CONTEXT.md` for the full inventory of creation sites.

**`Store` is a dumb pass-through.** `batch_update_agent/2` (store.ex) writes the fields verbatim into ETS; only the broadcast kwlist is stripped (`:context` removed, `message_count` added when a context is updated). `update_agent_context/2` delegates to `batch_update_agent` — same path. Both return `:ok`. **No read-back exists anywhere** — data flows one way: agent → ETS/pubsub → frontend. The dashboard reads `msg.metadata[:timestamp]` (Unix seconds) directly from `RemoteAPI.get_agent_history/1` (raw structs, unchanged).

`DateTime` was deliberately avoided (struct — heavier, and Jason-encodes to ISO-8601 strings elsewhere in the codebase); an integer is timezone-agnostic and trivial for the dashboard to convert to local display time. **The dashboard interprets integer timestamps as Unix SECONDS** (`EvoDashWeb.Helpers.format_history_timestamp/1`) — do NOT switch granularity without updating evo_dash. Second-granularity means messages produced within the same second share a stamp — acceptable by design. Compression (`context_compression.ex:85-86`) rebuilds context as `[system, initial_user, summary]` — the fresh summary is stamped via `tag_message_timestamp/1` at creation (history collapse is by design, not a bug).
