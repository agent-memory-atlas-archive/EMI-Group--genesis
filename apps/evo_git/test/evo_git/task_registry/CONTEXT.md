# Test Directory — task_registry

## Intent

ExUnit tests for `EvoGit.TaskRegistry` and `EvoGit.Store` persistence/lifecycle behavior: status transitions & guards, lease/heartbeat, cleanup, skip-and-log of undecodable rows, archive metadata, restart durability, the merge/resume/runtime-opts context builders, the `:reflect` task-type routing, and failed-transition diagnostics.

13 files / 188 tests. Measured baseline at `--seed 0`: ~3.6 s (1.0 s async, 2.5 s sync) whole directory.

## API Surface

| File | Tests | Mode | Covers |
|------|-------|------|--------|
| `persistence_test.exs` (2142 lines) | 50 | sync | Store CRUD, `TaskInfo` field backfill (`normalize_tasks` on restart), registry restart round-trips (`stop_supervised(EvoGit.TaskRegistry)` + `start_supervised({TaskRegistry, task_store: EvoGit.Store, data_dir: ..., name: EvoGit.TaskRegistry})` — store kept running), recent-project persistence, GenServer/corruption resilience, `archive_metadata`, `describe "status recovery from spurious :failed"` (665), `describe "cross-node PubSub filtering"` (796), `describe "startup reconciliation of orphaned :finalizing tasks"` (847, incl. orphaned `:cancelling` → `:cancelled`), `describe "list_tasks_summary / by_path / changed_since — since filter semantics"` (996), `describe "list_task_ids/1"` (1168), `describe "recheck_task resolution"` (1303), `describe "recent projects — nil last_opened_at handling"` (1471), `describe "graceful cancel_task / force_kill_task"` (1519), `set_review_metadata/3` (8), `TaskInfo field backfill` (108), `persistence` (188), `recent projects persistence` (255), `GenServer resilience` (283), `corruption resilience — structural` (352) |
| `lease_heartbeat_test.exs` (183 lines) | 4 | sync | lease cleared on completion; heartbeat renews only owned leases (never sweeps — even an expired-lease unowned task stays `:running`); one-shot `:lease_sweep` message sweeps expired-lease unowned `:running` tasks → `:failed` + the canonical `:lease_expired`/`:lease_sweep` error payload; heartbeat renews an owned `:cancelling` task's lease |
| `cleanup_test.exs` (238 lines) | 5 | sync | `Cleanup.task_history_config/0` merged-shape contract (both keys present, positive integers — never assert the literal 100/14, a developer's real config may override); `cleanup_expired_tasks` age/count limits; never cleans `:running`/`:pending` |
| `store_skip_and_log_test.exs` (279 lines) | 4 | sync | `Store.safe_select_all_tasks/1`, `safe_select_all_projects/1`, `safe_select_paginated_tasks/2` skip-and-log semantics: undecodable rows are SKIPPED + `Logger.warning`, never moved (the only tables in `sqlite_master` stay `tasks`/`projects`; bad rows remain untouched in the live table). Injects genuinely-undecodable rows via raw `Xqlite.open`/`XqliteNIF.execute`: malformed JSON `opts` (`[1,2,3]` — raises in `decode_opts/1`) for tasks, INTEGER `last_opened_at` in a pre-created INTEGER-affinity `projects` table (raises in `decode_datetime/1`). No store restart needed — WAL mode lets the store's own connection see raw-injected rows. `with_log` asserts the `"Store: skipping undecodable row in <table> (id: ...)"` warning; negative case (all rows decodable → no warning); paginated `total_count` is SQL-level and includes skipped rows. Each test starts its own uniquely-named Store GenServer (NOT the global `EvoGit.Store`) |
| `merge_context_test.exs` (375 lines) | 13 | sync | `TaskRegistry.MergeContext.apply_merge_context/4` — the merge-conflict-resolution context builder for evolve tasks created with `:merge_from`/`:merge_target`. Strips the merge keys, overrides `starting_commit` with the previous task's `commit_sha` (preserves the caller's when nil/empty), carries over `foreign_repos` normalized from persisted string-keyed maps to `%ForeignRepo{}` structs with `writable`/`base_sha` intact (unparseable entries dropped; key absent when the prev task has none), applies per-repo starting commits via `PrevTaskRepos.apply_starting_commits/2` (each carried repo's `base_sha` OVERRIDDEN from the prev result `repos[repo_id]["commit_sha"]`; repos absent from the result keep their own `base_sha`; legacy prev task without a `repos` key → no crash), prepends the context block (incl. a `Writable foreign repos:` line rendering `<id>@<base_sha|HEAD>` for writable repos only) to the objective, passes unrelated caller opts through untouched; missing previous task → stripped opts returned unchanged. The TaskExecutor hook (`execute_task(:evolve, ...)`) is deliberately not tested (would invoke the full Evolution/LLM runtime) |
| `merge_context_block_test.exs` (88 lines) | 5 | **async** | Pure `MergeContext.build_merge_context_block/2` only (block content incl. task id / base + end SHAs / branch / merge target / goal / hints, nil base_sha + nil branch_name + nil merge target fallbacks, non-`TaskInfo` input → `""`). Builds a local `%TaskInfo{}`; never touches the Store or the registry |
| `resume_context_test.exs` (174 lines) | 5 | sync | `TaskRegistry.ResumeContext.apply_resume_context/3` — the resume-context builder for evolve tasks resuming via `:resume_from`. Caller `:foreign_repos` normalized from string-keyed Codec maps / structs to `%ForeignRepo{}` structs with `writable`/`base_sha` intact, per-repo `base_sha` overridden from the prev result `repos` map, `:starting_commit` overridden with the prev `commit_sha` (nil-prev branch leaves it alone), no `:foreign_repos` key when the caller has none (both branches: prev task found / not found) |
| `resume_context_block_test.exs` (214 lines) | 16 | **async** | Pure `ResumeContext.build_resume_context_block/1` (fully delimited block, `<<<BEGIN/END OBJECTIVE/RESULT>>>` fences, multi-line content, `:prompt` fallback, nil/empty commit/objective/result omission, non-`TaskInfo` input → `""`) + `extract_result_summary/1` (`{:ok, %{result: binary|atom}}`, `{:error, reason}`, `{:exit, reason}`, everything else → nil) |
| `runtime_opts_test.exs` (369 lines) | 38 | **async** | `TaskRegistry.RuntimeOpts` — pure runtime-opts builder. `build_common_runtime_opts/3`: minimal opts (repo_path, mode default `:simple`, task_id) for evolve and genesis (genesis requires an explicit `"new"`/`"existing"` mode); optional keys (`node_path`, `starting_commit`, `foreign_repos`, `archive`, `model_id`, `build_system`, `agent`, `model_id_locked`) threaded only when present — empty-string/nil `model_id`/`agent` omitted, `model_id_locked` only when truthy. Mode normalization: `evolution_mode_atom/1`, `genesis_mode_atom/1` (custom → evolve-only error), `mode_atom/2`. ⚠️ Mode values are STRICT STRING-ONLY (atoms raise) |
| `task_executor_reflect_test.exs` (155 lines) | 3 | sync | The `:reflect` clause of `TaskExecutor` (repo-less self-reflective task type): `execute_task(:reflect, opts, task_id)` tolerates opts WITHOUT `:path` — the reflect clause deliberately skips `RuntimeOpts.build_common_runtime_opts/3` (which `Keyword.fetch!`s `:path`) — and routes into `EvoGit.Runtime.SelfReflective.run`; unexpected opts keys don't crash. End-to-end: `start_task(:reflect)` without `:path` completes `:failed` with `{:error, :llm_not_configured}` under `without_model_profiles/1` (the no-real-LLM idiom from `test/evo_git/runtime/evolution_test.exs`); the wait is **event-driven** (`await_terminal_status/1` receives the registry's terminal `"tasks"` broadcast, 5 s bound) — no polling |
| `completion_logging_test.exs` (246 lines) | 6 | sync | The observability + lost-result-guard `Logger` lines in `EvoGit.TaskRegistry`. Normal completion: seed `:running`, inject a fake `%Task{}` (live `spawn` pid + `make_ref()`) into task_refs via `:sys.replace_state`, `capture_info_logs` around a `{ref, {:ok, %{...}}}` send — asserts BOTH `"... wrapper returned — persisting terminal status :completed"` AND `"... terminal status persisted: :completed"` fire and the task lands `:completed` (⚠️ self-cast mailbox gotcha: the wrapper handler's terminal update is a SELF-cast queued BEHIND an already-queued `list_tasks` call — the flush idiom needs a SECOND call inside the capture window). Wrapper `{:error, _}` → kind `:error` / `{:exit, _}` → kind `:exit` payloads (source `:result_handler`). Unknown-ref guards: a raw `{make_ref(), {:ok, ...}}` send logs `"received task result for unknown ref ... result dropped"` and a raw `{:DOWN, make_ref(), :process, ...}` send logs `"received :DOWN for unknown ref ... ignoring"` — registry survives both. `capture_info_logs/1` helper: `config/test.exs` sets `config :logger, level: :warning` and ExUnit.CaptureLog does NOT override the primary Logger level, so `:info` lines are invisible unless the helper first lowers `Logger.level()` (:debug) for the capture and restores it after (safe: `async: false`) |
| `finalizing_watchdog_test.exs` (313 lines) | 6 | sync | The stuck-`:finalizing` grace watchdog (lib `task_registry.ex`, "Stuck-`:finalizing` Grace Watchdog"): a local-node `:finalizing` PubSub broadcast schedules a ONE-SHOT `{:recheck_task, task_id}` timer; when it fires while the task is STILL `:finalizing`, the task is resolved to `:failed` with the exact result `"Finalization did not complete within N minutes"` (env seam `:finalizing_watchdog_grace_minutes` — nil/unset → 60, numeric ≥ 0 → minutes, `false` → disabled; `config/test.exs` sets 1, per-test overrides use a local `set_grace/1` helper with snapshot + `on_exit` restore). Scenarios: scheduled-timer path end-to-end (grace 0, resolved WITHOUT a manual recheck send), hand-driven resolution arm (grace 2/3, exact message + the canonical `kind: :timeout`/`source: :finalizing_watchdog` error payload incl. the raw-Store column round-trip), terminal rows untouched, late wrapper `{ref, result}`/`{:DOWN, ...}` after resolution is a NO-OP (task_refs entry gone → row preserved, no `:completed` broadcast), `false` disables the watchdog entirely (row stays `:finalizing`) |
| `diagnostics_test.exs` (296 lines) | 33 | **async** | `TaskRegistry.Diagnostics` — failed-transition diagnostic logging. `log_failed_transition/4` (greppable `"TaskRegistry: FAILED_TRANSITION"` prefix + task_id/source/prev_status; `:result` opt; `:extra` fields; `:caller_info` tuple → caller_pid + caller stack), `capture_stacktrace/1`, `format_stacktrace/1`, `format_stacktrace_frame/1`, `format_function/2`, `format_location/1`. Pure Logger/`Process.info` functions — no GenServer/Store |

## `async: false` policy — 9 sync modules, 4 async

**9 modules use `EvoGit.TaskRegistryCase, async: false` and MUST stay sync**: `persistence`, `lease_heartbeat`, `cleanup`, `store_skip_and_log`, `merge_context`, `resume_context`, `task_executor_reflect`, `completion_logging`, `finalizing_watchdog`.
Forcing global state: `EvoGit.TaskRegistryCase#setup` calls `Supervisor.terminate_child(EvoGit.Supervisor, EvoGit.TaskRegistry)` and `(..., EvoGit.Store)` and then re-starts FRESH children registered under the SAME GLOBAL names (`name: EvoGit.TaskRegistry`) — a concurrently running module would observe the swapped singletons. Every sync module carries the same one-line `@moduledoc` rationale; do NOT remove it or flip the flag.

**4 modules are `async: true`** (`ExUnit.Case`): `runtime_opts`, `diagnostics`, `merge_context_block`, `resume_context_block`. The last two are pure-function extractions — see "Notes for Agents".

## Harness — `test/support/task_registry_case.ex` (82 lines, owned centrally)

- `setup`: terminate-child + `start_supervised` a FRESH `EvoGit.Store` (`data_dir: <temp>/tasks.sqlite`) and FRESH `TaskRegistry` (same Store, `name: EvoGit.TaskRegistry`) — a fresh empty SQLite DB per test. `on_exit`: `File.rm_rf(root)` + `Supervisor.restart_child` of Store and TaskRegistry.
- **Measured cost: ~30-35 ms of fixture per test** (a test that does nothing but read config still takes ~36 ms). This is the dominant cost of the sync pool in this directory.
- Helpers: `trigger_cleanup!/0` (direct `Cleanup.cleanup_expired_tasks(EvoGit.Store)`), `cleanup_process/1`, `old_age_days/0` / `within_age_days/0` (read runtime config, fallback 14).
- Isolation comes from the terminate-child + fresh-temp-DB pattern: `task_registry_case.ex` gives each test a FRESH empty SQLite DB. The app-level guard is `config/test.exs` pinning `config :evo_git, :data_dir` to a unique per-run temp path.
- Sync idiom: `update_task_status` is a cast → always `TaskRegistry.list_tasks()` (a call) afterwards to flush the mailbox before asserting.
- Restart pattern: `stop_supervised(EvoGit.TaskRegistry)` then `start_supervised({TaskRegistry, task_store: EvoGit.Store, data_dir: data_dir, name: EvoGit.TaskRegistry})` — keep the SAME Store running (it is durable on disk) so data survives the registry restart.

## Timing & Robustness Patterns (use these, not fixed sleeps)

- **Event-driven waits over polling.** For anything the registry BROADCASTS, subscribe to `"tasks"` BEFORE triggering the action, then `receive` the `{:task_updated, id, status, node}` message with a generous bound (e.g. 5 s) and `flunk` on timeout. Never poll `get_task/1` in a `Process.sleep(N)` loop. Live examples: `task_executor_reflect_test.exs` (`await_terminal_status/1`), `persistence_test.exs` `describe "recheck_task resolution"`.
- **FIFO-sync instead of a blind quiet window.** To assert that NO broadcast was emitted, `send` the trigger, then make a synchronous `GenServer.call` to the same registry (e.g. `TaskRegistry.list_tasks()`), then `refute_receive ..., 0`. The call is queued strictly AFTER the trigger, so once it returns the handler has fully run; and any broadcast the handler emitted was sent registry→test-process BEFORE the call reply (same sender→same receiver ⇒ signal order guaranteed), so it is already in the mailbox. This makes the check complete evidence at zero wall-clock cost. Examples: `persistence_test.exs` `describe "recheck_task resolution"`, `finalizing_watchdog_test.exs` "late wrapper result ... is a no-op".
- **One shared quiet window for N message shapes.** Two sequential `refute_receive ..., 200` calls cost 400 ms. Use a single `receive`/`after 200` that `flunk`s on the FIRST unexpected message instead (`persistence_test.exs` "graceful cancel emits exactly one tasks-topic broadcast").
- **`Process.sleep(:infinity)` inside `spawn(fn -> ... end)` is NOT a wait** — it keeps a fake wrapper process alive. Leave those alone.
- **A negative quiet window is only needed when an async trigger is unobservable** (e.g. a `Process.send_after` timer). There a bounded `refute_receive ..., N` (N ≈ 150 ms, far beyond any immediate-timer fire) is the right tool — see `finalizing_watchdog_test.exs` "disabled grace (false) ...".

## Key Status-Guard Semantics (pinned by `persistence_test.exs` `describe "status recovery from spurious :failed"` + `describe "cross-node PubSub filtering"`)

- **PubSub path** (`handle_info({:task_updated, id, status, node})`, lib `task_registry.ex`): updates are applied only when `node == node()` — a cross-node broadcast is ignored. Stale update rejected ONLY when current status is `[:completed, :cancelled, :cancelling]`. `:failed` is deliberately NOT in the guard — a `:finalizing` broadcast after a spurious `:failed` is ACCEPTED ("recovery before completion": task seeded `:failed`, `Phoenix.PubSub.broadcast(EvoGit.PubSub, "tasks", {:task_updated, task_id, :finalizing, node()})`, assert `status == :finalizing`).
- **Cast path** (`handle_update_status`, lib `task_registry.ex`): rejects only terminal→terminal transitions where `task.status != status` and `status != :completed` — i.e. `:failed` → `:completed` recovery is allowed; `:completed`/`:cancelled` are never overwritten.
- **Terminal set is `[:completed, :failed, :cancelled]`** (cast path) but the PubSub guard uses only `[:completed, :cancelled, :cancelling]` — `:failed` is recoverable in both paths.
- Runtime emits ONLY `:finalizing` on the `"tasks"` topic (`Helpers.notify_finalizing/1`); a `:failed` received on PubSub triggers `Diagnostics.log_failed_transition(..., :task_updated_pubsub, ...)`.
- A local-node `:finalizing` PubSub broadcast now ALSO schedules the ONE-SHOT stuck-`:finalizing` grace watchdog (lib `task_registry.ex` handler → `schedule_finalizing_watchdog/1`, only on the successful `put_task` `:ok` arm — never the disk-full path): `Process.send_after(self(), {:recheck_task, task_id}, trunc(minutes * 60_000))`. Grace = `Application.get_env(:evo_git, :finalizing_watchdog_grace_minutes)`, declared in `config/`: 60 in `config/config.exs` (prod default; module fallback 60 when unset), **1 in `config/test.exs`**. `false` → disabled (no timer); numeric ≥ 0 → minutes (0 = immediate).

## Known Test Gaps

1. **No restart-recovery test for an unowned expired-lease `:running` row.** The `:finalizing` case IS covered (`describe "startup reconciliation of orphaned :finalizing tasks"`, incl. orphaned `:cancelling` → `:cancelled`): seeds a `:finalizing` row + a `:running` control row (both with future leases), restarts the registry, and asserts the orphaned `:finalizing` row → `:failed` (finished_at set, lease cleared, result a binary) while the `:running` row with a valid lease is left untouched (orphaned-`:running` sweeping is the `:lease_sweep` path). Untested: a fresh registry against a `:running` row with a valid lease leaves it untouched (only pinned as the startup-reconciliation control row); expired-lease unowned `:running` rows get swept only by the one-shot sweep 150 s after init (or `{:recheck_task, id}` resolution). The restart/backfill tests only use `:completed` tasks. Closest analogues: `lease_heartbeat_test.exs` (seeds an expired-lease unowned `:running` row and sends `:lease_sweep` DIRECTLY — no restart; the startup sweep is one-shot at `(@lease_duration + 30) * 1000` and never fires during a test run); `store_test.exs` (status field round-trips as atom for all known values incl. `:finalizing`).
2. **`:failed` → `:finalizing` via the CAST path** (`update_task_status/4`) is untested — only the PubSub path is covered.
3. **`:running`-row recheck resolution and the 30 s-reschedule path have NO timer-driven test** — all recheck tests drive the handler via raw `send(EvoGit.TaskRegistry, {:recheck_task, id})` and never wait on the timer. The `:finalizing` recheck arm IS timer-driven now (`finalizing_watchdog_test.exs`, grace 0).
4. **No end-to-end `start_task(:genesis/:evolve)` lifecycle test in this dir — and `TaskExecutor.execute_task(:genesis/:evolve)` is never executed by any test here.** The ONLY real `start_task` lifecycle tests are `task_executor_reflect_test.exs` (`start_task(:reflect)` → terminal `:failed` with result `{:error, :llm_not_configured}` under `without_model_profiles`) and, outside this dir, `cli_task_routing_test.exs` / `cli_agent_flag_test.exs`. `start_task` elsewhere appears only as a direct `handle_call({:start_task, id, type, opts}, from, state)` invocation asserting the `:cancelled`/`:cancelling` guard reply `{:error, :cancelled}`. Fake `%Task{}` `mfa` payloads reference `TaskExecutor.execute_task` but are never invoked — their content is irrelevant. `merge_context_test.exs` states the `execute_task(:evolve, ...)` hook is deliberately untested (full Evolution/LLM runtime).
   Data-plane contract pinned by tests + lib: public `start_task(type, opts)` generates the id via `TaskExecutor.generate_id()` and `GenServer.call`s `{:start_task, id, type, opts}`; the handler guards existing rows with status `[:cancelled, :cancelling]` → `{:reply, {:error, :cancelled}, state}`; otherwise spawns `Task.Supervisor.async_nolink(EvoGit.TaskSupervisor, TaskExecutor, :execute_task, [type, opts, id])`, persists a `%TaskInfo{status: :running}` row (ref nulled — runtime-only; disk-full tolerated in-memory with warning), broadcasts `{:task_updated, id, :running, node()}` on `"tasks"`, and replies `{:ok, task}`. **Wrapper completion arrives as raw `{ref, result}`** (`{:ok, %{result:, commit_sha:, branch_name:, tag:}}` | `{:error, _}`) **and `{:DOWN, ref, :process, pid, reason}`** — exactly the shapes the fake-injection tests `send` directly; the terminal write removes the `task_refs` entry.
   ⚠️ **`RuntimeOpts` mode is STRICT STRING-ONLY**: `evolution_mode_atom`/`genesis_mode_atom` raise `ArgumentError` for atoms/non-strings, and genesis without an explicit `"new"`/`"existing"` raises. The CLI and shell task-opts modes are therefore ALWAYS STRINGS (never atoms).

### Known lib quirk (verified, tests pin the reachable semantics)
`TaskRegistry.resolve_recheck_task/3`'s `{:ok, %{branch_name: _}}` result-extraction branch remains **unreachable for `:running` rows at runtime** — `handle_info({:recheck_task, _})` gates on `Lease.sched_meta_has_active_agents?/1` (true when ANY ETS entry exists for the task_id → reschedules), while `Lease.lookup_sched_meta_result/1` (which supplies the result) REQUIRES an entry with `parent_id == nil` to exist. Mutually exclusive → the only reachable recheck outcome for a `:running` row is `{:completed, nil}` (warning logged). The grace watchdog schedules the FIRST recheck for tasks that entered `:finalizing` — those take the handler's dedicated `:finalizing` arm and never reach `resolve_recheck_task/3`; the generic `:running` arm has no production trigger of its own (nothing schedules a first recheck for a `:running` row — only the watchdog and the generic arm's own 30 s self-reschedule ever emit the message), so it is exercised only by raw `send` in tests.
Note the `describe "recheck_task resolution"` `setup` creates/deletes `:evogit_sched_meta` when the scheduler ETS table is absent and clears it before/after each test (`:ets.delete_all_objects` only, never `:ets.delete`).

## Notes for Agents

- **NEVER put pure-function tests inside a `EvoGit.TaskRegistryCase` module.** The fixture costs ~30-35 ms per test and exists only to provide an isolated Store + registry. Pure tests (helpers over a locally-built `%TaskInfo{}`, string builders, option builders) belong in an `async: true` `ExUnit.Case` module — `merge_context_block_test.exs` / `resume_context_block_test.exs` are the precedent (both extracted from their sync siblings for exactly this reason).
- **`async: false` on a `TaskRegistryCase` module is non-negotiable**; do not attempt to make them async (they and the whole suite would become racy). Unlocking them would require changing the central `EvoGit.TaskRegistryCase` to register an isolated Store/TaskRegistry under UNIQUE names and pointing each test at that instance — an umbrella-central (`test/support/`) change, not a change inside this directory.
- **`Process.sleep(N)` as a wait is banned here** — use the timing patterns above. The only acceptable fixed window is a bounded `refute_receive` for an unobservable async trigger.
- `runtime_opts_test.exs` and `diagnostics_test.exs` were already `async: true` — keep them that way.

## Constraints

- No mocking — real SQLite via xqlite, real `EvoGit.Store`/`TaskRegistry` GenServers; corrupt rows injected via raw `XqliteNIF.execute`.
- `EvoGit.TaskRegistryCase, async: false` for the 9 Store/registry-touching files; plain `ExUnit.Case, async: true` for the 4 pure modules.
- Tests must not mutate BEAM-global state outside their module's own fixture lifecycle; per-test global-env seams are snapshot + restored in `on_exit` (see `finalizing_watchdog_test.exs`'s `set_grace/1`).
