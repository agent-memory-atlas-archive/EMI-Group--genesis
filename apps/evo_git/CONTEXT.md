# EvoGit — Core Runtime Application

## Intent
The `:evo_git` OTP application implements an evolutionary software development runtime where LLM-powered agents create, analyze, and modify codebases using git worktree isolation. It models a codebase as a **Context Tree** (spatial dimension) and a **Phylogenetic Graph** (temporal dimension), evolved through a DAG of git commits. Two-phase execution — **Genesis** (create a codebase from a prompt) and **Evolution** (modify an existing one) — plus a repo-less **self-reflective** agent (`:reflect` task type) and an LLM/tool slot-gated scheduler. Task persistence (`EvoGit.Store`/`EvoGit.TaskRegistry`) lives in this app so the headless `genesis_remote` daemon has full task history.

### SSH Remote Development
Genesis supports SSH remote development (VSCode Remote-SSH-style): a headless `:evo_git` daemon (`genesis_remote` release) runs on a remote server; the local Phoenix dashboard controls it over an SSH tunnel via Erlang distribution. Foundation modules: `EvoGit.RemoteConnections` (TOML SSH-target store), `EvoGit.RemoteConnection` (bootstrap + connect lifecycle), `EvoGit.EpmdDist` (EPMD-less distribution), `EvoGit.Distribution` (on-demand distribution), `EvoGit.AgentScheduler.RemoteAPI` + `EvoGit.RemoteNode` (RPC surface). Full detail: `lib/evo_git/CONTEXT.md`.

## Routing Table
- `./lib/` → Application source (agents, core domain, adapters, runtime, scheduler, config)
- `./test/` → ExUnit test suite using real git operations on temp directories
- `./lib/evo_git/core/` → `ContextNode` (spatial) and `PhyloGraphNode` (temporal) data structures
- `./lib/evo_git/adapters/` → `Git` CLI adapter, `GitHub` (gh) adapter, `CowWorktree` (CoW worktree creation), `GitEnv` (commit-identity env) — detail in `adapters/CONTEXT.md`
- `./lib/evo_git/agent/` → Agent behaviour, Runner loop, tool library, context compression, subagent processing, usage tracking — detail in `agent/CONTEXT.md` (incl. Grace Period/Cancellation, Update-Timing Safety, Repo-Less Agents)
- `./lib/evo_git/agents/` → Agent implementations (Manager, Executor, TaskScheduler, Investigator, Architect, ContextExtractor, SkillExtractor, GenesisPlanner, Custom, SelfReflective) — detail in `agents/CONTEXT.md`
- `./lib/evo_git/custom_agents/` → `EvoGit.CustomAgents` (agents.toml store) + `EvoGit.CustomAgents.ModelSelector` (per-agent model selection script) — detail in `custom_agents/CONTEXT.md`
- `./lib/evo_git/runtime/` → Genesis (incl. Mode B two-root), Evolution (simple + custom modes), SelfReflective (repo-less runtime), Prompts — detail in `runtime/CONTEXT.md`
- `./lib/evo_git/agent_scheduler/` → `AgentState`, `SchedMeta`, `Slots` (LLM/tool slot pools), `Worktrees`, `WorktreeManager`, `RemoteAPI`, Repo-Less Plumbing, Graceful Cancellation, Shutdown & Restart Safety — detail in `agent_scheduler/CONTEXT.md`
- `./lib/evo_git/config/` → `EvoGit.Config` — 3-level config (defaults → user TOML → runtime overrides), schema, LLM catalog, Config Caching — detail in `config/CONTEXT.md`
- `./lib/evo_git/sandbox/` → `EvoGit.Sandbox` multi-platform backends (systemd-run, bwrap, sandbox-exec, none) + `SandboxSlice`/`SandboxProcessRegistry` — detail in `sandbox/CONTEXT.md`
- `./lib/evo_git/store/` → `EvoGit.Store` (SQLite) + `Store.Codec` — task/project persistence, summary projections, disk-full handling — detail in `store/CONTEXT.md`
- `./lib/evo_git/task_registry/` → `TaskRegistry` support modules — `Cleanup`, `Diagnostics`, `Lease`, `MergeContext`, `ResumeContext`, `RuntimeOpts`, `TaskExecutor`, Graceful Cancellation & Force Kill, Restart Recovery — detail in `task_registry/CONTEXT.md`
- `./lib/evo_git/agent/tools/` → Tool implementations incl. the repo-less `run_command` shell tool (task control, user guides, system info) — detail in `agent/tools/CONTEXT.md`
- `./lib/mix/tasks/` → `Mix.Tasks.Changelog`, `Mix.Tasks.Bump.Version`, `Mix.Tasks.Migrate.Store` (+ their per-call `root:`/`shell:`/summarizer-seam injection surface) — detail in `lib/mix/tasks/CONTEXT.md` and root `./CONTEXT.md` ("AI changelog generation"/"Versioning")
- SSH remote (RemoteConnection, RemoteBootstrap, EpmdDist, Distribution, tunnel readiness, bootstrap safeguards) → `lib/evo_git/CONTEXT.md` (RemoteConnection/RemoteBootstrap sections) + `apps/evo_dash/CONTEXT.md` (dashboard UX)
- Task persistence + startup reconciliation → `./lib/evo_git/store/` + `./lib/evo_git/task_registry/`
- Config caching behavior (remote/SSH scenarios) → `./lib/evo_git/config/` (Config Caching)
- Peak/off-peak hour scheduling → `./lib/evo_git/agent_scheduler/` (Slot Management) + `./lib/evo_git/config/`
- Cross-node RPC payload profile → `lib/evo_git/CONTEXT.md` (Review/RemoteNode Return-Shape Contract)
- Review API surface (per-repo-path capability vs task-level-only review state) → `./lib/evo_git/` (`review.ex` + `remote_node.ex`) + `./lib/evo_git/agent_scheduler/` (RemoteAPI mirrors) + `./lib/evo_git/task_registry/` (`set_review_status`/`set_review_metadata`) + `./lib/evo_git/store/` (single `tasks.review_status` column; no per-repo review state exists)
- Desktop backend port / orphan prevention / task cancellation model / ReqLLMPool sizing / peak-hours / merge-conflict check / native directory picker → root `./CONTEXT.md` (do not duplicate)

## API Surface

| Module | Description |
|---|---|
| `EvoGit` | Sandboxed command execution via `EvoGit.Sandbox` (`sandbox_run/4`, `sandbox_args/4`) |
| `EvoGit.Application` | OTP app callback — starts `EvoGit.PubSub` (PG2), `EvoGit.Supervisor` (agent group incl. `TaskSupervisor` + `AgentScheduler`, `PubSub.Throttle`, `SystemSampler`), `Store`, `TaskRegistry` + `ProcessRegistry`, `RemoteConnection.Registry`/`Supervisor`, `PeakHourEngine`, and conditionally `SandboxProcessRegistry` + `SandboxSlice` on Linux |
| `EvoGit.CLI` / `CLI.Parser` / `CLI.Setup` | CLI entry + `OptionParser` parsing (`-m`, `-R`, `--mode`, `--agent`, `-f`, `-p`, `--starting-commit`, `--node-path`, `--archive`, `--build-system`) + interactive setup wizard (provider/model selection, API key entry, config persistence). `genesis`/`evolve`/`reflect` are thin clients of the task data plane: they build task opts (ABSOLUTE `:path` resolved in the CLI process, STRING `:mode`, `:prompt`/`:objective`, put-if-present `:build_system`/`:node_path`/`:starting_commit`/`:agent`/`:archive`, always `:foreign_repos`) and enqueue via `TaskRegistry.start_task/2`, then block on a `"tasks"` PubSub subscription until `:completed \| :failed \| :cancelled` (`{:ok, id}` on completed / `{:error, msg}` on failed/cancelled; main never `System.halt`s). A raw first arg of `"run"` short-circuits BEFORE OptionParser → `run '<command string>'` executes via `EvoGit.CommandShell.execute(command, approval: :auto)` (terminal user = human authorizer). `-m/--model` is task-level model selection resolved pre-enqueue against configured `[[llm.models]]` profiles via `resolve_model_id/2` (sets `model_id_locked: true`). Removed flags (`-c/--concurrency`, `--tool-concurrency`, `-r/--retries`, `-t/--max-turns`, `--max-turns-root`) no longer parse — main scans raw argv and prints a "... was removed; set ... in config.toml ..." notice; `print_help` documents them + their config.toml keys. The CLI makes NO scheduler/config overrides (`AgentScheduler.update_config/1` is never called from the CLI). |
| `EvoGit.PromptFile` | Reads prompt/objective files for the CLI `-f`/`--file` option. **Plain text only**: non-UTF-8 binaries/PDFs/DOCX rejected with `{:error, {:not_text, ext}}`. `.docx`/`.pdf` conversion is an `evo_dash` frontend feature (`EvoDash.AttachedFile`) — deliberately not in core so `genesis_remote` stays lean |
| `EvoGit.Agent` / `Agent.Usage` / `AgentSpec` | Behaviour injecting the agent loop/tool dispatch/subagent management; `Usage` struct (`add/2`, `from_response_usage/1`, `zero/0`); `AgentSpec` opts carry `:archive`, `:model_id`, `:custom_agent_id`, `:model_id_locked`, `:repo_less`, `:repo_notes`, `:foreign_repos` (task-level `[ForeignRepo.t()]` — the role source for the spawn gate + tool-layer write gating). Detail: `agent/CONTEXT.md` |
| `EvoGit.Agents.Custom` | Runtime agent for ALL user-defined agents.toml agents — callbacks resolve via `Process.get(:custom_agent_id)` → `EvoGit.CustomAgents.get/1`; ROOT-only (`subagent_tool_name/0` nil); missing/unknown id raises descriptively. Detail: `custom_agents/CONTEXT.md` + `agents/CONTEXT.md` |
| `EvoGit.Agents.SelfReflective` | Repo-less chatbot agent (read-only over the Genesis source; task control, user guides, and system info via the single `run_command` shell tool). See "Self-Reflective Agent" section. Detail: `agents/CONTEXT.md` |
| `EvoGit.CommandShell` | Pure string parser + declarative command registry + whitelist dispatch for the self-reflective agent's task-control shell (`command_shell.ex`, sibling of `PeakHours` — no tools/ subdir). Parses a command STRING (`ListTasks.list_tasks`, `StartTask.start_task evolve "Write a parser"`) and dispatches through a **compile-time literal registry** of the 10 command-handler modules in `agent/tools/` (`ListTasks.list_tasks`/`GetTask.get_task`/`StartTask.start_task`/`CancelTask.cancel_task`/`ForceKillTask.force_kill_task`/`DeleteTask.delete_task`/`SpawnInvestigator.spawn_investigator`, `GuideUser.guide_user`, `ListRecentProjects.list_recent_projects`, `SystemInfo.system_info`) + built-in `help`. **Security levels**: every registry entry carries `level:` — 1 = safe read-only, executes immediately (`ListTasks`/`GetTask`/`ListRecentProjects`/`SystemInfo`/`SpawnInvestigator` + builtin `help`); 2 = needs user attention (`GuideUser.guide_user`); 3 = real side effects (`StartTask`/`CancelTask`/`ForceKillTask`/`DeleteTask`). Public `security_level/1` (path → 1\|2\|3; `"help"`/`"Help"` → 1; unknown/non-binary → 1). Approval mode is per-call: `execute/2` takes `opts[:approval]` ∈ `:auto \| :chat` (`:chat` default; `execute/1` delegates with default opts). In `:chat` mode (the self-reflective agent's `run_command` tool), level-2/3 commands route through `EvoGit.CommandApproval.request/5` at the dispatch choke point (blocking until user approval in the /help chat) and run ONLY on `:approved` — denied/timed-out fail closed (`{:error, "Action denied by the user: ..."}` / `{:error, "The user did not confirm ... in time ..."}`; the handler never runs); level-1 commands and parse/validation errors behave exactly as before (validation errors surface BEFORE the gate). In `:auto` mode (CLI `run` subcommand — the terminal user is the human authorizer) the gate is bypassed for ALL levels and every command runs immediately. Security: no `Code.eval_string`, no input-derived module/function names, **no `String.to_atom` on input** (enum/bool/statuses validated against literal lists, passed as strings), whitelist-only dispatch, length guardrails (`@max_command_length` 4000 / `@max_tokens` 40 / `@max_token_length` 2000). Public API: `execute/1`, `execute/2`, `list_commands/0`, `security_level/1`, `help/0`, `help/1`. Detail: `lib/evo_git/CONTEXT.md` + `agent/tools/CONTEXT.md` |
| `EvoGit.Agent.Tools.RunCommand` | The single `run_command` tool exposing the command shell to the self-reflective agent — dispatch-registered ONLY (never in `schemas/0` or `read_only_schemas/0`); passes the `command` string to `EvoGit.CommandShell.execute/1` (the `:chat` approval mode — as opposed to the CLI `run` subcommand's `:auto`) and returns handler output or an `Error: ...` string. Level-2/3 commands pause inside the shell awaiting user approval in the /help chat (`EvoGit.CommandApproval`) — the tool task blocks; level-1 commands return immediately. Detail: `agent/tools/CONTEXT.md` |
| `EvoGit.CustomAgents` / `CustomAgents.ModelSelector` | Pure TOML store for `<config_dir>/agents.toml` (CRUD, slugified ids, `[model_selection]` script section) + compile-once/per-spawn model-selection script evaluation (never raises). Detail: `custom_agents/CONTEXT.md` |
| `EvoGit.AgentScheduler` | GenServer for scheduling: agent lifecycles, ETS state (`:evogit_agent_state`, `:evogit_sched_meta`, `:evogit_archive_records`), LLM/tool slot pools, config updates; per-model LLM slot read `get_llm_slot_status/0` (live holder/queue/effective-capacity map — the sampler's `llm_slots` source). Scheduler-internal status model: `:pending \| :running \| :waiting \| :ready`. Detail: `agent_scheduler/CONTEXT.md` |
| `EvoGit.AgentScheduler.WorktreeManager` | Stateful GenServer owning the entire worktree lifecycle (lazy per-repo init gated on the `:evogit_worktree_repos` ETS marker; monitor-driven reclaim on `:DOWN`; restart re-monitoring). Per-repo init (`maybe_init_repo/3`, required `primary?` flag): the `.genesis/workers` wipe + orphaned `evogit-agent-*` branch cleanup run ONLY for the primary repo — foreign-repo branches are preserved. Detail: `agent_scheduler/CONTEXT.md` |
| `EvoGit.AgentScheduler.Worktrees` | Pure helpers: `workers_dir/1`, `assign_and_prepare_worktree/2`, `run_init_script/3`, `prepare_new_worktree/5`, `branch_name/2` (`"evogit-agent-T#{task}-A#{agent}"`). Fresh worktree per run (no pool, no reuse across retries). Detail: `agent_scheduler/CONTEXT.md` |
| `EvoGit.Adapters.Git` / `GitHub` / `CowWorktree` | Git CLI thin wrapper (uniform `{:ok, v} \| {:error, {tag, output}}` contract); gh-CLI wrapper (upstream detection + issue listing/markdown, node-aware); CoW worktree creation with graceful `{:fallback, reason}`. Detail: `adapters/CONTEXT.md` |
| `EvoGit.GitEnv` | Single source of truth for the env map injected into EVERY git invocation (`LC_ALL=C`, `GIT_EDITOR`, commit-identity resolution + `:persistent_term` memoization). See "GitEnv" section. Detail: `adapters/CONTEXT.md` |
| `EvoGit.Sandbox` / `SandboxSlice` / `SandboxProcessRegistry` | Multi-platform sandbox dispatch (`[sandbox] backend` = `:auto`/`:systemd`/`:bwrap`); systemd user-slice resource limits; caller-process monitor → orphaned `systemd-run` unit cleanup. Linux-only slice/registry, gated on the systemd backend being the actually-selected backend. Detail: `sandbox/CONTEXT.md` |
| `EvoGit.Task` | Legacy orchestration: `mutate/3`, `diagnose/3`, `resolve_conflict/3` (no lib/dashboard callers) |
| `EvoGit.Runtime` / `Runtime.Helpers` | Top-level coordinator (Genesis/Evolution); shared helpers — `merge_and_report/3,4` (per-repo `repos` report; 4-arity takes the foreign-repos list), `resolve_foreign_repo_starting_commit/2`, `notify_finalizing`, `generate_branch_name` (`genesis/agent_<hex>`), `resolve_starting_commit`, `load_foreign_repos` (up-front existence/git-ness/base_sha validation → descriptive `ArgumentError`), `load_repo_notes` (git-submodule awareness note), `resolve_root_agent/2`. Detail: `runtime/CONTEXT.md` |
| `EvoGit.Runtime.SelfReflective` | Repo-less runtime (`source_root/0` shared env chain, `build_spec/2` test seam) — the `:reflect` task entry point. See Self-Reflective section |
| `EvoGit.SelfReflectiveSource` | Manages the shallow managed clone at `<data_dir>/genesis-source`; owns the shared `reference_path/0` chain (`:self_reflective_source_root` app env → `GENESIS_SOURCE_ROOT` → valid managed dir → nil) and the cheap public `available?/0` — pure local-read check (no git subprocess/network, never raises) that resolves `reference_path() \|\| File.cwd!()` and validates it is a Genesis checkout (dir + `CONTEXT.md`), so the dashboard can prompt for a source download on a fresh install. LOCAL-NODE-ONLY (local BEAM data dir + process env) |
| `EvoGit.ProjectConfig` | Reads/parses `genesis.toml` from repo root (`[foreign_repos.<id>]` supports `writable = true|false` + `base_sha = "..."`) |
| `EvoGit.Review` | Review operations: diff-pipeline API (`load_review_data`, `load_review_metadata`, `load_file_diff`, `get_file_content`, ...), `FileInfo`/`CommitInfo` structs with `language` (highlight.js names), merge/reject (`merge_branch/2,3` — per-repo-path usable; multi-repo tasks carry one `evogit-agent-*` branch per writable foreign repo under the task's single branch name), `check_merge/3` (non-mutating `git merge-tree` dry-run). Detail: `lib/evo_git/CONTEXT.md` (Review/RemoteNode Return-Shape Contract) + the Multi-Repo Read-Write section |
| `EvoGit.Config` / `Defaults` | Unified 3-level resolver (defaults → user TOML → runtime overrides); `Defaults` is a backward-compatibility shim. Detail: `config/CONTEXT.md` |
| `EvoGit.Platform` | Cross-platform OS detection, config/data-dir resolution, `systemd_available?/0`, `bwrap_available?/0`, `sandbox_backend/0` capability chain `:systemd_run → :bwrap → :sandbox_exec → :none`, `cpu_threads/0` (single source of truth for CPU-thread-count-derived config defaults: the scheduler's `max_tool_concurrency` and both sandbox `cpu_quota` strings — detail: `lib/evo_git/config/CONTEXT.md`) |
| `EvoGit.Nix` | `nix print-dev-env` integration — builds the dev env once, caches the script to `<data_dir>/nix-dev-env.sh`, sources it per tool call via `bash -c`; `active?/0` disables on build failure |
| `EvoGit.RemoteConnections` | Pure TOML store for SSH targets (`~/.config/genesis/remote_connections.toml`): `ssh_target` (required), `local_binary_path`, `platform`, `dist_port` (default 9000), `remote_path`, `name`, `id`, `last_connected`; old `host`/`user`/`port`/`identity_file` fields auto-migrated to `ssh_target` on load |
| `EvoGit.RemoteBootstrap` | Pure platform/asset/download logic (no I/O): `parse_uname/2`, `parse_platform/1`, `daemon_os/1`, `asset_name/1` (always `genesis_remote_<platform>.tar.xz`, never suffixed), `download_url/1` (deterministic Cloudflare "smart download" URL), `cache_path/2`, `bash_wrap/1`, `nixos_patch_script/1`. Detail: `lib/evo_git/CONTEXT.md` |
| `EvoGit.RemoteConnection` | GenServer: bootstrap (local tarball scp OR auto-download; extract → `systemd-run --user`/launchd daemon launch; NixOS patchelf patching) + connect lifecycle (async event-driven: prompt `connect/1` reply, monitored-worker ssh tunnel + EpmdDist registration + `Node.connect`, full `:connecting`/`:connected`/`:error`/`:disconnected` status broadcasts), 10s heartbeat, status broadcasts on `"remote_connections"`. Per-target node names (`genesis_remote_<id>@127.0.0.1`) enable multiple simultaneous connections. Detail: `lib/evo_git/CONTEXT.md` |
| `EvoGit.EpmdDist` | Custom EPMD-less distribution module implementing the `erl_epmd` interface; `:persistent_term` node→port registry; `register_target(node_string, port)` called after SSH tunnel setup, before `Node.connect`. Referenced as `-epmd_module Elixir.EvoGit.EpmdDist` in all `rel/vm.args.eex` |
| `EvoGit.Distribution` | Runtime distribution: `maybe_enable/0` (config `[node] enabled`), `enable_for_remote/1` (on-demand `:net_kernel` start with longnames `genesis@127.0.0.1`, EPMD-less local port range 9100-9200, cookie from config — auto-generated on first bootstrap), `enable_for_connection/1` (sets `Application.put_env(:kernel, :epmd_module, ...)` before `:net_kernel.start/1`) |
| `EvoGit.Store` / `Store.Codec` | SQLite-backed task/project persistence (WAL, JSON codec, `@call_timeout` 30s for slow NFS, disk-full handling, summary projections, `updated_at` bookkeeping). Detail: `store/CONTEXT.md` |
| `EvoGit.TaskInfo` / `RecentProject` | Persisted structs. Gotcha: `opts` round-trips through the Codec JSON — `:foreign_repos` come back STRING-keyed maps, never `%ForeignRepo{}`; normalize via `EvoGit.Core.ForeignRepo.normalize/1` before dot-access (done centrally by `MergeContext.apply_merge_context/4` + `Runtime.Helpers.merge_foreign_repos/2`). The per-repo result `repos` map likewise round-trips STRING-keyed (`"repos" => %{repo_id => %{"commit_sha", "branch_name"}}`) — `Codec.decode_result` deliberately does NOT atomize `"repos"` |
| `EvoGit.TaskRegistry` | GenServer: task lifecycle, lease/heartbeat, cleanup, status tracking, PubSub broadcasts, graceful cancel + force kill, startup reconciliation. Detail: `task_registry/CONTEXT.md` |
| `EvoGit.RemoteNode` | Cross-node RPC helper wrapping `:erpc.call/5` (timeout `:remote_rpc_timeout`, default 30s, read at CALL time); node-first wrappers for agent/config/task/review/custom-agents/recent-projects/GitHub RPC; local → direct call, remote → `:erpc`. Detail: `lib/evo_git/CONTEXT.md` |
| `EvoGit.AgentScheduler.RemoteAPI` | Read-only RPC API over scheduler ETS (list_agents, get_agent_history, get_config, task-history + review delegates, custom-agents RPC, system samples) — returns native BEAM terms (`:erpc` transfers them without serialization) |
| `EvoGit.SystemSampler` | 3s system samples broadcast on PubSub `"system"` (EXACT 13 keys incl. per-model `llm_slots`); 60-sample ring buffer; runs on every node incl. `genesis_remote`. Detail: `lib/evo_git/CONTEXT.md` |
| `EvoGit.PeakHours` / `PeakHourEngine` | Pure peak-window math (`parse_time/1`, `parse_window/1`, `validate_windows/1`, `validate_days/1`, `in_peak?/2,3`, `next_transition/2,3`, `effective_concurrency/2,3`, `validate_timezone/1,2`, `wall_clock_in/2`) + the GenServer that flips scheduler concurrency on window transitions. See Peak/Off-Peak section. Detail: `agent_scheduler/CONTEXT.md` + `config/CONTEXT.md` |
| `EvoGit.ReqLLMPool` | Grow-only runtime reconciliation of ReqLLM's Finch pool (excess-queuing bumps + config-change reconciliation). Detail: `lib/evo_git/CONTEXT.md` (root `./CONTEXT.md` also covers the pool sizing formula) |
| `EvoGit.Powershell` | Pure Windows PowerShell invocation helpers (`-EncodedCommand`, BOM-aware output decode). Detail: `lib/evo_git/CONTEXT.md` |

### Remote Bootstrap Asset Format
The SSH remote-bootstrap asset is **`.tar.xz`**: `genesis_remote_<platform>.tar.xz` (never suffixed — glibc is the default and ONLY published Linux variant; the musl build is disabled). Uploaded per platform by `.github/workflows/build-desktop.yml` (linux_x64 / linux_arm64 / darwin_arm64 / windows_x64). Extracted with `tar -xJf` — no gz fallback (xz failure fails loudly). On NixOS hosts the glibc-only asset runs via on-the-fly patchelf patching during bootstrap. Detail: `lib/evo_git/CONTEXT.md` + `.github/workflows/CONTEXT.md`.

### SSH Tunnel Readiness (connect-path invariant)
`RemoteConnection`'s async connect worker polls the local forwarded port (`wait_for_tunnel/4`, 100ms interval, 10s budget) BEFORE `Node.connect/1`; no remote-daemon log entry for a connect attempt ⇒ tunnel/port race; "Invalid challenge reply" in the daemon journal ⇒ cookie mismatch. `connect/1` is NON-BLOCKING — it replies `{:ok, :connecting}`/`{:ok, :connected}` promptly and runs the ssh/Node.connect I/O in a monitored worker; terminal outcome arrives via `{:remote_connection_status, target_id, status_map}` broadcasts (`:connecting` start / `:connected` / `:error` with `last_error` / `:disconnected` on cancel). Full detail: `lib/evo_git/CONTEXT.md`.

## PubSub Event Contract (event-driven dashboard)

All dashboard-visible state changes are PUSHED on `EvoGit.PubSub` (a Phoenix.PubSub PG2 cluster-aware instance — broadcasts on a remote daemon node reach local subscribers; `EvoGit.RemoteNode` is RPC-only and needs no pubsub changes). Every message carries the emitting BEAM `node` as its final element. **Node-identity filtering rule**: subscribers MUST ignore messages whose node ≠ their own `node()` when the payload is node-local state (task/agent ids are per-node; TaskRegistry's `{:task_updated, id, :finalizing, node}` handler is guarded `when node == node()`). Emitter-side detail: `lib/evo_git/task_registry/CONTEXT.md`.

| Topic | Message | Emitted by | Notes |
|---|---|---|---|
| `"tasks"` | `{:task_updated, task_id, status, node}` | TaskRegistry mutations; `Runtime.Helpers.notify_finalizing/1` | status = the task's CURRENT status after the mutation (`:pending \| :running \| :finalizing \| :cancelling \| :completed \| :failed \| :cancelled`), `nil` for review-status/metadata updates. No broadcast on stale/no-op/missing-row updates; heartbeat stays silent (event-storm avoidance). **`notify_finalizing/1` is the ONE broadcast fired BEFORE its persist** (wrapper→TaskRegistry-handler persist; disk-full → row stays old status) — every other broadcast is AFTER the store write; detail: `lib/evo_git/task_registry/CONTEXT.md` (PubSub Broadcast Contract) |
| `"tasks"` | `{:task_deleted, task_id, node}` | `clear_finished_tasks` (one per deleted id), `delete_task` | **`Cleanup.cleanup_expired_tasks/1` deletes rows with NO broadcast** (silent — only `clear_finished_tasks`/`delete_task` emit) |
| `"agents"` | `{:agent_registered, agent_id, summary, node}` · `{:agent_updated, agent_id, changed_fields, node}` · `{:agent_removed, agent_id, node}` · `{:agents_updated, node}` | `EvoGit.AgentScheduler.PubSub` helpers (Throttle flush or immediate fallback when the Throttle is down) | `:agent_updated` deltas strip the heavy `:context` field; `message_count` (integer) is prepended so consumers detect LLM-history changes without re-transferring `get_agent_history/1` |
| `"scheduler_config"` | `{:scheduler_config_updated, node}` | `AgentScheduler.broadcast_config_updated/0` (agent_scheduler.ex:840,856; state.ex:408) | |
| `"system"` | `{:system_sample, node, seq, sample}` | `EvoGit.SystemSampler` every 3s | sample keys EXACTLY: `llm_used, llm_waiting, tool_used, tool_waiting, llm_capacity, tool_capacity, agents_total, agents_running, agents_blocked, agents_waiting, agents_pending, scheduler_alive, llm_slots`; `llm_slots = %{model_id => %{used, waiting, capacity}}` reports REAL per-model LLM slot occupancy (live holders/queue/effective capacity via `AgentScheduler.get_llm_slot_status/0`, fetched per tick; `%{}` when the scheduler is down) while the aggregated `llm_used`/`llm_waiting` keys remain running/blocked proxies; seq = monotonic per sampler |
| `"guides"` | `{:guide_updated, guide_id, %{message: String.t(), page: String.t() \| nil, selector: String.t() \| nil, dismissible: boolean}, node()}` | `EvoGit.Agent.Tools.GuideUser` (the `GuideUser.guide_user` command handler, invoked via the self-reflective agent's `run_command` shell tool) | Transient user-guide push to the dashboard; best-effort (PubSub down swallowed, tool still returns its confirmation) |

The payload-less `{:tasks_updated}` / `{:task_status, task_id, status}` shapes do NOT exist — all task/agent/config/system messages carry the trailing node element.

## Custom Agents & Model Selection

User-defined agents live in ONE file: `<config_dir>/agents.toml` (per-node semantics — the file on the node executing the agent). Optional `[model_selection] script` + `[[agents]]` array-of-tables:

```toml
[model_selection]
script = """if agent.custom_agent_id == "code_reviewer", do: "deepseek-reasoner", else: nil"""

[[agents]]
name = "Code Reviewer"          # required
description = "Reviews code"
prompt = "You are..."           # required — persona/behavior rules
agent_type = "read"             # "read" | "read_write" (default "read_write")
delegation_level = "low"        # "high" | "low" (default "low")
model_id = "gpt-5"              # optional default model PROFILE id (nil = auto/default)
max_turns = 40                  # optional per-agent turn cap override
tools = ["read_file", "run_bash", "write_file"]  # optional whitelist; absent = all standard tools
subagents = ["executor", "investigator"]          # optional built-in agent types this agent may spawn
```

- **Definition map** (atom keys): `%{id, name, description, prompt, agent_type: :read | :read_write, delegation_level: :high | :low, model_id, max_turns, tools: [String.t()] | nil, subagents: [String.t()]}`; defaults applied on save: `agent_type :read_write`, `delegation_level :low`, `subagents []` (the `EvoGit.Agent` macro default is `:high`; the agents.toml schema default is `:low`). `tools`/`subagents` entries are STRINGS (atom-only saves rejected with `:invalid_tools`/`:invalid_subagents`). Save validation errors: `:missing_name, :missing_prompt, :invalid_agent_type, :invalid_delegation_level, :invalid_max_turns, :invalid_tools, :invalid_subagents, :duplicate_id`. Reads are lenient (never raise); only `save/1` validates.
- **Model-selection script contract** (`ModelSelector.describe_contract/0`): body wrapped as `fn agent -> <body> end` — `agent = %{agent_type, custom_agent_id, depth, parent_id, task_id, objective}`; LAST expression = model profile id `String.t() | nil` (`""`/`false` also mean default). Script errors never crash the scheduler — compile errors/raises captured, surface as `ModelSelector.status/0` → `{:error, {:compile_error, msg}}`; a raising script logs a warning + falls back to default at dispatch.
- **Dispatch priority chain** (`Dispatch.register_agent/7`, before `resolve_model_for_agent/2`): (1) `spec.opts[:model_id_locked]` — user-explicit choice wins, script skipped; (2) model-selection script — evaluated for EVERY spawn (root AND subagents) unless locked; (3) `spec.model_id` from agents.toml or inherited; (4) default profile via `resolve_model_for_agent/2` (logs + falls back for unknown ids). Subagent `model_id` inheritance does NOT set the lock. Custom `max_turns` overrides `state.max_turns_root`/`max_turns` for that spawn.
- **Runtime opts threading**: task opts `:agent` (validated by `Runtime.Helpers.resolve_root_agent/2` — raises `ArgumentError` on unknown id) + `:model_id_locked` (task-level model selection: CLI `-m` resolved pre-enqueue to a configured profile id via `resolve_model_id/2`, or dashboard explicit model choices). Root spec: `agent_module: EvoGit.Agents.Custom` + `opts: [custom_agent_id: id]`; the Runner puts `:custom_agent_id` into the process dictionary (`setup_dispatch_context/1`) — the zero-arity-callback bridge. Custom agents are ROOT agents only (`subagent_tool_name/0` nil).

Detail: `custom_agents/CONTEXT.md`.

## Custom Task Mode (evolve)

The `:evolve` task type supports a `"custom"` mode opt (task type stays `:evolve`) which runs a user-defined agents.toml agent as the ROOT agent instead of the built-in `EvoGit.Agents.Manager`. Semantics are otherwise identical to simple mode (same spec-building + `merge_and_report(repo_path, agent_output, "evolve")` → reviewable `genesis/agent_*` branch).

- **Mode value**: string `"custom"` (dashboard AND CLI — task-opts modes are ALWAYS strings from both entry paths; never atoms, `RuntimeOpts` raises on atoms); `"simple"`/`:simple`/nil/absent → simple; ANY other value → `Logger.warning` + simple fallback. Normalization lives in `EvoGit.Runtime.Evolution.mode_atom/1` (`@doc false`, directly testable).
- **Required `agent:` opt**: custom mode raises `ArgumentError` when `:agent` is nil/empty — at the top of `Evolution.run/2` BEFORE any repo/git I/O; unknown ids raise via `Helpers.resolve_root_agent/2` (default `EvoGit.Agents.Custom`, after repo validation).
- **Root resolution**: `resolve_root_agent(opts, EvoGit.Agents.Custom)` → `{EvoGit.Agents.Custom, [custom_agent_id: id]}`; the simple and custom paths share ONE private helper (`run_resolved_root_agent/7`) — no duplication.
- **Entry paths**: CLI `evogit evolve --mode custom --agent <id>` (`--mode custom` WITHOUT `--agent` prints a clear error and does not spawn; genesis `--mode custom` prints a dedicated "custom mode is evolve-only" error); dashboard sends `mode: "custom"` → `RuntimeOpts.mode_atom(:evolve, "custom")` → `:custom` (`genesis_mode_atom("custom")` raises a specific ArgumentError); defense-in-depth `EvoGit.Runtime.Genesis.resolve_mode/2` raises for `:custom`/`"custom"`.

Detail: `runtime/CONTEXT.md` + `task_registry/CONTEXT.md` (RuntimeOpts/TaskExecutor).

## Multi-Modal Objective Attachments (`:attachments` opt)

Initial objectives may carry image/audio data so the ROOT agent's first LLM user message is real multimodal req_llm content. The opt-contract single source of truth is `EvoGit.Attachments` (`lib/evo_git/attachments.ex`).

- **Opt shape**: task opt `:attachments` = a list of STRING-keyed maps `%{"type" => "image"|"audio", "name" => <basename>, "media_type" => <IANA type e.g. "image/png"/"audio/mpeg">, "data" => <base64-encoded file bytes (ASCII string)>}`. Base64 is MANDATORY in the opts layer — Store.Codec Jason-encodes whole opts and raw non-UTF-8 binaries abort Jason (the encode fallback would silently drop the key); raw bytes are decoded ONLY at LLM-message materialization (`Attachments.to_content_parts/2`: `"image"` → `ReqLLM.ContentPart.image(raw, media_type)`, `"audio"` rides as a `:file` part `ContentPart.file(raw, name, media_type)` — req_llm has no audio part type). The text objective stays a plain `String.t()` end-to-end (leading `ContentPart.text`), never made non-string.
- **Caps (core-enforced, authoritative raise)**: at most 4 attachments per task, each ≤ 15 MiB RAW bytes — `EvoGit.Attachments` module attributes are the single core source. Malformed payloads fail task start with a descriptive `ArgumentError` via `RuntimeOpts` → `Attachments.validate/1` (spec-error style).
- **Root-only semantics**: media parts ride the ROOT agent's FIRST user message only (`ContextBuilder.build_initial_messages/4`, gated on `parent_id == nil` AND a non-empty list; subagents never inherit). Genesis Mode A root, Genesis Mode B BOTH sequential roots (Architect + phase-2 Manager — the shared runtime opts are forwarded verbatim into the phase spec), and the Evolve root all receive them. Reflect never receives the key (no attachment UI; a stray key degrades to the plain-text path, never a crash).
- **No model-capability check** (no llm_db modality gating, no codec-capability gating — per product decision): if the chosen provider cannot handle the attached modality, the provider erroring is the user's concern.
- **Known limitation**: resume / merge-auto-resolve / GitHub-fix follow-up tasks do NOT re-carry `:attachments` (they build fresh text objectives; the user can re-attach). `ResumeContext`/`MergeContext` never inherit the previous task's key.

Detail: `lib/evo_git/agent/CONTEXT.md` (first-message materialization + compression cost) + `lib/evo_git/task_registry/CONTEXT.md` (RuntimeOpts validation chain) + `lib/evo_git/store/CONTEXT.md` (Codec known-opt-key atomization) + `lib/evo_git/runtime/CONTEXT.md` (Mode B forwarding).

## Multi-Repo Read-Write Foreign Repos

Foreign repos may be **writable** — a task still has exactly ONE primary repo and one final report; foreign repos are additional working repos whose agent modifications are tracked but NEVER merged back by the task itself (merge/reject happens later via the dashboard review page).

- **Data model** (`EvoGit.Core.ForeignRepo`): new fields `writable: boolean()` (default `false` — existing read-only semantics preserved) + `base_sha: String.t() | nil` (per-repo starting commit; `nil` = foreign repo HEAD, same semantics as the primary's starting commit). `new/*` and `normalize/1` are safe for STRING-keyed Codec round-trip maps (missing keys default correctly). `ProjectConfig` (`genesis.toml` `[foreign_repos.<id>]`) accepts `writable = true|false` and `base_sha = "..."`.
- **CLI**: `-R <id>:<path>` stays READ-ONLY by default; marking writable/base_sha is TOML-only (`genesis.toml`). Windows drive-letter paths (`-R C:\path`) are NOT split on `:` (`CLI.Parser.split_foreign_repo_spec`).
- **Spawn gate** (`Subagents.validate_spatial_contract_for_spec/3,4` — the 4-arity adds `parent_depth`; the 3-arity delegates with root depth 0 for backward compat): read-only cross-repo spawns into ANY foreign repo are **UNRESTRICTED at any depth**. `:read_write` cross-repo spawns require the target's task-level entry to have `writable: true` (else `{:error, {:foreign_repo_read_only, msg}}`) AND are restricted two ways: **root-agent-only** — the parent must be depth 0, else `{:error, {:foreign_repo_write_not_root, msg}}` (teaches: only the ROOT agent may spawn write-capable subagents in a foreign repository; a nested agent's write scope is its assigned node in its own repo; alternatives: spawn read-only agents into the foreign repo, or report the needed change back up to the root which spawns the writable subagent one at a time); **one-at-a-time** — at most ONE writable-foreign-repo spec per spawn batch (`spawn_validated_subagents/5` threads a `writable_accepted` flag through its reduce; the 2nd+ accepted writable foreign spec fails with `{:error, {:foreign_repo_write_serialized, msg}}`; one-per-batch == one-at-a-time because the parent blocks until all batch subagents complete). **Same-repo spawns WITHIN a foreign repo (parent already running inside it, `spec.repo_id == parent repo_id`) are UNRESTRICTED** — that is how the foreign-repo Manager does its own internal parallelism; only cross-repo `:read_write` spawns are gated. `format_subagent_result/1` (`agent/subagent_processing.ex`) surfaces all three rejection shapes verbatim as `"Error: #{msg}"`. The role is resolved by id-matching `spec.foreign_repos` (threaded through AgentSpec/LoopState; the Runner puts `Process.get(:foreign_repos)`), NEVER from the path alone — the same path can be writable for one task and read-only for another; unknown ids default to read-only.
- **Tool-layer write gating**: `Tools.execute/5` chains `maybe_block_read_only_foreign_repo/5` after `maybe_block_repo_less/5` — blocks every `@write_tools` entry (now incl. `run_git` + `curl`) when the agent's repo id resolves to a read-only foreign repo (message family "read-only foreign repository"); `Shared.validate_file_scope/3` additionally rejects write targets inside read-only foreign repos (defense-in-depth). `Tools.read_only_schemas/0` NO LONGER includes `Curl` (read tools + Context write/edit + ShellTool remain). Repo-less write guard unchanged.
- **Per-repo runtime roots + report** (`Runtime.Helpers.merge_and_report/3,4`; 3-arity delegates with `foreign_repos: []`): writable foreign repos get worktree roots like the primary; **ONE branch name per task** is reused across the primary + every writable foreign repo (`Git.create_branch(repo.root, branch_name, sha)` — NEVER merged into the foreign default branch; branch failure logs + keeps the entry with `branch_name: nil`). The report gains a top-level **`repos` map: `%{repo_id => %{commit_sha, branch_name}}`** with STRING keys (JSON/Codec-safe): `"primary"` always present (`branch_name` `nil` when no changes), each writable foreign repo that produced a commit present, read-only repos ABSENT. Top-level `commit_sha`/`branch_name`/`result` stay the PRIMARY's (backward compat for TaskInfo/dashboard). The per-repo data source is `Result.foreign_repo_commits` (`%{repo_id => latest_commit_sha}`, default `%{}`), rolled up by the scheduler (`Lifecycle.inject_foreign_repo_commits/2` + `store_sub_result/3` child-wins merge; `AgentScheduler.get_foreign_repo_commits/1` returns `%{}` on missing sched_meta) and read defensively via `Map.get`. Survives the Store/Codec round trip — `Codec.decode_result` deliberately does NOT atomize `"repos"`.
- **Review APIs (dashboard workstream contract)**: each writable foreign repo carries an `evogit-agent-*` branch under the task's SINGLE branch name, pointed at `repos[repo_id].commit_sha`. `EvoGit.Review.merge_branch/2,3` and `reject_branch` are per-repo-path APIs (unchanged signatures) — the dashboard merges by calling `merge_branch` for EVERY repo path (primary + each writable foreign repo) and rejects per path likewise. The task never merges; the review page does.
- **WorktreeManager scoping**: `maybe_init_repo/3` (REQUIRED `primary?` param) — the `.genesis/workers` rm_rf wipe and `clean_orphaned_branches` (deletes every `evogit-agent-*` branch) run ONLY for the primary repo; foreign repos skip both (their `evogit-agent-*` branches carry real task work). `run_init_script` stays primary-only.
- **Graceful foreign path validation**: `Runtime.Helpers.load_foreign_repos/2` validates every entry up front — root must exist AND be a git repo (`Git.rev_parse` succeeds) and a non-nil `base_sha` must resolve — raising a descriptive `ArgumentError` (id/path/problem) at task start, never a mid-run crash. `SubagentProcessing` returns `{:error, {call, 0, "Error: foreign repository path does not exist or is not a git repository: <path>"}}` instead of a MatchError. `resolve_foreign_repo_starting_commit/2` = `base_sha` (non-nil) → that sha, else foreign HEAD. Subagent phylo-node precedence: task-level `base_sha` → tracked `foreign_repo_commits` → foreign HEAD.
- **MergeContext/ResumeContext**: `apply_merge_context/4` carries the full `:foreign_repos` list (writable/base_sha intact) and applies per-repo starting commits — each carried repo's `base_sha` is overridden from the previous task's `repos[repo_id]["commit_sha"]` (repos absent from the prev result keep their base_sha; a legacy prev task without `repos` never crashes). `apply_resume_context/3` no longer drops `:foreign_repos` (caller maps normalized to `%ForeignRepo{}` structs before `RuntimeOpts.build_common_runtime_opts`; per-repo base_sha overridden in BOTH branches). Shared pure helper: `EvoGit.TaskRegistry.PrevTaskRepos` (`prev_repos_map/1`, `repo_commit_sha/2`, `apply_starting_commits/2`, `writable_repos_line/1`).
- **Prompts**: agent prompts teach the writable-foreign-repo delegation model as an **enforced gate** — read-only foreign-repo spawns unrestricted for everyone; write-capable (`:read_write`) foreign-repo spawns are **ROOT-AGENT-ONLY (depth 0) + ONE-AT-A-TIME** (rationale: agents edit only their own path; parallel foreign-repo writes create merge conflicts the spawning agent cannot resolve because the sandbox restricts write access to the agent's LOCAL path; the Manager running INSIDE the foreign repo owns its own parallelism). Static prompts (manager/executor/task_scheduler/genesis_planner/architect/context_extractor + shared `PromptFragments.writable_foreign_repo_clause/0`) are **role-agnostic** — static text cannot tell a recursively-spawned agent its own root status, so they tell NESTED agents needing foreign-repo changes to report the need back up to their **parent agent (the higher level in the delegation chain)**, which will handle it, and end with a pointer: "Your first-user context states whether you are the ROOT or a NESTED agent of this task and your exact foreign-repo authority." **Each agent's own role is stated authoritatively in its FIRST-USER context**: `Runner.do_run/2` adds a "Delegation authority" bullet to the `<context>` body via the pure `EvoGit.Agent.ContextBuilder.build_authority_section/1` (root signal = `AgentState.parent_id == nil`, exactly equivalent to SchedMeta depth 0) — ROOT agents (no parent) MAY spawn write-capable subagents into writable foreign repos, one at a time; NESTED agents (non-nil parent) may NOT, and report the need to their parent; the section is omitted (`""`) for repo-less agents and for tasks with no non-primary foreign repos. Changes committed to `evogit-agent-*` branches, tracked by the task (per-repo commit + branch in the final report), NEVER merged back by the task. Enforcement (the spawn gate in `agent_scheduler/subagents.ex`) is unchanged.

Detail: `runtime/CONTEXT.md` + `task_registry/CONTEXT.md` (PrevTaskRepos/MergeContext/ResumeContext) + `agent/CONTEXT.md` + `agent_scheduler/CONTEXT.md` (spawn gate, roll-up, WorktreeManager scoping) + `core/CONTEXT.md` (ForeignRepo).

## Self-Reflective Agent (repo-less)

A special **repo-less** agent (chatbot-like, conversational) with no git repository or worktree of its own: it reads the Genesis source/docs **read-only**, controls the task system on the user's behalf, and shows transient user guides in the web dashboard.

- **Task type**: `:reflect` — `TaskRegistry.TaskExecutor.execute_task(:reflect, opts, task_id)` runs `EvoGit.Runtime.SelfReflective.run(opts ++ [task_id: task_id])`; codec atom `:reflect`; task mode string `"reflect"`. Opts: `objective` (String), optional `model_id`/`source_root`/`task_id` — **NO `:path`** (the executor deliberately does NOT call `RuntimeOpts.build_common_runtime_opts`, which `Keyword.fetch!`s `:path` → KeyError on repo-less tasks). Result: `{:ok, %{result: <summary_string>, commit_sha: nil, branch_name: nil, tag: nil}}`.
- **Repo-less plumbing**: `spec.opts[:repo_less] = true`, `phylo_node` nil. `Dispatch.resolve_agent_repo_root/2` short-circuits to `resolve_repo_less_root/0` — shared chain `EvoGit.SelfReflectiveSource.reference_path/0` (`:self_reflective_source_root` app env → `GENESIS_SOURCE_ROOT` → valid managed clone at `<data_dir>/genesis-source`) → `File.cwd!()` → `"[system]"`. No worktree is ever created: the Runner skips `create_worktree_for_agent`, sets `Process.put(:repo_less, true)`; `commit_pending_in_worktree/0` no-ops; git/dirty checks skipped; phylo nil-safe everywhere. **Write guard**: `Tools.execute/5` blocks `@write_tools` when `Process.get(:repo_less)` ("read-only access to the system"); `available_tools/0` omits write tools.
- **Modules**: `EvoGit.Agents.SelfReflective` (`agent_type :read`, `delegation_level :low`, `subagent_modules []`; tools = read tools (FileRead, Ripgrep, Glob, ListDirectory, `Context.read_schema`, SearchContext, SearchHistory) + `WebSearch` (gated on `EvoGit.Config.tools_search_enabled?/0`) + `RunCommand.schema()` + `CompleteTask.schema()`); `EvoGit.Runtime.SelfReflective` (`source_root/0`, `run/1,2`, `build_spec/2` seam); `EvoGit.SelfReflectiveSource` (managed shallow clone; `status/0` pure local read; `clone/0`/`update/0` guards).
- **One `run_command` shell tool**: task control, user guides, and system inspection are exposed through the single `run_command` tool (`EvoGit.Agent.Tools.RunCommand`), backed by the declarative `EvoGit.CommandShell` registry (`apps/evo_git/lib/evo_git/command_shell.ex`) — 10 commands: `ListTasks.list_tasks`/`GetTask.get_task`/`StartTask.start_task`/`CancelTask.cancel_task`/`ForceKillTask.force_kill_task`/`DeleteTask.delete_task`/`SpawnInvestigator.spawn_investigator`, `GuideUser.guide_user`, `ListRecentProjects.list_recent_projects`, `SystemInfo.system_info` + built-in `help`. The 10 former per-function tool modules are now COMMAND HANDLERS invoked by the shell (no per-tool schema). `run_command` is dispatch-registered ONLY in `tools.ex` — never in `Tools.schemas/0` or `read_only_schemas/0` (giving every coding agent task-control access would be a scope/security violation). `SpawnInvestigator.spawn_investigator` runs a **deterministic read-only repo probe** (`EvoGit.Agent.Tools.SpawnInvestigatorProbe`): it validates `path`/`objective`, then returns a genuine bounded investigation report (repo/worktree state incl. `gitdir:` resolution, CONTEXT.md chain excerpts, top-level inventory, objective-keyword hits with `rel:line`) with ZERO side effects on the target repo (no `.genesis`, no worktree, no branch, no LLM) — preserving its `level: 1` no-approval safety. It does NOT spawn a full `EvoGit.Agents.Investigator` LLM-agent run — residual blockers + future design path: `agent/tools/CONTEXT.md`. Security: compile-time literal registry (no `Code.eval_string`, no input-derived names), **no `String.to_atom` on input** (enum/bool/statuses validated against literal lists, passed as strings), whitelist-only dispatch, length guardrails (`@max_command_length` 4000 / `@max_tokens` 40 / `@max_token_length` 2000). **Security levels & approval gate**: each registry entry carries `level:` — L1 read-only commands (`ListTasks`/`GetTask`/`ListRecentProjects`/`SystemInfo`/`SpawnInvestigator` + builtin `help`) execute immediately; L2 `GuideUser.guide_user` and L3 side-effect commands (`StartTask`/`CancelTask`/`ForceKillTask`/`DeleteTask`) route through `EvoGit.CommandApproval.request/5` at the dispatch choke point and run ONLY after the user approves in the /help chat (bounded window, default 120s via app env `[:evo_git, :command_approval_timeout]`; denied/timed-out fail closed and the handler never runs; task-lifecycle auto-deny on `:cancelling`/terminal `{:task_updated, ...}`; approvals broadcast on PubSub topic `"approvals"`). Docs: `agent/tools/CONTEXT.md`.
- **`"guides"` topic contract**: `GuideUser` broadcasts `{:guide_updated, guide_id, %{message, page, selector, dismissible}, node()}` on `EvoGit.PubSub` topic `"guides"`; best-effort (PubSub down swallowed, confirmation still returned).
- **CLI**: `evogit reflect "<objective>" [-m model]` — objective from argv or `-f`; `-m` resolves task-level against configured profiles pre-enqueue (`model_id_locked: true`); the task is enqueued via `TaskRegistry.start_task/2` (opts `[objective: ...]` + model opts only — NO `:path`, NO `:mode`) and the CLI blocks until a terminal status.
- **Chat readiness (reflect event flow)**: transitions `:running → :completed/:failed/:cancelled` with NO `:finalizing` broadcast (`SelfReflective.run` never calls `notify_finalizing`) — the dashboard must NOT wait for `:finalizing` on reflect tasks. Root agent identifiable by `parent_id: nil` + matching `task_id` (no task→agent lookup RPC — filter `list_agents/0` summaries). Live content is FETCH: watch `message_count` increments / `{:agents_updated, node}` and re-fetch `get_agent_history/1`; **history is wiped on completion** (`recycle_agent` deletes the ETS rows → `get_agent_history/1` returns `[]` after the task ends — a chat page must cache messages incrementally during the run). Persisted terminal artifact = `task.result` string. No `resume_from` for reflect (ResumeContext is evolve-only) — each reflect task is a fresh, stateless agent run.
- **Test coverage**: `reflect_tools_test.exs` (10 command handlers + write guard), `self_reflective_source_test.exs` (`reference_path/0` precedence), `self_reflective_test.exs` (`build_spec` seam, error propagation), repo-less branches in `dispatch_test.exs`/`remote_api_test.exs`/`tool_dispatch_test.exs`, `store_test.exs` (`:reflect` codec), `task_executor_reflect_test.exs` (no-`:path` KeyError regression + end-to-end).

Detail: `agents/CONTEXT.md` + `runtime/CONTEXT.md` + `agent_scheduler/CONTEXT.md` (Repo-Less Plumbing) + `agent/tools/CONTEXT.md`; dashboard Home chat (`EvoDashWeb.HomeLive` at `GET /help`) + global guide overlay: `apps/evo_dash/CONTEXT.md`.

## Task Summary Query Path (key facts)

`select_tasks_summary` / `select_tasks_summary_by_path` / `select_tasks_changed_since` return the **16-key projection** (id, status, review_status, started_at, finished_at, type, project_path, opts, branch_name, model_id, agent_count, base_sha, commit_sha, lease_expires_at, updated_at, error) — **`result` is deliberately NOT selected/decoded** in the summary path (the heavy `Codec.decode_result` — embeds usage + archive_records — never runs here; `updated_at` stays raw ISO). The added `error` key is the light structured failure record: `nil` for non-failed rows, ATOM-keyed `%{kind, source, message, stacktrace}` after decode for `:failed` rows (closed-set atoms only — no archive/usage weight; see task_registry/CONTEXT.md "Failed-task error record"). Optional `statuses` list → `WHERE status IN (...)` (index-backed `idx_tasks_status`); `since` (fixed-precision ISO string) → `WHERE updated_at > ?` (index-backed `idx_tasks_updated_at`); by_path → `idx_tasks_project_path`. **No ORDER BY / LIMIT in any summary query.** Undecodable rows are skipped + logged. Heavy Store/TaskRegistry handlers offload query+decode to short-lived Tasks (`GenServer.reply/2`) so decoded terms never inflate the GenServer heap. Dashboard call sites are all statuses-filtered; the Tasks page is push-driven (no polling). Detail: `store/CONTEXT.md` + `task_registry/CONTEXT.md`.

## LLM Concurrency & Slot Gating

Per-model slot pools: `State.concurrency_for/2` (agent_scheduler/state.ex:206-208) returns `Map.get(state.model_concurrency, model_id, state.default_llm_max_concurrency)`; both enforcers read it (`Slots.do_handle_request_llm_slot/4`, `Slots.grant_llm_from_queue/2`). `AgentScheduler.with_llm_slot/2` BLOCKS in an `:infinity` `GenServer.call` until a slot is granted — slot requests ENQUEUE in the per-model waiting queue (agent status `:blocked`); there is NO `{:error, :no_capacity}` reply. `with_llm_slot` RAISES only on the `{:error, :cancelled}` purge reply (force-kill / graceful-cancel purge). `default_llm_max_concurrency` acts as a FLOOR (`apply_default_llm_concurrency_override/2` never lowers explicit profile concurrencies; explicit 0 stays 0 — hard pause). Runtime overrides are in-memory only (`AgentScheduler.update_config/1`; no session file; lost on restart) — the CLI makes NO scheduler overrides (concurrency is set in config.toml only); `update_config` is invoked by dashboard saves, `RemoteAPI.reload_config`, and `PeakHourEngine`. The end-of-update `Slots.grant_pending_on_resume/1` sweep grants queued waiters on capacity increase / rejects on decrease. Non-slot-gated `stream_text` calls: `system_check.ex:344` (LLM connectivity self-check) + `runtime/pull_request.ex:153` (PR-title generation) only. Detail: `agent_scheduler/CONTEXT.md` (Slot Management) + `agent/CONTEXT.md` (LLM retry loop).

## Peak/Off-Peak Hour Concurrency Scheduling

Each `[[llm.models]]` profile may declare five optional fields — `peak_concurrency` (concurrency used during peak windows), `peak_hours` (list of `%{start: "HH:MM", end: "HH:MM"}` half-open daily windows, each optionally carrying `days` — day identifiers `"mon".."sun"` and/or keywords `"weekdays"`/`"weekends"`, absent = every day; `start > end` = overnight wrap; absent / `[]` / invalid → disabled, normal `concurrency` 24/7), `off_peak_days` (profile-level list of the same day identifiers/keywords — on those days the profile is off-peak the ENTIRE day: normal `concurrency` 24/7, every `peak_hours` window suppressed, `peak_concurrency` incl. the hard-pause 0 never applies; wins over window `days`; e.g. DeepSeek weekends-off-peak: `off_peak_days = ["weekends"]`), and `timezone` (optional IANA name; absent = server local wall clock, never UTC). **`peak_concurrency = 0` = hard pause**: the model gets ZERO LLM slots during peak windows — the explicit 0 is never raised by the `default_llm_max_concurrency` floor. `timezone` requires the tz database at boot: `Calendar.put_time_zone_database(Tz.TimeZoneDatabase)` in `EvoGit.Application.start/2` (`{:tz, "~> 0.28"}` dep). Validation single source of truth: `EvoGit.PeakHours` (pure) — `parse_time/1`, `parse_window/1`, `validate_windows/1` (overlap validation is per-day — windows with disjoint `days` never overlap), `validate_days/1` (day identifiers/keywords, case-insensitive → canonical atoms), `in_peak?/2,3` (day-aware), `next_transition/2,3` (day-aware — midnight day-boundary flips ARE transitions), `effective_concurrency/2,3`, `validate_timezone/1,2` (nil/"" valid; unknown name → ValidationError at `[:llm, :models, <i>, :timezone]`), `wall_clock_in/2` (DST-aware); the config schema delegates to it — do NOT re-implement. `EvoGit.PeakHourEngine` (supervised after the scheduler) computes the **floored** effective map — `max(effective_concurrency, default)` per model, EXCEPT explicit 0 stays 0 (making the apply a fixed point, so its own `{:scheduler_config_updated, node()}` broadcast re-check no-ops) — and applies via `AgentScheduler.update_config(model_concurrency: map)` only when it differs; wake triggers: day-aware `next_transition` timer (+100ms epsilon, 6h safety-net cap, pending timer cancelled before rescheduling) + local `"scheduler_config"` broadcasts — midnight day-boundary flips ARE transitions, and profiles with ONLY `off_peak_days` contribute no transitions (no needless mid-day wakeups). **0-capacity blocks like paused**: slot requests ENQUEUE (blocked) rather than failing fast; waiters are granted on the 0→N transition by the end-of-update `grant_pending_on_resume` sweep; in-flight holders are never evicted; a blocked agent waits until peak exit (by design). Clock seams: `:peak_hours_now_fun` / `:peak_hours_utc_now_fun` app env, read per wakeup. Detail: `agent_scheduler/CONTEXT.md` (Slot Management) + `config/CONTEXT.md`; root `./CONTEXT.md` also covers the TOML contract — do not duplicate.

## GitEnv — Commit Identity Resolution

`EvoGit.GitEnv.git_env/1` is the single source of truth for the env map injected into EVERY git invocation — both `EvoGit.Adapters.Git` and the sandbox backends (`git_env_list/1`). It always sets `LC_ALL=C` + `GIT_EDITOR` (resolved `true` path). Identity priority: (1) **inherited environment** (`System.get_env/1`, non-empty; empty string treated as unset, mirroring git) — never clobbered; (2) **repository's effective `git config user.name`/`user.email`** (no scope flag → repo → global → system, exactly what git uses); (3) **fallback** `"Genesis"`/`"noreply@evogit.ai"` only when nothing is configured anywhere, so automated commits never fail with "Please tell me who you are". Memoized per repo path in `:persistent_term` (`{GitEnv, :config_identity, repo_path}`; the `nil` key = global/no-repo resolution); stale-if-config-changed until VM restart is an accepted trade-off (avoids 2 extra process spawns per git invocation); inherited env vars are read fresh at call time, never memoized. Test helper: `GitEnv.clear_config_identity_cache/1` (`@doc false`). Only bare `git_env/0` calls (nil path) resolve global-level identity. Detail: `adapters/CONTEXT.md` (GitEnv section).

## Agent Loop Control Flow + Graceful Task Cancellation

**Loop injection point**: `Runner.loop/1` is the top of each turn (after `compress_if_needed`/`check_limit_warnings`, before `do_turn`'s LLM call); `AgentState.pending_user_messages` (`[String.t()]`) is the cross-process writable surface (written via `Store.append_pending_user_message/2` — shared by `send_user_message/2` AND graceful cancellation), drained by `drain_and_inject_user_messages/1` → appended as user-role messages (turn-tagged) → `sync_context_to_ets/2` immediately (visible in `get_agent_history/1`). API chain: `EvoDash.NodeContext` → `RemoteNode.send_agent_message/3` → `RemoteAPI.send_agent_message/2` (guard `{:error, :scheduler_not_started}` when `:evogit_agent_state` ETS undefined) → `AgentScheduler.send_user_message/2` → `Store.append_pending_user_message/2`. Returns `:ok | {:error, :not_found}` (message NOT echoed); ⚠️ RemoteNode's local path nests the inner error — `{:ok, {:error, :not_found}}` — dashboard falsely reports success for a missing agent. Exposed via `get_agent_state/1` (drops only `:context`); every append/drain emits a `{:agent_updated, ..., [pending_user_messages: [...]]}` delta but nothing renders it. Detail: `agent/CONTEXT.md` + `agent_scheduler/CONTEXT.md`.

**Two cancellation actions** (node-aware via the RemoteNode RPC chain — `RemoteNode.cancel_task/2` + `force_kill_task/2`):
- **Graceful cancel** (`TaskRegistry.cancel_task/1`): `:pending` → immediate `:cancelled` (and `start_task` refuses already-cancelled/cancelling ids); `:running` → status `:cancelling` + `AgentScheduler.begin_graceful_cancel/1` (`:evogit_cancelling_tasks` ETS marker + per-agent cancel message via `Store.append_pending_user_message/2` (NOT `send_user_message` — self-call deadlock) + `AgentState.cancel_requested` flag). Runner drains the flag right after user messages (`maybe_enter_cancel_grace/1`) and enters cancel-grace with a **3-turn budget** (`LoopState.grace_turns_remaining`; `maybe_recovery_auto_commit` runs first — "save the changes"). Final-status force-mapping persists `:cancelled` WITH result/usage/archive **preserved** (never nil'd). `:cancelling` is non-terminal + idempotent (codec atom, TaskInfo typespec, lease valid, `clear_finished_tasks` won't delete it, startup reconciliation maps orphaned `:cancelling` → `:cancelled`). New root spawns are blocked for cancelling tasks (`run_agent` replies `{:error, :cancelled}` — blocks Genesis Mode B's second root); new-agent registration for a cancelling task gets the flag immediately.
- **Force kill** (`TaskRegistry.force_kill_task/1`): kills all agents + the wrapper, persists **`:failed`** with result nil'd. `:cancelled` means ONLY gracefully-cancelled tasks. Works from `:running` and `:cancelling`.
- **Dashboard**: visible Cancel button (`[:pending, :running]`) → custom warning modal → `confirm_cancel_task`; force kill lives in the task-card dropdown (`[:running, :cancelling]`, "Danger zone") → warning modal → `confirm_force_kill_task`. `:cancelling` renders violet and is reviewable like completed (ReviewLive treats `:cancelled` like `:completed`).

Detail: `task_registry/CONTEXT.md` (Graceful Cancellation & Force Kill) + `agent_scheduler/CONTEXT.md` (Graceful Cancellation) + `agent/CONTEXT.md` (Grace Period).

## Sandbox / Test Interaction

Sandbox mode is resolved via **`EvoGit.Config.resolve([:sandbox, :mode])`** (defaults → user TOML → runtime overrides) — **NOT application env**: `Application.put_env(:evo_git, :sandbox, :disabled)` in tests is a NO-OP, and `config/config.exs:57-58`'s `config :evo_git, sandbox: :auto` is unused (no lib module reads `Application.get_env(:evo_git, :sandbox)`). All Linux sandbox modules gate on `@mix_env == :test` FIRST (`Linux.enabled?/0`, `Bwrap.enabled?/0`, `SandboxSlice.sandbox_enabled?/0`, `SandboxProcessRegistry.sandbox_enabled?/0`) → in test env the Linux backend takes the disabled `bash -c` path (identical to `None`) — no `systemd-run` is ever invoked in tests (CI containers lack the systemd user bus). Tool commit paths: `write_context`/`edit_context` with `commit: true` DO go through the sandbox dispatch (`EvoGit.sandbox_run` for `git add` + `git commit`, context.ex:273,276 / 209,212); **`make_dir` is the ONLY committing tool that bypasses the sandbox** (`Shared.do_git_commit` → `Adapters.Git` raw `System.cmd`); file/context read tools use `File.*` directly (no sandbox). `tools_test.exs` has no `run_bash`/`run_git`/`run_powershell` tests; no `@tag :skip` or `ExUnit.configure(exclude: ...)` exists. The real systemd-run path has zero DIRECT test coverage (only `Linux.args/4`/`Bwrap.args/4` arg generation + the disabled/`None` backends are tested). Detail: `sandbox/CONTEXT.md` + `test/CONTEXT.md`.

## Update-Timing Safety (current mechanics)

No restart/update machinery exists in `:evo_git` (zero `System.restart`/`:init.restart`/`:init.stop` usages; no custom signal handlers; no state checkpointing on exit). Graceful stop is done from `:evo_dash` (`EvoDashWeb.LiveHooks.DesktopQuit.default_stop/0`, SystemLive local Stop — both `System.stop/0`). The Rust shell NEVER sends SIGTERM (headless mode + watchdog force-kill use `child.kill()` = SIGKILL on Unix). `bin/<release> stop`/`restart` require `RELEASE_DISTRIBUTION != none` + a resolvable `RELEASE_NODE` — the desktop sidecar runs non-distributed (`RELEASE_DISTRIBUTION=none`), so `bin/genesis_desktop stop` cannot connect; the desktop backend must be stopped from within. Pre-restart mechanism = **graceful cancel** (3-turn grace; NOT wall-clock bounded — each grace turn is a full LLM+tools cycle with retry backoff, so an update flow must wait for a terminal status with a generous timeout, or force-kill). Idle-detection API (dashboard "safe to restart" check): `TaskRegistry.list_task_ids(statuses \\ [])` / `list_tasks_summary(statuses \\ [], since \\ nil)` / `list_tasks_changed_since/1` / `get_task/1` (+ RemoteAPI/RemoteNode mirrors); in-flight set = `[:running, :pending, :finalizing, :cancelling]`. Task status lives ONLY in SQLite (`tasks.status`); scheduler ETS (`:evogit_sched_meta`, `:evogit_agent_state`, `:evogit_cancelling_tasks`, `:evogit_archive_records`) holds live-agent state only and dies with the app. Lease/heartbeat: `@lease_duration` 120s, heartbeat every 60s renews `[:running, :pending, :cancelling]` (not `:finalizing`), one-shot `:lease_sweep` at `@sweep_after` 150s marks orphaned `:running` → `:failed` ONLY when the lease is invalid AND no live sched_meta agents — the sweep never reschedules (a `:running` row whose lease is still valid at 150s post-boot stays `:running` forever). Startup reconciliation: `:finalizing` → `:failed`, `:cancelling` → `:cancelled` (synchronous, result preserved); `:running` deliberately untouched. **No auto-resume** — resume is always a NEW `:evolve` task via the dashboard Review page (`resume_from` → `ResumeContext`; `review_status: :continued` is a persisted marker only — core never gates on it). Detail: `agent/CONTEXT.md` (Update-Timing Safety) + `agent_scheduler/CONTEXT.md` (Shutdown & Restart Safety) + `task_registry/CONTEXT.md` (Restart Recovery).

## Cross-Node RPC Payload Profile (key design facts)

Remote dashboard RPC (`EvoGit.RemoteNode` → `EvoGit.AgentScheduler.RemoteAPI` via `:erpc.call/5`; local node = direct call) transfers **uncompressed native BEAM terms** (no JSON boundary; `:remote_rpc_timeout` default 30s read at call time). Every wrapper: local → direct call (errors surface truthfully), remote → `:erpc` normalized `{:ok, _} | {:error, {kind, reason}}` with per-function failure fallbacks (`[]`/`%{}`/`nil`/`false`). Payload profile: 🔴 `get_agent_history/1` (full `context.messages`, pre-compression up to ~100k tokens default threshold), `list_tasks/0` (full decode — no dashboard caller); 🟠 `list_agents/0` (re-fetched per broadcast; `objective` dominates when attached-file content is included), `get_task/1`, `list_tasks_paginated/1`, `load_file_diff`/`get_file_content` (lazy per file click); 🟢 `list_tasks_summary/1` (15-key, no result), `list_task_ids/1`, `list_tasks_changed_since/1` (biggest transferred-vs-consumed gap — consumers use only `:updated_at`), config reads, review metadata/commits/`check_merge`. Dead surface (no dashboard caller): `get_agent_state/1`, `load_review_data/2`, `list_tasks/0` — candidates for removal/deprecation. Detail: `lib/evo_git/CONTEXT.md` (Review/RemoteNode Return-Shape Contract).

## Mix Tasks

- **`mix changelog <version> [--from <ref>] [--to <ref>] [--model <id>] [--file <path>]`** (`Mix.Tasks.Changelog`): PR/merge-aware first-parent collection — each merge commit becomes ONE change carrying its branch commits (`git log --no-merges <merge>^1..<merge>`), non-merge first-parent commits = single-commit changes; `^Bump version to` + mechanical-noise (`^Update mix hash`, `^Update CONTEXT.md`) commits filtered (all-filtered → "No commits found..." + exit). **Record-parsing gotcha**: `--pretty=format:` joins records with `\n` — split on `@record_sep` (`"\x1e"`) AND trim each record (`String.split(trim: true)` alone leaves a leading `\n` corrupting hashes). Two-stage map-reduce via `ReqLLM.stream_object` (default `deepseek:deepseek-v4-flash`, `--model` overrides) → Keep-a-Changelog section (Added/Changed/Fixed/Removed/Security/Deprecated, non-empty only); stage 2 (`build_aggregate_prompt/2`, `@doc false` public for direct prompt tests) explicitly instructs the LLM to **merge related changes across PRs/merges into single entries** — separate merges that add/fix/improve the same feature collapse into ONE entry describing the net user-visible state (e.g. "Add feature xyz"). **Docs-only changes never appear** (README rewording, docs/CONTEXT.md edits, comment-only work): only commit subjects/bodies are collected (no file paths), so exclusion is PROMPT-driven — stage 1 summarizes only the user-facing CODE parts of mixed changes and replies with the fixed `__NO_USER_FACING_CHANGES__` marker for entirely non-user-facing changes (`summarize_prs/3` drops marker/empty summaries before stage 2), and stage 2 is told to omit any remaining non-code entry; all-docs releases short-circuit via `{:ok, []}` → "No user-facing code changes found..." and CHANGELOG.md is NOT modified (mirrors the empty-range path). CHANGELOG.md created with `# Changelog` + empty `## [Unreleased]` when missing; else new `## [<version>] - <date>` inserted after the header or same-version section REPLACED in place; on LLM error the file is NOT modified; asks to commit (only that file staged). Test seams: `:changelog_summarizer` / `:changelog_pr_summarizer` / `:changelog_aggregator` (app-env). Full detail: root `./CONTEXT.md` ("AI changelog generation").
- **`mix bump.version <new-version>`** (`Mix.Tasks.Bump.Version`): updates `VERSION` (single source of truth) + syncs `tauri.conf.json`, `Cargo.toml`, `Cargo.lock`, `README.md` badge; interactively asks whether to commit (only touched files staged, never `git add -A`), then optionally delegates to `mix changelog` — invoked DIRECTLY (`Mix.Tasks.Changelog.run([version])`), NOT `Mix.Task.run/2` (refuses re-runs in the current Mix invocation). Full detail: root `./CONTEXT.md` ("Versioning").
- **`mix migrate.store`** (`Mix.Tasks.Migrate.Store`): standalone one-time DB upgrade (never starts `:evo_git` — works even when the app cannot boot on an unmigrated DB): create_tables → migrate_schema → normalize_timestamps → canonical result rewrite → opts object rewrite → branch_name/updated_at backfill → quarantine-table drops; idempotent. Detail: `store/CONTEXT.md`.

## Known Issues

- **Process/thread exhaustion inside containers breaks git forks (EAGAIN).** In a rootless podman container every container process shares the host user's UID, so the kernel's per-UID `RLIMIT_NPROC` (`ulimit -u`) is enforced across container + host user processes together (every OS thread counts — BEAM scheduler/async threads, node/chrome worker threads, git/gh port forks). When hit, `fork()` fails with `EAGAIN` — git prints "cannot fork() ... Resource temporarily unavailable" (e.g. during `git worktree add`). A heavy parallel run can exhaust it mid-run; the WorktreeManager survives such events (persistent init marker + re-monitoring — no cascade wipe), but the run still loses agents to crash-retry. Prevention: (1) raise the HOST user's nproc limit (`/etc/security/limits.conf` or `systemctl --user set-property user.slice TasksMax=infinity`); (2) launch the container with `--ulimit nproc=65536:65536 --pids-limit -1` (or `pids_limit = -1` + `default_ulimits` in `~/.config/containers/containers.conf`); (3) rootless-with-systemd-cgroups: delegate the pids controller (`/etc/systemd/system/user@.service.d/delegate.conf` → `[Service] Delegate=cpu cpuset io memory pids`; `systemctl daemon-reload`); (4) verify live usage via `ulimit -u`, `/sys/fs/cgroup/pids.max`, `/sys/fs/cgroup/pids.current`; (5) reduce runtime concurrency (fewer parallel agents / lower `[scheduler] default_llm_max_concurrency` or per-profile `concurrency` in config.toml).

## Constraints

- Part of an **umbrella project** — deps, build artifacts, and lockfile live at the repository root. Elixir ~> 1.18 required. **Git CLI only** (no libgit2 bindings). No source code at root — all code under `./apps/`.
- All git operations must go through `EvoGit.Adapters.Git` — no direct `System.cmd("git", ...)` in domain modules (exceptions: `Shared.do_git_commit`'s raw `System.cmd` for `make_dir` commits, and GitEnv's own config reads).
- `remote_api.ex` (~1090 lines) and `remote_node.ex` (~1150 lines) are intentionally long cohesive RPC wrapper modules — do NOT split them; add new RPC functions to these modules (identity-unwrap wrappers via the `defnode` macro from `EvoGit.RemoteNode.Defnode`; per-function-fallback wrappers stay hand-written).
- Agents are transient modules using the `EvoGit.Agent` behaviour; framework state lives in ETS. Agent execution happens in **isolated git worktrees** managed by `AgentScheduler.WorktreeManager` (monitor-driven reclaim) — never on the main working copy. Agents commit before delegating subagents (auto-commit fallback enforced by scheduler).
- Subdirectories follow Elixir convention: `./lib/evo_git/<subdir>/` maps to `EvoGit.<Subdir>` namespace.
- **Three-level configuration** (`EvoGit.Config`): built-in defaults → user config (`~/.config/genesis/config.toml`) → runtime overrides. **No default model or username is hardcoded**; user config follows XDG conventions.
- **Slot discipline**: all LLM calls must acquire/release slots via `AgentScheduler`. Multi-model support — each model has independent concurrency slots, generation params, and per-model 60-second backoff. Config via `[[llm.models]]` TOML array-of-tables; old flat `[llm]` auto-migrates to a single-profile list with id `"default"` **at load time only**; on save, flat gen-param fields are stripped when `llm.models` is non-empty. `AgentSpec.model_id` binds an agent to a profile; child agents inherit the parent's `model_id` by default. Tool executions independently throttled via `max_tool_concurrency`.
- **Task-scoped naming**: worktree directories `worker_T<task_number>_A<task_local_id>`, branches `evogit-agent-T<task_number>-A<task_local_id>`. `task_id` = 16-char hex GUID (archive refs, task grouping); `task_number` = short integer from a linear scan of `.genesis/workers/`, used only for naming.
- **Task persistence runs in `:evo_git`** — `EvoGit.Store` + `EvoGit.TaskRegistry` are started by `EvoGit.Application`'s supervision tree, so the headless `genesis_remote` release has full task history without any dashboard dependency. TaskRegistry reconciles against the shared platform SQLite DB (`<data_dir>/tasks.sqlite`); a freshly-started Registry leaves orphaned `:running` rows to the lease/heartbeat sweep (respects lease validity for the legitimate multi-VM case). `trim_recent_projects/1` is nil-safe (nil `last_opened_at` sorts LAST — the `DateTime` sorter would raise). Test isolation guard: `config/test.exs` pins `config :evo_git, :data_dir` to a UNIQUE per-run temp path (`<tmp>/evogit_test_data/genesis-<os-pid>-<unique-integer>`), so concurrent runs/worktrees never share a `tasks.sqlite` and tests never touch the production DB. Detail: `store/CONTEXT.md` + `task_registry/CONTEXT.md`.
- **Task Archive (opt-in)**: `--archive` CLI flag → `runtime_opts` → `AgentSpec.opts` → `AgentState.archive`. Each completing agent (`CompleteTask.complete/4`, via `ToolDispatch.do_complete/2`) writes `refs/genesis/archive/T{task_id}-A{agent_id}-start`/`-final` git refs + a usage git note + an archive record into `:evogit_archive_records` (ETS `:set` keyed `{task_id, agent_id}`, idempotent — at most ONE record per agent per task). Only writer is `CompleteTask.complete/4`; a CRASH/FAILURE/ABORT writes NO record (crashed agents ABSENT from `archive_records`, usage LOST — retry resets to `Usage.zero()`). Records are collected into `Result.archive_records` ONLY at successful root completion (`Lifecycle.handle_task_result/3`) then cleared (consumed); they flow into the `archive_metadata` column AND the `result` column JSON. Genesis Mode B: 2 roots share one `task_id` → exactly 2 nil-parent records. Full record field map (`usage` as PLAIN MAP, `llm_settings`, `agent_settings`, `foreign_repos`) + JSON round-trip caveats (string keys/timestamps — consumers normalize via core `Usage.from_archive_map/1` or dash `ArchiveHelpers.normalize_agent_keys/1`; canonical cost keys are `input_cost`/`output_cost`/`total_cost`, never `cost`): `agent_scheduler/CONTEXT.md` (Task archive table).
- **Distribution & Release Setup** (EPMD-less distribution via `-epmd_module Elixir.EvoGit.EpmdDist` + `-start_epmd false` in all `rel/vm.args.eex`; local dashboard enables distribution on-demand, remote daemon starts distributed at boot on port 9000; release definitions in the umbrella ROOT `mix.exs` (`genesis`/`genesis_desktop`/`genesis_remote`); endpoint bind `config/runtime.exs` prod-only `PORT`/`PHX_IP` → `[server] listen_port`/`listen_ip` (default 9999); orphaned-backend zombie prevention via dynamic port + `EVOGIT_LIFETIME_PORT` pipe): detail `lib/evo_git/CONTEXT.md` + root `./CONTEXT.md`.
- **SSH Remote Bootstrap Failure Modes** (remote ssh commands are argv arrays, never quote-wrapped spawn strings — `run_ssh_command/3` `{:spawn_executable, ...}` + one argv element, bash-wrapped via `bash_wrap/1` for login-shell independence; `systemctl --user reset-failed` before launch; `verify_daemon_healthy/2` 3×1s; best-effort `:copying_config` scp; EPMD module set at runtime via `Distribution.enable_for_connection/1`): detail `lib/evo_git/CONTEXT.md`.
- **Config Caching Behavior** (mtime+size-validated `:persistent_term` cache for `user_config/0`/`credentials/0`; the ACTUAL cache is AgentScheduler state — `get_config/0` cached, `reload_config/0` + `get_config_status/0` re-read disk; `save_user_config/1` does NOT auto-reload; `RemoteConnections` does NOT auto-create `remote_connections.toml`): detail `config/CONTEXT.md` (Config Caching).
- **Sandbox & Test Suite Interaction** (mode via `Config.resolve([:sandbox, :mode])` — app-env put_env is a no-op; `@mix_env == :test` gates; `make_dir` is the only unsandboxed committing tool): detail `sandbox/CONTEXT.md` + `test/CONTEXT.md`.
- **Genesis Mode B — Two-Root-Agent Architecture** (`Genesis.run_new_codebase/4`): Phase 1 `EvoGit.Agents.Architect` (designs CONTEXT.md/directory structure/API; delegates implementation to subagents) → Phase 2 `EvoGit.Agents.Manager` (second root starting from the architect's final commit; "Genesis Implementation Mode" prompt section). Both share ONE `task_id` (generated upfront); usage (`Usage.add/2`), `agent_count`, and `archive_records` merged into a single `%Result{}` → `merge_and_report/3`. Architect fails → immediate error return; Architect succeeds but Manager fails → architect's result returned as partial success with a warning log. Mode A (existing codebase / `ContextExtractor`) uses a single root agent. Detail: `runtime/CONTEXT.md`.
- **No `try/rescue` anti-pattern**: prefer `case`/`if`/`cond`/`with` + non-crashing function variants (`Regex.compile` not `Regex.compile!`, `JSON.encode/2` not `Jason.encode!/2` + rescue, `:persistent_term.get/2` not `get/1` + rescue, `Integer.parse/1` not `String.to_integer/1` + rescue). Justified exceptions only: `terminate/2` cleanup that must never crash the caller; centralized exception boundaries (`safe_llm_call/2`); `SystemCheck` diagnostics; `String.to_existing_atom/1` on untrusted keys; `try/catch` around functions with no non-crashing variant (`Path.wildcard`). **Every remaining `try/rescue` MUST have a comment justifying it.** Eliminate: rescues around `!`-functions with non-crashing variants; silent `rescue _ -> :ok`; LLM rescues degrading silently to defaults; broad rescues hiding bugs; redundant internal rescues when the public entry point already rescues.
