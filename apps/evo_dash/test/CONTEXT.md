# Test Directory

## Intent

Holds all test files for the EvoDash application: the ExUnit test runner configuration, shared test support modules (ConnCase, TestHelpers, picker fakes), and suites for the domain layer, web layer, LiveView pages, components, controllers, and live hooks.

NOTE: The domain-layer modules (`Store`, `TaskRegistry`, `TaskInfo`, `RecentProject`) live in the `:evo_git` app as `EvoGit.Store`, `EvoGit.TaskRegistry`, `EvoGit.TaskInfo`, `EvoGit.RecentProject` and are tested there — this app has no domain-layer test suite for them. Web-layer tests reference `EvoGit.TaskRegistry` / `EvoGit.Store` / `EvoGit.TaskInfo` directly (per-test isolation of those two singletons is owned by `EvoDash.Test.IsolatedTaskStore` — they are children of `EvoGit.Supervisor`, NOT of `EvoDash.Supervisor`).

## Routing Table

- `support/` → Shared test support modules (ConnCase, TestHelpers, directory-picker fakes)
- `evo_dash/` → Domain-layer tests (AttachedFile, SettingsUtils, DesktopLifetime, NodeContext, DirectoryPicker, MarkdownRender, UpdateStatus)
- `evo_dash_web/` → Web-layer tests — LiveView suites, components, controllers, live hooks, shared helpers
- `evo_dash_web/live/` → LiveView page test suites + support-module unit tests (has its own CONTEXT.md)
- `evo_dash_web/live_hooks/` → Live-hook tests (NodeAware, Guide, Appearance, DesktopQuit, UpdateStatus)
- `evo_dash_web/components/` → Function-component tests
- `evo_dash_web/controllers/` → Controller / error-template tests

## API Surface

### Top-Level
- `test_helper.exs` — Bootstraps ExUnit (`ExUnit.start(capture_log: true)`). Entry point for the test suite. Suite-wide setup before ExUnit starts: redirects `XDG_DATA_HOME` to a temp dir, disables the directory picker (`Application.put_env(:evo_dash, :directory_picker, enabled: false)`; `config/test.exs:54` carries the same flag for a real `mix test` run), and pins `Application.put_env(:evo_git, :nix_enabled, false)` so `EvoGit.Nix.enabled?/0` never falls back to the developer's real `~/.config/genesis` config or shells out to real `nix print-dev-env` during the suite (tests that specifically need nix enable it explicitly). An `ExUnit.after_suite/1` hook removes ONLY this run's own unique data dir (the `:evo_git, :data_dir` app-env value from `config/test.exs`), never the shared `System.tmp_dir!()/evogit_test_data` parent (a concurrent `mix test` owns a sibling unique dir under it). It ALSO redirects `XDG_CONFIG_HOME` at boot to a RUN-UNIQUE EMPTY dir (`<tmp>/evogit_test_config/genesis-<os.pid()>-<unique_int>`, test_helper.exs:21-28), removing it in the same `after_suite` hook — so the whole suite is hermetic and nothing reads or writes the developer's real `~/.config/genesis` (a no-op on macOS, where `EvoGit.Platform.base_dir/3` ignores XDG_CONFIG_HOME). Suites that WRITE config still point `XDG_CONFIG_HOME` at their own per-test temp dir.

### `support/`
- `conn_case.ex` — `EvoDashWeb.ConnCase`: shared `ExUnit.CaseTemplate` for connection-based tests. Endpoint registration (`@endpoint EvoDashWeb.Endpoint`), verified routes (`use EvoDashWeb, :verified_routes`), imports `Plug.Conn` / `Phoenix.ConnTest`, default setup returns a built `%Conn{}`.
- `test_helpers.ex` — `EvoDashWeb.TestHelpers`: shared `flush_loading/4` — polls the LiveView test proxy's rendered HTML until a loading marker disappears (async `Task.Supervisor`-backed page loads) or `flunk`s on timeout. `render_async/2` does NOT await TaskSupervisor children, so page-load tests must flush. The LiveView test files' private `flush_*_load` helpers are one-line delegates to it.
- `fake_directory_picker.ex` / `fake_directory_picker_wx.ex` — `EvoDash.DirectoryPicker.Fake` (module-level fake, installed via the `:directory_picker_module` env) and `EvoDash.DirectoryPicker.Wx.Fake` (wx seam fake, via the `:directory_picker_wx` env); used by the directory-picker and attach-file tests (see "Notes for Agents — attach-file" below).
- `isolated_task_store.ex` — `EvoDash.Test.IsolatedTaskStore`: deterministic per-test `EvoGit.Store`/`EvoGit.TaskRegistry` isolation for `async: false` suites. `isolate!/1` (prefix used in the temp-dir name) terminates the production children (`Supervisor.terminate_child(EvoGit.Supervisor, ...)`), starts a temp-sqlite pair under a helper-owned supervisor, and registers an `on_exit` that stops the isolated pair FIRST, `rm_rf`s ONLY its own `<tmp>/evogit_test_<prefix>_<int>` dir, then restarts the production children with CHECKED results (a failed restore raises instead of silently leaving the globals bound to a foreign DB) and verifies the restored Store's `data_dir`. `assert_production!/0` is the read-side guard for suites that use the production pair without isolating (`review_live_test.exs` calls it in setup): it raises when the `EvoGit.Store` name is bound to a non-production data dir or `EvoGit.TaskRegistry` is down/bound to a foreign `task_store`. `production_sqlite_path/0` mirrors `EvoGit.Application.start/2`'s derivation (`Application.get_env(:evo_git, :data_dir, EvoGit.Platform.data_dir())` + `tasks.sqlite`).

### `evo_dash/` (domain-layer tests)
- `attached_file_test.exs` — `EvoDash.AttachedFileTest` — attached-file reading/trimming behind the objective editor's file attach.
- `settings_utils_test.exs` — `EvoDash.SettingsUtilsTest`
- `desktop_lifetime_test.exs` — `EvoDash.DesktopLifetimeTest`
- `node_context_test.exs` — `EvoDash.NodeContextTest` — NodeContext delegation shape incl. `cancel_task/2` / `force_kill_task/2` smoke tests (see task-cancellation notes).
- `directory_picker_test.exs` — `EvoDash.DirectoryPickerTest` (async: true) — Real picker GenServer + `EvoDash.DirectoryPicker.Wx.Fake` (per-test `enabled: true` override + `:directory_picker_wx` env injection): `pick/2` and `pick/3` (file mode) — file pick delivers the file path via the fake's `new_file_dialog` + ref-typed `get_path`, `pick/2` ≡ `pick/3 :directory`, kind-agnostic busy serialization (`set_gate/1` dialog block), wx init-failure/server-death degradation to `:unavailable` with busy cleared, disabled-config rejection.
- `markdown_render_test.exs` — `EvoDash.MarkdownRenderTest` — Markdown-to-HTML rendering edge cases (nil, empty, headings, code blocks, tables, bold).
- `update_status_test.exs` — `EvoDash.UpdateStatusTest`

### `evo_dash_web/` (top-level web tests)
- `helpers_test.exs` — `EvoDashWeb.HelpersTest` — shared UI helpers (status badges, datetime formatting, icons, modals), incl. the exact-string `:cancelling` badge test.
- `theme_color_test.exs` — `EvoDashWeb.ThemeColorTest`

### `evo_dash_web/components/`
- `project_components_test.exs` — `EvoDashWeb.ProjectComponentsTest` — command-palette project selector (`project_omnibox/1`): trigger rendering + client-side wiring (`phx-keydown`/`phx-change` search input, `phx-click-away`/`phx-click` overlay/backdrop). Uses `render_component/2` + Floki helpers; the active_project fixture must include a `:path` key.
- `task_form_components_test.exs` — `EvoDashWeb.TaskFormComponentsTest` — `layout_for/1` boundary tests (1200-char / 32-line thresholds), bottom-toolbar contract (attach "+" first inside `.input-controls`, mode/agent/model selects in DOM order, MODE select carries `ml-auto` leading the right-aligned cluster, circular `#task-launch-button` LAST/rightmost with no auto margin, `select-sm` compact classes), no-model-profiles case, disabled state, textarea bindings (`phx-update="ignore"` + `phx-hook="AdaptiveInput"`, no per-keystroke server event), `.input-controls` `flex-nowrap` contract pin, and the attach-file "+" button (`button#objective-file-button`, `phx-hook="FilePicker"`, INSIDE `.input-controls` as its first element, hidden when disabled).
- `diff_viewer_test.exs` — `EvoDashWeb.DiffViewerTest` — `parse_hunk_header/1` + `build_split_pairs/1` pure units and split-view rendering contract: diff renders as ESCAPED plain text (no HTML injection), no server-side highlight markup, `data-language` attribute per `.diff-file-section`, hunk headers as plain text, gutters/content on both sides, file header with path + stats. Floki used purely as an HTML query helper. No other test file asserts on diff body content.
- `model_profiles_editor_test.exs` — `EvoDashWeb.ModelProfilesEditorTest`
- `task_card_components_test.exs` — `EvoDashWeb.TaskCardComponentsTest` — task card affordances incl. the cancel/force-kill button visibility rules.
- `archive_tree_test.exs` — `EvoDashWeb.ArchiveTreeTest`
- `remote_gate_components_test.exs` — `EvoDashWeb.RemoteGateComponentsTest`
- `setting_card_test.exs` — `EvoDashWeb.SettingCardTest`

### `evo_dash_web/controllers/`
- `error_html_test.exs` — `EvoDashWeb.ErrorHTMLTest` — HTML error templates (404 → "Not Found", 500 → "Internal Server Error").
- `error_json_test.exs` — `EvoDashWeb.ErrorJSONTest` — JSON error responses (404/500 with the `%{errors: %{detail: ...}}` shape).
- `page_controller_test.exs` — `EvoDashWeb.PageControllerTest`
- `task_export_controller_test.exs` — `EvoDashWeb.TaskExportControllerTest` — JSON export endpoint.

### `evo_dash_web/live_hooks/`
- `node_aware_test.exs` — `EvoDashWeb.NodeAwareTest` (async: false) — the stateful `EvoDashWeb.LiveHooks.NodeAware` tests that need the isolated Store+TaskRegistry, per-test `XDG_CONFIG_HOME` isolation, and fake `ConnectionManager` GenServers registered in `EvoGit.RemoteConnection.Registry`: `load_running_and_pending_tasks/1` node-aware sourcing, `assign_node/2` sidebar reload, the `handle_tasks_result/2` hub-write path, and the async-connect helper.
- `node_aware_pure_test.exs` — `EvoDashWeb.NodeAwarePureTest` (async: true) — 26 pure tests (the stateful NodeAware integration tests stay in `node_aware_test.exs`): `handle_connection_status/2` transition detection (read-only registry reads), pure `partition_active_tasks/1`, the node-filtered `handle_task_info/2` debounce, and pure `event_from_current_node?/2`. Uses a hand-built minimal `%Phoenix.LiveView.Socket{}` (local `socket/1`) + `patch_to/1` push_patch extractor; touches no Store/TaskRegistry, XDG_CONFIG_HOME, or ETS hub.
- `guide_test.exs` — `EvoDashWeb.LiveHooks.GuideTest` (async: false) — guide overlay hook LiveView integration tests (real `/tasks` mounts against an isolated Store/TaskRegistry).
- `guide_pure_test.exs` — `EvoDashWeb.LiveHooks.GuidePureTest` (async: true) — pure `normalize_guide/2` + `relevant?/2` unit tests (the Guide LiveView integration tests live in `guide_test.exs`); no LiveView, no Store/TaskRegistry, no XDG, no ETS hub.
- `desktop_quit_test.exs` — `EvoDashWeb.LiveHooks.DesktopQuitTest` (async: true) — tray-quit confirm hook; saves/restores the `:desktop_quit_stop_fun` seam instead of deleting it.
- `update_status_test.exs` — `EvoDashWeb.LiveHooks.UpdateStatusTest` (async: false) — shares `:desktop_quit_stop_fun` + the `EvoDash.UpdateStatus` hub with the `async: true` `desktop_quit_test.exs`, so it must stay serialized.
- `appearance_test.exs` — `EvoDashWeb.LiveHooks.AppearanceTest` (async: false) — the Appearance on-mount hook (configurable accent color → `data-accent-color` on `#app-layout`): pure seams `resolve_accent/1` + `accent_from_config/1`, dead-render on_mount seeding "blue" (or a warm `EvoDash.AccentCache` value for the page's node param), full-page connected renders asserting the attribute with the default AND a non-default value (local resolution is synchronous in the attached `:handle_params` interceptor → correct on the FIRST connected render, no flush), and the remote `?node=` accent-cache scenarios (warm remote accent renders synchronously with no async flush, successful async fetch writes the cache, stale-guard drops superseded results without a cache write, failed fetch is a no-op never regressing a warm value, pending targets show their warm accent never the local one) — via fake ConnectionManagers registered in `EvoGit.RemoteConnection.Registry` (local-BEAM-node fake → deterministic success; nonexistent node → fail-fast no-op). Per-test XDG_CONFIG_HOME isolation; config write-then-read is fresh without a cache reset (`EvoGit.Config` revalidates by mtime+size); per-test `EvoDash.AccentCache.reset()` + `EvoDash.ActiveTasks.reset()` discipline (boot-created shared named-ETS tables). Notes in the "appearance accent-color tests" section below.

### `evo_dash_web/live/` (LiveView page suites — see its own CONTEXT.md for routing)
- `home_live_test.exs` — `EvoDashWeb.HomeLiveTest` (async: false) — **Home chat page** (`GET /help`, ChatGPT-style chat wired to the `:reflect` self-reflective agent; the Projects page is served at `GET /` and `GET /projects`). 76 tests: idle render, send-message (real `render_submit` → assert the persisted task ROW via `EvoGit.Store.safe_select_all_tasks`: `mode: "reflect"`, NO `:path` key; a second message's objective = `Transcript.build_preamble` + text; chat_task_id regression — a real send sets `chat_task_id` synchronously to the persisted reflect task's id and renders no "Failed to start the task" error bubble, uses `cleanup_task_on_exit/1`), whitespace no-op, new-chat (**ChatHistory store semantics**: fresh id, old chat KEPT, prune cap 10), onboarding dead-render redirect, streaming display (REAL-shaped core payloads: INTEGER agent ids, KEYWORD-LIST `changed_fields` `[message_count: n]` — the `is_list + Keyword.has_key?` guard regression test, atom lists ignored, HistoryGate; real `%ReqLLM.Message{}` histories + real `:evogit_agent_state` ETS rows via `seed_agent_state!/2`; foreign-node filtering; injected results `{:chat_history_loaded, node, seq, id, msgs}` use the CURRENT `chat_fetch_seq` read dynamically), completion/error rendering (plain-map `{:chat_task_loaded, node, seq, "t1", %{status:, result:}}` injections + badge persistence after finalize), assistant task-card (status badge on the LAST assistant entry only — `badge_count/1` counts `~r/>\s*Task\s*</s` headers so the sidebar "Active Tasks" never counts; badge transitions :pending→:running→:completed; "Thought process" `<details>` with tool-call rows), chat persistence/restore (remount via `GenServer.stop(view.pid)` → `live(conn, "/help")`; mid-run remount ONE-SHOT `reconcile_chat` terminal/live variants), production-mimicking crash repro (real SchedMeta+AgentState ETS rows + real :running/:completed reflect rows + the exact production event sequence), sidebar robustness (nil-project_path reflect rows, nil timestamps, nil-status broadcasts, real send through the real TaskRegistry), stop/cancel flow, node-awareness (fake ConnectionManager in `EvoGit.RemoteConnection.Registry`; node switch starts a NEW persisted chat, old one kept), model selector (per-chat model `<select>`: rendering with/without profiles, `model_id`+`model_id_locked` threading into the reflect task, Auto-threads-neither-key, remount persistence, suggestion-chip quick-message sends threading the pin exactly like a typed submit (Auto threads neither key), and the pin surviving `new_chat` (reset_chat keeps it; first-ever mounts still Auto) + node switches — assign-based, the remote header select is absent since `ModelSelect.load` degrades to `{[], false}`; assistant-message coverage: finalized text renders `.md-content` via `EvoDash.MarkdownRender` (Floki-scoped — the copy button's `data-content` carries the raw source), streamed text stays plain + `.help-caret`, per-message raw toggle (`toggle_assistant_raw`, scoped `#chat-entry-<id>`), `copied` → "Copied to clipboard" info flash, and user-bubble class pins on BOTH send routes (typed + chip → `w-fit bg-base-300 text-left` inside the `flex justify-end` wrapper, never `bg-primary`); component-level markdown/raw/action-group coverage lives in `home_live/assistant_message_test.exs`. Full detail: `evo_dash_web/live/CONTEXT.md`). **Setup MUST call `EvoDash.ChatHistory.reset()`** (process-shared global GenServer that survives the per-test Store/TaskRegistry isolation — the node_aware_test.exs pattern). **IMPORTANT — fail-fast send + ETS sweep**: the file setup pins the GLOBAL `EvoGit.AgentScheduler` to `model_profiles: []` (L48-51, restored in `on_exit`) so every real send is rejected with `{:error, :llm_not_configured}` BEFORE any agent registration, ETS row, or LLM call (L33-47) — no agent can exist, and the persisted row is `:failed`. `cleanup_task_on_exit/1` (L169-202) still cancels + deletes the row and defensively sweeps any `:evogit_sched_meta`/`:evogit_agent_state` entry by task_id (cancel/delete do NOT remove them) so nothing leaks into `agents_live_test.exs` (which expects an empty agent registry). Note the ExUnit ordering fact recorded at L164-168: the test supervisor (incl. the isolated Store/TaskRegistry) is terminated BEFORE `on_exit` callbacks run, hence the `catch :exit` wrappers. **Config-file setup**: the file-level setup isolates `XDG_CONFIG_HOME` to a fresh temp dir per test (no config exists by default); the "model selector" tests then write a REAL `~/.config/genesis/config.toml` (path via `EvoGit.Config.config_path()`) holding one `[[llm.models]]` profile (`id = "profile-a"`) through the local `write_model_profile_config/0` helper — the empty-config dir drives the no-profiles / Auto paths in all other tests (`ModelSelect.load/1` sees zero profiles → the header select is absent).
- `welcome_live_test.exs` — `EvoDashWeb.WelcomeLiveTest` — Welcome/onboarding page: flat model grid (multi-provider, variants, alphabetical sort), search, all-set state, skip/get-started redirects (to `/welcome/complete`), credential detection, back navigation (client-side `history.back()` fallback — the rendered onclick HTML-escapes quotes), LLM connection-test UI, and the example-task teaching section is NOT rendered on `/welcome` (it lives on `/welcome/complete`).
- `welcome_complete_live_test.exs` — `EvoDashWeb.WelcomeCompleteLiveTest` — `/welcome/complete`: high-level-prompt explanation + example objective (`EvoDashWeb.ExampleTask.example_objective()`, asserted via HTML-special-free distinctive lines since `<`/`&` are escaped in the `<pre>` block) + copy button (`welcome-example-copy`, `ClipboardCopy` hook), and the "Go to Dashboard" CTA — clicking `go_to_dashboard` asserts `assert_redirect(view, "/projects")` AND onboarding completion via `EvoGit.Config.VersionState` (`onboarding_needed?() == false`), under the same per-test temp `XDG_CONFIG_HOME` isolation as `welcome_live_test.exs`.
- `projects_live_test.exs` — `EvoDashWeb.ProjectsLiveTest` — Projects page (served at `GET /` and `GET /projects`): task form, command-palette project selector, project settings, task notifications, directory-picker browse flows, and the file-attach flow (`file_pick` event + `"objective_file"` picker id — happy path pins the exact appended block; manual fallback `file_pick_manual`). **Empty-state contract**: with no active project the bottom toolbar (`.input-controls`, `button#task-launch-button`) is NOT rendered — the hint overlay `"Open a project to get started"` shows. NOTE: ~3500 lines — a legitimately large suite; don't split casually.
- `settings_live_test.exs` — `EvoDashWeb.SettingsLiveTest` — Settings page: search input/handler/results, custom model providers (OpenRouter / OpenAI-Compatible) form rendering + saving, and whitelist safety (unknown category/provider/variant/key-path values safely map to nil/default instead of crashing).
- `settings_live_agents_test.exs` — `EvoDashWeb.SettingsLiveAgentsTest` — the Settings **Agents** category (custom agents + model-selection script editors), incl. its own fake ConnectionManager.
- `tasks_live_test.exs` — `EvoDashWeb.TasksLiveTest` — Cross-project task list: search (by prompt/objective/ID), filter selects, pagination, node-aware behavior, `:cancelling` status display, review button, cancel/force-kill modal actions. **Event-driven broadcast tests** (the page is fully push-based — no polling): manually injected `{:task_updated, ...}` / `{:task_deleted, ...}` (does-not-crash + catch-all), stale page-load results dropped by the `tasks_load_seq` stale-guard, a matching-node broadcast triggering the debounced reload (two-phase `:tasks_reload_pending` true→false pattern), foreign-node broadcasts ignored. Uses an isolated TaskRegistry + Store (`async: false`).
- `review_live_test.exs` — `EvoDashWeb.ReviewLiveTest` — Review page suite (~3900 lines, 83 tests, 32 describe blocks) against the PER-REPOSITORY CARD redesign — the conversation tab renders one card per repo (`RepoCards.repo_cards/1` via the `ReviewComponents.repo_cards/1` facade; the primary-scoped Continue-task button + "…" overflow menu live in `Actions.task_actions/1`). Module setup stubs `:merge_check_runner` to a fast `{:ok, :clean}` + resets `EvoDash.ActiveTasks`. Merge/reject are PER-REPO and never navigate — `render_click(view, "merge", %{"repo_id" => id, "target_branch" => t})` / `render_click(view, "reject", %{"repo_id" => id})`, each card owning `<form phx-submit id="merge-form-<repo_id>">` (hidden `name="repo_id"` + its own `<select name="target_branch">`); the runner seam is invoked ONCE per call. Ids: `#review-repo-cards`, `#review-completion-banner`/`#review-completion-back` (only when completion != nil), `#repo-card-<repo_id>`, `#repo-resolution-<repo_id>` (always present, empty when unresolved), `#merge-form-<repo_id>`. Per-repo `resolution` state machine: `nil` (actions iff `branch_exists`), TERMINAL `:merged`(`target`)/`:rejected`/`:handled` (load-time seed: non-blank branch_name ∧ NOT branch_exists), non-terminal `:error`/`:conflict`(`detail`); a partial failure renders on the CONFLICTING repo's card and the page STAYS. Coverage: missing-task error, ignore action, cancelled tasks, nil branch, archive tab, merge-target selector, async merge check (6-tuple `:merge_check_result` injection), auto-resolve (incl. unreachable-remote), async review-data load + stale guards, and the multi-repo suite (repo-list construction primary-first / read-only + no-commits absent / legacy single-primary, per-repo merge via the `:review_merge_runner` seam, per-repo reject via `:review_reject_runner`, per-repo merge-check results + `switch_repo` via the `<select phx-change>` repo selector — the repo affordance renders only under its gates: the per-card merge select needs `branch_exists`, the Files-changed toolbar select needs non-nil `review_data` — plus the "multi-repo review — repo selector presence and switch semantics" describe block (repo selector renders on BOTH the Files-changed toolbar and the Commits tab only when `length(@review_repos) > 1` — single-repo/legacy tasks get none; `switch_repo` accepts both `%{"repo_id"}` and the defensive `%{"value"}` shape — both switch the active repo + reset `file_filter`, unknown id is a no-op; the Commits tab shows the ACTIVE repo's commits after switching; `repo_select_state/1` Floki helper extracts selector options/selection), auto-resolve/resume primary-scoped foreign-repo carry — runtime-only, core-tested in evo_git `merge_context_test.exs`). Per-repo behavior covered: whitelisted + TERMINAL resolutions are no-ops (runner NOT called), per-repo target change/read (`merge_target_change` moves only that repo's select; a blank/unknown target falls back to that repo's `default_merge_target`), load-time `:handled` seeding, `open_repo_diff` (valid id → `review_tab: :files_changed` + that repo's diff; unknown/malformed → no-op), OVERALL COMPLETION (private `completion_status/2` driven through real clicks: once EVERY resolution is terminal → `:merged` when ≥1 merged else `:rejected`, an already-persisted `:merged`/`:rejected` wins, persisted via `NodeContext.set_review_status/3` + success flash + `#review-completion-banner`, NO navigation, with `invalidate_active_tasks/1` running ONLY on this path), and in-file component pins for `repo_cards/1`/`task_actions/1` via `render_component/2`. Redesigned affordances covered: files-changed tree/filter (`toggle_dir` on the FULL dir path, collapsed-by-default chain, `expand_all_dirs` ancestor-chain / `collapse_all_dirs`, `filter_files` flat case-insensitive mode + "No matching files", `switch_repo` resets `file_filter`), task-actions overflow `<details class="dropdown dropdown-end dropdown-top ml-auto">` (extracted by the `overflow_menu/1` regex helper; item order pinned Create GitHub PR → Extract Skills → Export JSON (gated on `archive_metadata`) → Ignore last/plain/no danger zone) + the Continue-task (`resume`) secondary button, AGGREGATE diff stats (stats bar + `page_tabs` count badges sum across ALL repos via `aggregate_stats/1`, NOT the active repo — pinned with per-repo counts deliberately ≠ `length(files)`), and `short_title` truncation in `page_header` (one-line h1 ≤100 chars + "…", full objective in the `title` attr). File-local helpers: `flush_review_load/2`, `wait_until/2`, `wait_hub_warm/0`, `mount_with_repos/3`, `review_repo/4`, `repo_card_html/2`, `overflow_menu/1`, `repo_select_state/1`, `repo_target_selection/2`, `completion_back_href/1`, `target_branch_select/1`.
- `platform_info_test.exs` — `EvoDashWeb.PlatformInfoTest` — Pure unit tests (async, no LiveView) for `EvoDashWeb.PlatformInfo`: `os_for_node/1` with the `:platform_os_override` injection seam, `show_sandbox?/1` + `sandbox_backend_for/1` truth tables, `filter_schemas_by_category/2`, and the round-trip safety pin (macOS-filtered schemas must NOT deep-delete hidden sandbox keys absent from form params).
- `system_live_test.exs` — `EvoDashWeb.SystemLiveTest` — System page: scheduler pause/resume, system controls (remote-node-only confirm paths via direct `SystemLive.handle_event/3` — the local VM-killing confirm paths are intentionally NOT unit-tested), system self-check, Status sandbox helpers (incl. the `:bwrap` clauses), modal-test idiom. Also carries the `describe "genesis source card"` suite (Genesis Source card = the `EvoGit.SelfReflectiveSource` clone/update UI, local-only): the `:source_status_runner` / `:source_clone_runner` / `:source_update_runner` app-env seams are injected via `Application.put_env` BEFORE `live/3` (resolved at spawn time inside the spawned Task), with fixtures `source_status/1` (full 10-key backend status map) and `not_cloned_status/0` (`exists: false`); the file setup snapshot-restores every source/update/system_samples seam through `restore_env_value/2`. Covers: card renders as a peer cell INSIDE the self-check grid (`check_grid/1` Floki scope, `id="genesis-source-card"`), hidden on a remote node (events `clone_source`/`update_source` are guarded no-ops — runners never invoked), not-cloned vs cloned rendering (Clone/Update buttons, no reference line), explicit reference-override note, loading spinner, clone/update busy→success/error flashes, and the `Code.ensure_loaded?`-guard unavailability path (`{:unavailable, :module_missing}` → "Genesis source is not available in this version"). Helpers: `mount_remote_system/1` (fake `ConnectionManager` + per-test `XDG_CONFIG_HOME`) and `await_view_assign/4` (polls socket assigns; `render_async/2` does not await TaskSupervisor children).
- `agents_live_test.exs` — `EvoDashWeb.AgentsLiveTest` — agent tree: node-aware async loads, push-driven refresh via injected `"agents"`-topic events, history gating.
- Support-module unit tests (pure, no LiveView): `projects_live/` (`project_flow_test.exs` — path normalization incl. `normalize_remote_project_path/2` tilde seam + node-aware `build_foreign_repo/4`/`load_foreign_repos/3`, `path_suggestions/2,3`; `state_persistence_test.exs` — `maybe_restore_foreign_repos/2`; `project_test.exs`), `settings_live/` (`model_profile_helpers_test.exs`, `config_io_test.exs`), `agents_live/optimistic_messages_test.exs`, `system_live/charts_test.exs`.

## Notes for Agents — testing the push-based event contract

The dashboard is fully push-based (node-identity PubSub events on `EvoGit.PubSub`; see "Push-based event contract" in `apps/evo_dash/CONTEXT.md`). LiveView tests drive events by **manual injection** — `send(view.pid, {:task_updated, id, status, node()})`, `send(view.pid, {:task_deleted, id, node()})`, `send(view.pid, {:agent_updated, id, changed_fields, node()})`, `send(view.pid, {:system_sample, node, seq, sample})`, etc. — with **foreign-node broadcasts** (`node != node()`) asserted ignored. Async page loads still run in `EvoDash.TaskSupervisor` children that `render_async/2` cannot see: flush with `EvoDashWeb.TestHelpers.flush_loading/4` (file-local `flush_*_load/2` delegates) and stale-guard paths with `send(view.pid, ...)` result-message injection (`{:tasks_page_loaded, seq, node, result}`, `{:agents_data_loaded, ...}`, `{:agents_refresh_result, seq, node, result}`, `{:system_samples_seeded, seq, node, result}`). The 300ms reload debounce is asserted with the two-phase `:tasks_reload_pending` true→false pattern (tasks_live_test.exs). **Any test asserting on `chart_samples` MUST stub the seed runner** (`Application.put_env(:evo_dash, :system_samples_runner, stub)` in the test body BEFORE mounting — the runner is resolved at spawn time; the default runner reads the REAL `EvoGit.SystemSampler` ring buffer, non-empty whenever any sampler tick has occurred in the VM). Full story: `./evo_dash_web/live/CONTEXT.md` → Notes for Agents.

## Notes for Agents — task-cancellation UX tests

The backend ships `EvoGit.RemoteNode.cancel_task/2` (graceful: `:pending`/`:running` → `:cancelling` → persisted `:cancelled` with result preserved) and `EvoGit.RemoteNode.force_kill_task/2` (brutal: kills agents+wrapper from `:running`/`:cancelling`, result nil'd), with `EvoDash.NodeContext.cancel_task/2` and `force_kill_task/2` delegating. Current test coverage and facts:

- **`:cancelling` is store-safe**: `EvoGit.Store.Codec`'s `@known_atoms` whitelist includes `:cancelling` (and `:cancelled`); `Store.put_task` round-trips it (verified in evo_git `store_test.exs`). A fixture is created with the standard `insert_fixture!(status: :cancelling, ...)` idiom (tasks_live_test.exs, node_aware_test.exs) — direct `EvoGit.Store.put_task(EvoGit.Store, %TaskInfo{status: :cancelling, ...})` — no backend changes needed.
- **`filter_tasks` has NO status whitelist**: `status_filter` stays a string end-to-end (tasks_live.ex) and binds into SQL as a string (`Store.Queries.build_where`), so the filter `<option value="cancelling">` works without core changes.
- **Cancel-button affordance**: task_card_components.ex shows the graceful Cancel button for `[:pending, :running]`; the three-dot dropdown "Force kill" item shows for `[:running, :cancelling]` under a "Danger zone" divider. The menu `<ul>` is always in the DOM (CSS-hidden) so tests assert item presence/absence directly. `:finalizing`/`:cancelling`/terminal statuses get NO cancel button.
- **`partition_active_tasks/1` unit tests**: `node_aware_pure_test.exs` has a pure `describe "partition_active_tasks/1 — pure partitioning"` (`:cancelling` → running partition; `:cancelled` → not; `:running`/`:pending`/`:finalizing` regression) plus an integration test in `node_aware_test.exs`'s `load_running_and_pending_tasks/1` describe (a `:cancelling` fixture with `finished_at: nil` appears in `running_tasks`).
- **Cancel/force-kill handler tests**: `tasks_live_test.exs` has `describe "cancelling status display"`, `describe "cancel task action"`, `describe "force kill action"` (15 tests) covering the modal open/close idiom, `:pending`→`:cancelled` and `:running`→`:cancelling` store-verified transitions, `:completed`→"Failed to cancel task" flash, nil-guard no-ops, filter `value="cancelling"`, and the force-kill success path via `:sys.replace_state(EvoGit.TaskRegistry, ...)` owned-wrapper injection. `helpers_test.exs` has the exact-string `:cancelling` badge test + differs-from-fallback test.
- **`node_context_test.exs`**: `EvoDash.NodeContext.cancel_task/2` / `force_kill_task/2` delegation smoke tests on the real local path (`{:error, :not_found}` for missing ids) + a `:pending`→`:cancelled` round-trip (`:pending` cancels immediately — no live wrapper needed).
- **Force-kill msgid**: the force-kill error-flash test asserts the msgid `"Failed to force kill task"` (tasks_live.ex). Do NOT weaken that assertion back to the old shared `"Failed to cancel task: %{reason}"` wording.
- **Modal-test idiom** (system_live_test.exs): `render_click(view, "request_X")` opens (assert modal title string appears), `render_click(view, "cancel_X")` closes (refute). Handlers that would tear down the local VM are tested via direct `SystemLive.handle_event/3` on a hand-built `%Phoenix.LiveView.Socket{}` with a non-local `current_node` (safe: RPC to a non-existent node degrades to `:ok`); the local VM-killing confirm paths are intentionally NOT unit-tested.
- **Dropdown idiom** (projects_live_test.exs): dropdown content stays in the DOM (hidden via CSS); tests assert the `phx-click="close_configure_dropdown"` click-catcher overlay appears/disappears on `toggle_configure_dropdown`.

## Notes for Agents — attach-file (picker file-mode) tests

The dashboard attach-file flow (`file_pick` event → `EvoDash.DirectoryPicker.pick/3 :file` → `handle_info({:directory_picker_result, "objective_file", ...})` in projects_live.ex) is tested end-to-end via the two support fakes:

- **Module-level fake** (`EvoDash.DirectoryPicker.Fake`, installed via the `:directory_picker_module` env): `pick/2` and `pick/3 :directory` always deliver `{:ok, "/fake/picked/dir"}`; `pick/3 :file` delivers the result stored under `:persistent_term` key `{__MODULE__, :file_result}` (default `{:ok, "/fake/picked/file.txt"}`). Set per test with `set_file_result/1` (`{:ok, path} | :cancelled | :unavailable`): the happy path must point at a REAL temp file (EvoDash.AttachedFile reads it), the error path at a nonexistent one. Always call `Fake.reset()` in on_exit so the persistent_term result never leaks across tests.
- **wx seam fake** (`EvoDash.DirectoryPicker.Wx.Fake`, installed via the `:directory_picker_wx` env): `new_file_dialog/2` returns `{:wx_ref, 1, :wxFileDialog, []}`; `get_path/1` branches on the ref type (`:wxFileDialog` → `"/fake/picked/file.txt"`, `:wxDirDialog` → `"/fake/picked/dir"`). The `set_gate/1` dialog-block is kind-agnostic (busy serialization tests work for both kinds).
- **Exact block shape** (pinned by the happy-path test): `"\n\n---\n## Attached file: <basename>\n\n<content>\n"` appended to the `file_pick` event's `prompt` snapshot — NOT the stale `@task_prompt` (the `file_pick_bases` snapshot is consumed and cleared). `EvoDash.AttachedFile.read/1` trims plain text; a missing file flashes `"Failed to attach file: File not found: <path>"` and pushes `%{error: true}`.
- Remote-node and disabled-config `file_pick` cases push `%{unavailable: true}` synchronously from `handle_event` (no picker involvement); `:cancelled`/`:unavailable` result kinds hit the generic `handle_info` clauses.

## Notes for Agents — theming / color-sensitive tests (accent/theme overhaul blast radius)

Nothing in this tree asserts on the theme-toggle markup (root.html.heex localStorage `phx:theme` / `data-theme` attrs), on `dark:` variants, or on generated CSS assets (`priv/static/assets/css/app.css`, digest/manifest, favicon, logo). The layout sidebar's `<details>` theme dropdown appears only in Floki-scoping COMMENTS (`system_live_test.exs:830-834, 2265-2269` — `check_grid/1` exists so disclosure assertions never see it). Color-sensitive tests, by file:

- `theme_color_test.exs` (pure unit) — the ONLY file asserting actual color VALUES on `EvoDashWeb.ThemeColor`: exact OKLCH strings for `accent_color_for_mode/1,2` (genesis red `oklch(0.62 0.19 25)`, existing blue `255`, evolve green `0.72 0.17 152`, custom violet `0.68 0.17 290`, resume-lighter `0.78 0.16 152/290`), `default_color() == "#6366f1"`, and `accent_color/1` project-name-hash → `#rrggbb` hex (regex `^#[0-9a-f]{6}$`; comment at :6-8 says the mapping mirrors `[data-mode]` hover rings in `assets/css/app.css`; :121-124 the `--project-ring-accent` top-bar ring). Breaks if accent values, the default color, or the OKLCH/hex output format change.
- `helpers_test.exs` — `agent_status_color/bg/border/1`, `task_status_badge/1`, `history_entry_color/1` assert exact DaisyUI token class strings (`text-success`, `bg-base-200/60`, `border-info/30`, `text-accent`, `text-base-content/80`); `task_status_badge/1` pins the Adwaita semantic scheme (running/finalizing/cancelling `bg-warning/10 text-warning`, completed `bg-success/10 text-success`, failed `bg-error/10 text-error`, cancelled/pending `bg-base-content/10 text-base-content/60`). ALSO unit-tests the four shared status/connection helpers added with the Adwaita overhaul: `task_status_dot_class/1`, `task_status_tint/1`, `connection_status_dot_class/1`, `connection_status_badge_class/1` (every status/phase branch + fallbacks). Break only on helper-impl changes (class names), NOT on palette value swaps.
- `home_live_test.exs:526-531, 551-556` — user bubble pins `bg-base-300` present + `bg-primary` absent (comment: DaisyUI theme-token bubble, correct in light AND dark themes).
- `tasks_live_test.exs` — `:cancelling` pulsing dot assertion pins the warning-family class (`html =~ "bg-warning"`, dot via `Helpers.task_status_dot_class`).
- `system_live/charts_test.exs` — chart series pins assert the CSS-var strings: capacity line `style="stroke: var(--chart-capacity)"` (inline style — `var()` is invalid in SVG presentation attributes), legend-dot counts via `[style*="var(--chart-capacity)"]`, gridlines `var(--color-base-content)`. No hex literals asserted (chart colors are theme-block `--chart-*` vars).
- `projects_live_test.exs:1786, 1962, 2081` — spinner `class="loading loading-spinner loading-lg text-info"`; `:4741-4742` — `badge-success` "Open" / `badge-outline` "bug" (GitHub issues).
- `project_components_test.exs:362, 393` — `.badge-warning` "Writable" foreign-repo badge.
- `welcome_complete_live_test.exs:94` — CTA `btn btn-primary rounded-xl px-8`; `settings_live_test.exs:342-361` — `btn-primary` Save-Model button.
- `settings_live_test.exs:2615-2983` — DaisyUI wizard steps `li.step.step-primary` counts (remote bootstrap progress).
- `task_form_components_test.exs:331-353` — Launch button `data-mode` attribute VALUES (the hook CSS `[data-mode]` hover rings key off).
- `project_components_test.exs:44`, `setting_card_test.exs:36-38` — generic class-string pins containing `text-base-*` tokens (input/row chrome).
- `system_live_test.exs:750-810` health-light "green/red" tests assert state/text, not CSS classes.

`test_helper.exs` + `support/`: no theme/accent/config-color manipulation (only XDG_DATA_HOME redirect + directory-picker disable + nix disabled).

## Notes for Agents — appearance accent-color tests (swatch picker + `data-accent-color`)

The configurable accent color (Settings `:appearance` category → `[:appearance, :accent_color]`, palette `blue teal green yellow orange red pink purple brown slate`) is covered by `setting_card_test.exs` (swatch describe), `settings_live_test.exs` ("appearance category / accent color"), and `live_hooks/appearance_test.exs` (hook + `#app-layout` attribute). Facts that matter when touching these tests:

- **Active-swatch discrimination**: every swatch button carries `focus-visible:ring-2` in its BASE classes — do NOT match a generic `ring-2`. The only active marker is the `ring-base-content/70` fragment (paired with `scale-110`); assert active/inactive by `ring-base-content/70` presence/absence.
- **Hidden-input contract**: the special-cased card renders `<input type="hidden" name="appearance.accent_color">` whose value falls back to `"blue"` for nil/unknown/non-palette values. The swatch visuals are **CSS-driven** (accent role-split): each chip carries `data-accent-color={name}`, fill is `bg-primary`, and the check glyph is `text-primary-content` — the SAME class for every accent; app.css's `[data-accent-color="yellow"]` rule retargets `--color-primary-content` to the dark contrast value there. There is NO per-accent Elixir class (the old `accent_check_class/1` yellow → `text-black` special case is deleted); `SettingCard.accent_palette/0` returns a plain list of name STRINGS (no `{name, hex}` tuples).
- **Draft flow**: `select_appearance_accent` stores ONLY the `:appearance_accent_draft` assign (whitelisted via `SettingCard.accent_name?/1`; unknown names → "Unknown accent color." flash, value unchanged); the draft is cleared on any authoritative file_config replacement (mount/save/reset/node-data reload). `save_category` with the dotted key `"appearance.accent_color"` deep-puts the string raw via `params_to_category_config/3` (`config_io.ex` `:string` pass-through) → TOML line `accent_color = "teal"`.
- **Config freshness**: per-test unique `XDG_CONFIG_HOME` + write-config-then-assert needs NO manual cache reset — `EvoGit.Config`'s `persistent_term` cache is keyed by file path and revalidated by mtime+size on every call (`cached_file_read/2`).
- **Synchronous node-aware resolution with a per-node accent cache**: for LOCAL nodes the Appearance hook resolves the accent synchronously in its attached `:handle_params` interceptor (`EvoGit.Config.resolve([:appearance, :accent_color])`) — a connected `live(conn, page)` shows the correct `data-accent-color` on `#app-layout` on the FIRST render (no flush/async idiom needed). Remote `?node=` targets are served by the `EvoDash.AccentCache` hub (boot-created ETS, keyed by target id, `nil` = local): a warm entry paints synchronously on the first render (dead render AND connected mount); a cold connected remote still fetches async (Task.Supervisor + seq/node stale-guard) and every successful fetch writes the cache; a failed fetch is a no-op. Pending (saved-not-connected) targets prefer their warm accent over the local one.
- **Lightest full-page mount for hook tests**: `/welcome` (WelcomeLive mount spawns no TaskSupervisor children) and `/system` (mounts cleanly per desktop_quit_test) — both render through `Layouts.app`. Assertions need `Floki.parse_document!/1` first (a raw html binary is not a Floki-queryable tree).
- **Direct on_mount invocation** (dead-render tests) needs a BOOT-SHAPED socket: `attach_hook` for `:handle_params` raises when `router: nil`, and `socket.private.lifecycle` must be present — see the in-file helper in `appearance_test.exs`.

## Notes for Agents — mount/patch async-load settle (avoiding the mount-apply race)

Some LiveViews spawn a supervised `EvoDash.TaskSupervisor` child at mount (or on a `handle_params` patch); its result message is applied LATER and REPLACES assigns with its mount-time snapshot, silently clobbering state a test set after its first event.
The fix is a file-local "settle funnel" applied right after `live/3` (and after the relevant `render_patch/2`) and BEFORE the test drives any events: `await_mount_async_loads/1` (or `await_async_loads/1` when it also covers a patch) waits for THIS mount's `EvoDash.TaskSupervisor` children — matched by the view pid appearing in their `$callers` process-dictionary entry (`mount_async_task_pids/1` + `task_callers/1`) — then calls `render(view)` to drain the queued apply.
Implemented in `settings_live_test.exs` (`mount_settings/2` → `await_mount_async_loads/1`), `review_live_test.exs` (`flush_review_load/2` → `await_mount_async_loads/1`), `projects_live_test.exs` (`await_async_loads/1`), and `settings_live_agents_test.exs` (`mount_settings/1` waits for the tasks observed at mount to exit via a bounded `wait_until/2`), all under `evo_dash_web/live/`.
Reuse one of these helpers instead of re-discovering the race — a fixed sleep cannot cover it deterministically.

## Notes for Agents — event-driven waits (no fixed sleeps)

The big LiveView suites wait for state changes with event-driven waits instead of fixed `Process.sleep` calls.
The `assigns/1` helper those suites use is a synchronous `:sys.get_state(view.pid)` round-trip, and Erlang guarantees per-sender→receiver ordering, so `send(view.pid, msg)` followed by any `assigns/1` read is already synchronized — a sleep there is redundant.
Debounce-sensitive waits poll the file's `tasks_reload_pending`-style flag, or reuse the file-local `wait_until/2` / `flush_*_load/2` helpers.
Kept intentionally: `settings_live_test`'s fake-ConnectionManager `state.delay_ms` sleeps, `node_aware_test`'s fake-manager `delay_ms` busy-wait, and `system_live_test`'s deterministic `:checking` observation window.

## Known Issues

- **`EvoDash.ActiveTasks` hub assertions must not pin a TRANSIENT state**: when a review merge/reject settles a repo in-page, `invalidate_active_tasks/1` deletes the hub key SYNCHRONOUSLY, but the same callback's `set_review_status` broadcast makes the still-mounted page run its own debounced (300 ms) sidebar reload, which legitimately re-writes the key as `{:ok, {[], []}}`. Asserting `EvoDash.ActiveTasks.get(...) == :empty` therefore races that documented refresh and can fail under heavy CPU load. Assert the DURABLE property instead — the settled task id is absent from the snapshot — via the file-local `assert_hub_excludes_task/1` (`review_live_test.exs`); where a comparison against a pre-action snapshot is genuinely intended, await the settled state with the file-local `wait_until/2`. The in-page merge/reject completion sites (the merge/reject completion and hub-family describes, where the page STAYS mounted) already use `assert_hub_excludes_task/1`. The remaining `== :empty` sites assert a page that NAVIGATES AWAY (Ignore, auto-resolve), so the page terminates before its debounced reload can fire and the exact cold-state assertion is safe; the `== pre_hub` comparisons pin the deliberate partial-resolution no-invalidate contract and are left as-is.
- **Global `EvoGit.Store`/`EvoGit.TaskRegistry` per-test isolation**: 5 sync suites (`home_live_test`, `tasks_live_test`, `live_hooks/node_aware_test`, `live_hooks/guide_test`, `node_context_test`) stop the production children and run a temp-sqlite pair through `EvoDash.Test.IsolatedTaskStore.isolate!/1` (`support/isolated_task_store.ex`) — the helper stops its isolated pair FIRST, then restarts the production children with CHECKED results (a `{:error, _}` restore raises) and verifies the restored `EvoGit.Store`'s data dir. Suites that rely on the production pair without isolating (`review_live_test.exs`) call `IsolatedTaskStore.assert_production!/0` in setup, which raises on a leaked/foreign Store or TaskRegistry binding. Everything else uses the run's single unique data dir (`<tmp>/evogit_test_data/genesis-<pid>-<int>` from `config/test.exs`, shared by both umbrella apps within the run).
The standing reminder for any new test that touches tasks: prefer unique task ids + cleanup, never bare global assumptions.
- **`EvoDash.ActiveTasks` is a boot-created named ETS hub shared by ALL suites**: `EvoDashWeb.ConnCase` resets it in `setup` + `on_exit`; a non-ConnCase suite that mounts whole pages must reset it in setup (`EvoDash.ActiveTasks.reset()`).

- **`File.mkdir_p/1` returns `:ok | {:error, reason}` — never `{:ok, _}`**: `EvoDashWeb.ProjectsLive.ProjectFlow.create_project/2` must match the plain `:ok` returned by `File.mkdir_p/1`. The projects_live_test cases "creates and activates a new project" and "creates a fully non-existent nested path recursively" pin this behavior.

## Notes for Agents — event-driven async remote-connect tests

Coverage of the event-driven async remote-connect flow (lib contract: `NodeAware.initiate_remote_connect/2` + per-page `retry_remote_connection`/`connect_remote_target`/`select_node` → terminal outcomes ONLY via `{:remote_connection_status, target_id, status_map}` broadcasts) lives in three places, all `async: false`:
- `live_hooks/node_aware_test.exs` — `describe "initiate_remote_connect/2 — shared async connect helper"` (5 tests): off-process spawn on `EvoDash.TaskSupervisor` (recorded caller pid ≠ test process), socket returned unchanged, `{:ok, :connecting}`/`{:ok, :connected}` → NO `{:remote_connect_result, ...}` self-message, `{:error, reason}` → self-message delivered, nil target → no spawn.
- `live/settings_live_test.exs` — `describe "remote-connection async connect flow (event-driven flash)"` (6 tests): no premature flash on Connect click, info flash exactly once on `:connected`, duplicate terminal broadcast silent (marker consumed), `:connecting` neither flashes nor consumes the marker (later terminal still flashes), error broadcast flashes `last_error` once, duplicate-click-while-pending no-op, sync-error `{:remote_connect_result, ...}` clears the marker + flashes.
- `live/projects_live_test.exs` (describe "remote node contexts") — 3 tests: sidebar `select_node` returns immediately with a DELAYED fake connect (300ms) + off-process caller pid; a mid-session `{:set_status, error}` + broadcast reconciles the gate from the connecting spinner to the error state (error text + last_error + Retry); Retry-from-gate delegates to the shared helper and the sync-error self-message flashes.

**Testing mechanics that matter**: (1) a fake manager exercising a connect MUST handle `GenServer.call(:connect)` returning the new async contract (`{:ok, :connecting}` etc.) — a status-only fake crashes when a connect routes to it via `EvoGit.RemoteConnection.Registry`. (2) NodeAware's `handle_connection_status/2` recomputes `@remote_status`/`@remote_statuses` from the LIVE manager (`NodeContext.connection_status/1`), never from the broadcast payload — a mid-session gate phase-change test must mutate the fake's stored status FIRST (fake `{:set_status, status}` call), then emit the broadcast (realistic core ordering). (3) Broadcasts are delivered as direct `send(view.pid, {:remote_connection_status, id, status_map})` (existing idiom; LiveViews are PubSub-subscribed by NodeAware on_mount and receive the same shape). (4) Per-suite `ConnectionManager` fakes (node_aware / settings `ConnectManager` / projects) are extended with `{:set_status, status}`, `:calls`, `:callers`, configurable `connect_result` + `connect_delay_ms` — keep the original 2-arity `{target_id, status}` start shape so existing call sites stay untouched. (5) DOM-driven `select_node` clicks need `element("#node-selector button", "Test Target") |> render_click()` (targets `@myself`, unreachable as a bare event) plus an extra `render(view)` flush before `assert_patch` observes the `{:node_selected, _}` → push_patch.

## Notes for Agents — async policy

Of the 54 test modules in this tree, 36 are `async: true`; the following 18 are `async: false`, each for a documented process-wide global-state reason.
ExUnit drains ALL `async: true` modules before running any `async: false` one (`ExUnit.Runner.async_loop/4` waits for the whole async cohort before spawning any sync module), so a flip is unsafe only when the module shares a MUTABLE global with ANOTHER module in the async cohort (async-vs-sync overlap is impossible).
`test_helper.exs` seeds `XDG_CONFIG_HOME` at boot to a run-unique EMPTY dir, so a suite that only READS config no longer needs to touch the env — but NO sync module is a pure reader: every `XDG_CONFIG_HOME`-setting suite below WRITES config (or saves a remote target / version-state marker) into its own per-test temp dir, which is exactly why it stays sync.
- **`XDG_CONFIG_HOME`** (process-global `System.put_env`; the only config-isolation seam — `EvoGit.Config.config_dir/0` derives from it with no `Application.get_env` override — so it cannot be made per-test/per-process): `evo_dash_web/live/projects_live_test.exs`, `settings_live_test.exs`, `review_live_test.exs`, `home_live_test.exs`, `system_live_test.exs`, `welcome_live_test.exs`, `welcome_complete_live_test.exs`, `settings_live_agents_test.exs`, `live_hooks/appearance_test.exs`, `live/projects_live/project_test.exs`, `evo_dash_web/controllers/page_controller_test.exs`, `evo_dash_web/controllers/task_export_controller_test.exs`, `evo_dash/node_context_test.exs`. Every module that SETS it is sync — including the export suite, whose `?node=` tests would otherwise mutate the env var underneath a concurrently-reading async module.
- **Global `EvoGit.Store`/`EvoGit.TaskRegistry` terminate+restart juggling**: `live_hooks/node_aware_test.exs`, `live_hooks/guide_test.exs`, and `tasks_live_test.exs` (plus `home_live_test`/`node_context_test`, already sync for config reasons).
- **The boot-created `EvoDash.ActiveTasks` named-ETS hub** (reset by `EvoDashWeb.ConnCase`): `evo_dash/active_tasks_test.exs`.
- **A shared app-env key with an `async: true` module**: `live_hooks/update_status_test.exs` (shares `:desktop_quit_stop_fun` + the `EvoDash.UpdateStatus` hub with the async `desktop_quit_test.exs`), and `settings_live_test.exs` (`:nix_available_override`/`:platform_os_override` vs async `platform_info_test.exs`).

- **No flip candidates remain**: the modules that look like read-only config consumers all WRITE — `page_controller_test` (`VersionState.complete_onboarding/0` :21), `task_export_controller_test` (`RemoteConnections.save/1` :141), `welcome_complete_live_test` (`complete_onboarding` via the CTA click, asserted `File.exists?` :108), `projects_live/project_test` (`File.write!(config_path, …)` :72), `node_context_test` (`save_custom_agent` → agents.toml :168) — and every other sync module mutates a hub/app-env/seam an async peer also touches (`:directory_picker`/`:remote_path_expand_runner` → async `directory_picker_test`/`project_flow_test`; `EvoDash.ChatHistory` → async `chat_history_test`; `EvoDash.UpdateStatus` + `:desktop_quit_stop_fun` → async `desktop_quit_test`/`update_status_test`; `EvoDash.ActiveTasks` → every ConnCase module via `support/conn_case.ex:47-48`, incl. the async `error_html_test`/`error_json_test`). The async full-page mounters `agents_live_test` (`/agents`) and `desktop_quit_test` (`/system`) read config on mount through the NodeAware/Appearance hooks, so a config-writer must never run concurrently with them.
Enablers that would unlock the config-isolated majority are production changes: an injectable config dir (`EvoGit.Platform.config_dir/1`) and a `server \\ __MODULE__` first arg on `EvoGit.TaskRegistry`'s client functions plus a store/registry seam in `EvoDash.NodeContext`. A purely process-dictionary override would NOT suffice — LiveView processes do not inherit the test process's process dictionary, so the config dir / store name must be resolvable per node/session.
Per-file evidence lives in the child CONTEXT.md files — `evo_dash/CONTEXT.md`, `evo_dash_web/live/CONTEXT.md`, `evo_dash_web/live_hooks/CONTEXT.md`, `evo_dash_web/controllers/CONTEXT.md`.

## Constraints
- Follow standard Phoenix test conventions: mirror the `lib/` directory structure under `test/`.
- All connection-based tests must `use EvoDashWeb.ConnCase`.
- Support modules live in `support/` and are compiled automatically by Mix.
- Test file naming: `*_test.exs`.
- Tests requiring an isolated TaskRegistry/TaskStore terminate the production children from `EvoDash.Supervisor` and restart them via `Supervisor.restart_child/2` in `on_exit` to avoid breaking other suites. (These children live in `EvoGit.Application`'s supervision tree; tests reference them via `EvoGit.Store` / `EvoGit.TaskRegistry`.)
- All task persistence in tests goes through `EvoGit.Store` (SQLite-backed) — no other storage formats are used.
- **`try/rescue`/`catch` policy for test files:** While the codebase's design philosophy treats `try/rescue` as an anti-pattern, in test files it is acceptable — and encouraged — in `on_exit`/teardown cleanup paths, because a teardown failure should NOT mask or obscure an actual test failure. Such cleanup rescues must be accompanied by a brief comment documenting why. Rescues that wrap actual test assertions or mask real behavior under test are forbidden; if an assertion would fail, the test must fail loudly.
- **LiveViewTest `render/1` cannot see `:sys.replace_state` mutations** on the LiveView process: the test proxy (ClientProxy) renders from its own cached `html_tree`, updated only via channel diffs — a direct `:sys.replace_state` on the live view pid is never reflected by `render/1`. To test transient states (like a `:testing` spinner), drive the real event path (`render_click`/`render_change`) or send the actual `handle_info` message to `view.pid` — not `:sys.replace_state`.
