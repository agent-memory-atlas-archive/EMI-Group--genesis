# Test Directory — EvoDashWeb Live Tests

## Intent

ExUnit test files for the EvoDash LiveView pages and their support modules. Mirrors `apps/evo_dash/lib/evo_dash_web/live/` under `apps/evo_dash/test/evo_dash_web/live/`.

## Routing Table

- `./projects_live_test.exs` → Comprehensive `ProjectsLive` dashboard suite (~3500 lines; project form/palette/settings, task notifications, remote-node contexts via a fake `ConnectionManager` in `EvoGit.RemoteConnection.Registry`, directory picker, file attach, cross-OS remote path handling)
- `./projects_live/` → Pure unit tests for `ProjectsLive` support modules (`project_flow_test.exs` — path normalization incl. `normalize_remote_project_path/2`, `absolute_path_for_node?/2`, node-aware `build_foreign_repo/4` + `Project.load_foreign_repos/3` raw-storage tests, `path_suggestions/2,3`; `state_persistence_test.exs` — `maybe_restore_foreign_repos/2` node-aware restore with minimal LiveView sockets; `project_test.exs`)
- `./home_live_test.exs` → Home chat page (`GET /help`, `:reflect` self-reflective agent) — full suite notes in `../CONTEXT.md`; the "send message" describe includes a canonical real-send pin ("starts a reflect task with the right opts") asserting synchronous `chat_task_id` tracking (non-nil, matching the persisted reflect row) and NO "Failed to start the task" bubble — see the no-real-LLM rule below; plus user-bubble class pins on BOTH send routes (typed submit + suggestion-chip click → `w-fit bg-base-300 text-left` inside the `flex justify-end` wrapper); the "model selector" describe covers the per-chat model `<select>` (`select_chat_model` event → `selected_model_id` assign, threaded as `"model_id"`/`"model_id_locked"` STRING-keyed opts — the Store codec does NOT whitelist them, see `projects_live_test.exs` "model selection auto/lock semantics"), Auto-threads-neither-key, remount persistence (per-chat `selected_model_id` restored via `EvoDash.ChatHistory`), suggestion-chip sends threading the pin exactly like a typed submit (Auto threads neither), and the pin SURVIVING `new_chat` (fresh chat id; first-ever mounts still Auto). Finalized assistant text is markdown-rendered (`.md-content`, Floki-scoped assertions — the copy button's `data-content` carries the raw source), mid-stream text stays plain, and the per-message raw toggle (`toggle_assistant_raw`, scoped `#chat-entry-<id>`) + `copied`→info-flash are covered (component-level markdown/raw/action-group coverage lives in `./home_live/assistant_message_test.exs`). The node-awareness describe additionally pins model survival across a local→remote→local node switch (assign-based — the remote header select is absent since `ModelSelect.load` degrades to `{[], false}`). The "command approvals (security levels 2 & 3)" describe covers the interactive run-command approval cards: level-2 informational vs level-3 danger rendering, own-request filtering (task/agent/node), `approval_resolved` removal, Confirm/Deny routing through the call-time `:approval_responder` seam (installed per test via `Application.put_env` + `on_exit` delete), invalid-decision no-op, responder-error flash with optimistic card removal, and duplicate upsert semantics. The `:failed` terminal-error path is covered (completion/error-rendering describe): fetched-task payloads carrying a structured `error` map render `error.message` as the terminal text, missing/nil/blank-message payloads fall back to the generic `"The task failed."` msgid, legacy non-map errors (`{:error,_}` result rows, tuple/string `error`) no-crash. The pure helper `AgentStream.extract_failed_message/1` (nil/non-map → nil, absent/nil/non-binary message → nil, blank `""`/whitespace-only → nil, atom-key `:message` AND string-key `"message"` valid messages returned verbatim, no untrimming) is unit-tested in `./home_live/assistant_message_test.exs`; the sidebar-robustness describe's summary fixture carries the 16th `error` key (`error: nil`) matching the 16-key `list_tasks_summary/1` comment. The "genesis source gate" describe covers the LOCAL-only Genesis-source gate on /help (`EvoDashWeb.HomeLive.SourceGate`): `:source_availability_runner` / `:source_clone_runner` are injected via `Application.put_env` BEFORE `live/3` (resolved at spawn time inside the `EvoDash.TaskSupervisor` child) and restored in `on_exit`; a known-`false` local availability renders `#genesis-source-gate` + `#genesis-source-download`, swaps the empty-state chips for the blocked note, and blocks the composer/send (error flash, no `:reflect` row persisted, no optimistic bubble); available → no gate + a normal send; a successful download (`{:ok, _}`) clears the gate after the post-clone re-check (busy "Cloning…" label + disabled button first); a failed download flashes and keeps the gate; the node-awareness describe adds a remote-node case (gate never renders, the local-only check is never spawned). The async check can't be awaited by `render_async/2` — the file-local `await_source_available/3` polls `assigns(view)[:source_available]` via `wait_until/2` then syncs with `render/1`. The "genesis source gate" describe covers the LOCAL-only Genesis-source gate on /help (`EvoDashWeb.HomeLive.SourceGate`): `:source_availability_runner` / `:source_clone_runner` are injected via `Application.put_env` BEFORE `live/3` (resolved at spawn time inside the `EvoDash.TaskSupervisor` child) and restored in `on_exit`; a known-`false` local availability renders `#genesis-source-gate` + `#genesis-source-download`, swaps the empty-state chips for the blocked note, and blocks the composer/send (error flash, no `:reflect` row persisted, no optimistic bubble); available → no gate + a normal send; a successful download (`{:ok, _}`) clears the gate after the post-clone re-check (busy "Cloning…" label + disabled button first); a failed download flashes and keeps the gate; the node-awareness describe adds a remote-node case (gate never renders, the local-only check is never spawned). The async check can't be awaited by `render_async/2` — the file-local `await_source_available/3` polls `assigns(view)[:source_available]` via `wait_until/2` then syncs with `render/1`
- `./settings_live_test.exs` → Settings page (search, custom model providers, whitelist safety) + the appearance-category accent-color describe (swatch render, `save_category` persistence of `appearance.accent_color`, `select_appearance_accent` draft + whitelist rejection — detail in `../CONTEXT.md` "Notes for Agents — appearance accent-color tests")
- `./settings_live_agents_test.exs` → Settings **Agents** category (custom agents + model-selection script editors)
- `./settings_live/` → Pure unit tests for SettingsLive support modules (`model_profile_helpers_test.exs`, `config_io_test.exs`)
- `./tasks_live_test.exs` → Cross-project task list (search/filter, cancellation modals, `:cancelling` display, event-driven debounced reloads)
- `./review_live_test.exs` → Review page suite (~3900 lines, 32 describes / 83 tests) against the PER-REPOSITORY CARD redesign — one card per repo (`RepoCards.repo_cards/1` → `ReviewComponents.repo_cards/1`; the primary-scoped Continue-task + "…" overflow menu live in `Actions.task_actions/1`; `merge_box/1`, `merge_outcomes_panel/1` and the `#merge-form` (singular) / `#repo-switch-form` ids are GONE). Module setup (L8-29) stubs `:merge_check_runner` to a fast `{:ok, :clean}` + resets `EvoDash.ActiveTasks`; merge/reject are PER-REPO and never navigate — `render_click(view, "merge", %{"repo_id" => id, "target_branch" => t})` / `render_click(view, "reject", %{"repo_id" => id})`, each card owning `<form phx-submit id="merge-form-<repo_id>">` (hidden `name="repo_id"` + its own `<select name="target_branch">`; the runner seam is invoked ONCE per call); ids `#review-repo-cards`, `#review-completion-banner`/`#review-completion-back` (only when completion != nil), `#repo-card-<repo_id>`, `#repo-resolution-<repo_id>` (ALWAYS present, empty when unresolved), `#merge-form-<repo_id>` (`#diff-repo-switch-form`/`#commits-repo-switch-form` unchanged); resolution states `nil` (actions iff `branch_exists`), TERMINAL `:merged`(`target`)/`:rejected`/`:handled` (load-time seed: non-blank branch_name ∧ NOT branch_exists), non-terminal `:error`/`:conflict`(`detail`): missing-task error, ignore action, cancelled tasks, nil branch, archive tab, merge-target selector, async merge check (6-tuple `:merge_check_result` injection), auto-resolve (incl. unreachable-remote), async review-data load + stale guards, and the multi-repo suite (repo-list construction primary-first / read-only + no-commits absent / legacy single-primary, per-repo merge via the `:review_merge_runner` seam, partial failure rendered on the CONFLICTING repo's card while the page STAYS — there is NO `@merge_outcomes` panel, per-repo reject via `:review_reject_runner`, per-repo merge-check results + `switch_repo` via the `<select phx-change>` repo selector — the repo affordance renders ONLY under its gates: the per-card merge select needs `branch_exists`, the Files-changed toolbar select needs non-nil `review_data`, so orphaned-branch fixtures assert the selector's ABSENCE + the "No diff data available" empty state, auto-resolve/resume primary-scoped foreign-repo carry — runtime-only, core-tested in evo_git `merge_context_test.exs`). Per-repo behavior covered: whitelist + TERMINAL resolutions are no-ops (runner NOT called), per-repo target change/read (`merge_target_change` moves ONLY that repo's select; a blank/unknown target falls back to that repo's `default_merge_target`), load-time `:handled` seeding, `open_repo_diff` (valid id → `review_tab: :files_changed` + that repo's diff; unknown/malformed → no-op), OVERALL COMPLETION (the private `completion_status/2` driven through real clicks: once EVERY resolution is terminal → `:merged` when ≥1 merged else `:rejected`, an ALREADY-PERSISTED `:merged`/`:rejected` wins, persisted via `NodeContext.set_review_status/3` + success flash + `#review-completion-banner`, NO navigation, with `invalidate_active_tasks/1` running ONLY on this path), and in-file component pins for `repo_cards/1`/`task_actions/1` via `render_component/2` (the components suite is a sibling node). Redesigned affordances covered: files-changed tree/filter (`toggle_dir` on the FULL dir path, collapsed-by-default chain, `expand_all_dirs` ancestor-chain / `collapse_all_dirs`, `filter_files` flat case-insensitive mode + "No matching files", `switch_repo` resets `file_filter`), task-actions overflow `<details class="dropdown dropdown-end dropdown-top ml-auto">` (extracted by the `overflow_menu/1` regex helper; Reject is NOT a menu item anymore — it moved to the repo cards — with the item order pinned Create GitHub PR → Extract Skills → Export JSON (gated on `archive_metadata`) → Ignore last/plain/no danger zone) + the Continue-task (`resume`) secondary button, AGGREGATE diff stats (stats bar + `page_tabs` count badges sum across ALL repos via `aggregate_stats/1`, NOT the active repo — pinned with per-repo counts deliberately ≠ `length(files)`), and `short_title` truncation in `page_header` (one-line h1 ≤100 chars + "…", full objective in the `title` attr). File-local helpers: `flush_review_load/2`, `wait_until/2`, `wait_hub_warm/0`, `mount_with_repos/3`, `review_repo/4`, `repo_card_html/2`, `overflow_menu/1`, `repo_select_state/1`, `repo_target_selection/2`, `completion_back_href/1`, `target_branch_select/1`.
- `./welcome_live_test.exs`, `./welcome_complete_live_test.exs` → Onboarding pages
- `./system_live_test.exs` → System page (scheduler controls, system check, Status sandbox helpers)
- `./system_live/` → `charts_test.exs` — SystemLive chart/ring-buffer tests
- `./agents_live_test.exs` → Agent tree (node-aware async loads, push-driven refresh)
- `./agents_live/` → `optimistic_messages_test.exs` — agents page optimistic-message handling
- `./platform_info_test.exs` → Pure unit tests for `EvoDashWeb.PlatformInfo` (platform gating)
- `../components/` → Function-component tests (sibling — read-only, escalate writes to parent)
- `../live_hooks/` → Live-hook tests (NodeAware, Guide, DesktopQuit, UpdateStatus) (sibling — read-only, escalate writes to parent)
- `../controllers/` → Controller / error-template tests (`error_html_test.exs`, `error_json_test.exs` live HERE, not in `live/`) (sibling — read-only, escalate writes to parent)

## Notes for Agents

- **NO real LLM/exec in dashboard live tests** (suite policy): a test must never be able to reach `AgentScheduler.run_agent`/the network.
  - Home chat (`home_live_test.exs`): chat state is seeded via `seed_chat_state/2` + synthetic `{:task_updated, ...}` events and terminal paths via `finalize_failed/2` / `insert_task_fixture!` — NOT by submitting `send_message` to start a real `:reflect` task. Exactly ONE real-send canonical pin ("starts a reflect task with the right opts") remains and is network-free by construction: the suite setup pushes `model_profiles: []` into the AgentScheduler (`update_config`, restored in on_exit — the same seam `:evo_git`'s own suites use), so `run_agent` rejects every spawn synchronously with `{:error, :llm_not_configured}` before any agent registration or HTTP call. Note the counterintuitive fact (verified): a BOGUS `model_id` does NOT fail fast — `Dispatch.resolve_model_for_agent/2` warns and falls back to the FIRST real profile, which would hit the ambient expired-key LLM.
  - Projects page (`projects_live_test.exs`): the ~10 task-form/GitHub-fix launches pin the dashboard→TaskRegistry opts-threading contract on the persisted `EvoGit.TaskInfo` row and are kept real for exactly that contract, but structurally cannot reach the LLM: 9 fail fast at `validate_node_path` via the `node_path: "./nonexistent-dir"` lever (a VALID node path would pass validation and proceed to `run_agent` with the boot-time ambient LLM config — there is no valid-but-quiet path), and the GitHub-fix test dies at `resolve_starting_commit` on an unborn-HEAD repo seed (`make_git_repo!` gives an empty committed repo so `ensure_repo` no-ops). In-body `cancel` calls were dropped (wrapper already terminal) and `stub_github_upstream_check!/0` keeps the async GitHub-upstream `git remote get-url` from racing setup `on_exit` teardown.
  - `ExUnit.start(capture_log: true)` in `test_helper.exs` hides these side effects for passing tests, which is WHY the policy is structural (no path to the LLM), not output-grep-based.

- **SystemLive chart tests must stub the seed runner**: any test that asserts on `chart_samples` (e.g. "a foreign-node system sample broadcast is ignored") MUST set `Application.put_env(:evo_dash, :system_samples_runner, stub)` in the test body BEFORE mounting. The default runner reads the real `EvoGit.SystemSampler` ring buffer, which is non-empty whenever any sampler tick has occurred in the VM (the evo_git suite's `system_sampler_test.exs` ticks the registered sampler; umbrella `mix test` runs both apps in one VM). Test env disables the tick via `config/test.exs:32` (`:system_sample_interval_ms, 86_400_000`), but that is an accident of config, not a guarantee — the file-level setup block restores the env in `on_exit`.
- **Remote-node test pattern** (used by the cross-OS path tests): save a unique target via `EvoGit.RemoteConnections.save/1` under per-test isolated `XDG_CONFIG_HOME`, then `start_supervised!` a fake `ConnectionManager` GenServer registered in `EvoGit.RemoteConnection.Registry` with `%{phase: :connected, node: "genesis_remote@127.0.0.1", last_error: nil}`. `:erpc` calls to the fake node fail fast, so RPC-dependent remote success paths (e.g. `NodeContext.dir?/1` → true) are unreachable in tests — only the validation/degradation branches are covered end-to-end.
- **`normalize_remote_project_path/2` tilde seam**: `Application.get_env(:evo_dash, :remote_path_expand_runner)` is read at CALL time; tests inject fakes with `Application.put_env` + `on_exit` restore (see `project_flow_test.exs` `install_expand_runner/1`).
- **Foreign-repo raw storage in remote contexts**: `EvoGit.Core.ForeignRepo.new/3` runs `Path.expand/1` on the root, so EvoDash remote contexts bypass it via the node-aware seam `ProjectFlow.build_foreign_repo/4` (local node → `ForeignRepo.new/3`; remote node → RAW root, no local expansion). Wired into the `add_foreign_repo` handler, `Project.load_foreign_repos/3` (remote genesis.toml loading), and `StatePersistence.maybe_restore_foreign_repos/2` (persist/restore round-trip). Tests assert raw-verbatim roots (POSIX and Windows, host-OS independent) in `project_flow_test.exs` + `state_persistence_test.exs`.
- **`EvoDash.ActiveTasks` sidebar-hub pollution (page suites)**: every full-page `live(conn, path)` mount seeds `:running_tasks`/`:pending_tasks` from the shared in-memory `EvoDash.ActiveTasks` hub (local key `{nil, node()}`, remote `{node_param, remote_node}`) via `LiveHooks.NodeAware.on_mount` (`apps/evo_dash/lib/evo_dash_web/live_hooks/node_aware.ex` L102-176; the connected-mount LOCAL fetch is UNCONDITIONAL — remote/pending contexts never fetch on mount — and every applied fetch writes the hub; `{[], []}` IS a valid snapshot, a fetch against an empty registry warms it empty). **Every page suite in this subtree resets the hub per test in setup** (`EvoDash.ActiveTasks.reset()` — home_live L51, tasks_live L27, projects_live via its `reset_active_tasks_hub/1` setup tag (L75), review_live L22, system_live L35, agents_live L26, settings_live L46, settings_live_agents L34, welcome L35, welcome_complete L37). Reason: suites that insert sidebar-visible rows (status `:running/:pending/:finalizing/:cancelling`, or `:completed` + `branch_name` + `review_status: nil`) leave rows in the shared store (home_live/tasks_live/projects_live/review_live), and the per-test Store/TaskRegistry isolation (terminate + `start_supervised` + restart_child) and `ChatHistory.reset()` do NOT clear the hub — without the reset, a warm key would seed those leftover rows into later whole-page mounts' first paint, and the (unconditional) mount fetch against the shared store could fetch them back into the sidebar (gate: `apps/evo_dash/lib/evo_dash_web/components/layouts.ex` L179). Contract: any NEW suite that mounts whole pages through `Layouts.app` must call `EvoDash.ActiveTasks.reset()` in setup (sidebar labels derive from `opts[:objective] || opts[:prompt]`, truncated to 30 chars — `task_label/1` in layouts.ex).
- **Review per-repo actions vs the ActiveTasks hub** (regression pins in `review_live_test.exs`): a per-repo merge/reject settles ONLY that repo (`MergeCheck.update_repo/3`) and the page NEVER navigates; `EvoDash.ActiveTasks.invalidate/2` — via `review_live.ex` `invalidate_active_tasks/1` — runs ONLY on the COMPLETION path, i.e. when every repo's resolution became TERMINAL (the aggregate status is then persisted via `NodeContext.set_review_status/3` with a success flash and the completion banner; `ignore`/`resume`/`auto_resolve` still `push_navigate`). Asserted with the file-local `wait_hub_warm/0` idiom (wait for the connected-mount fetch to warm `{nil, node()}`, act, then compare `EvoDash.ActiveTasks.get(nil, node())`): a partial resolution leaves the warmed snapshot byte-for-byte unchanged, the final terminal resolution yields `:empty`.
- **`test_llm` handlers spawn REAL provider requests — a `mix test` console-noise source**:
  `SettingsLive.handle_event("test_llm", …)` (`settings_live.ex`) and `WelcomeLive.handle_event("test_llm", …)` (`welcome_live.ex`) each start a DETACHED `EvoDash.TaskSupervisor` child running `EvoGit.SystemCheck.llm_test/1,2` → `ReqLLM.stream_text/3`.
  When a provider key is resolvable (env var / the process-global `ReqLLM` key store, which tests populate via `EvoGit.Config.credentials/0`) that child makes a real streaming HTTP request, and ReqLLM's `ReqLLM.Streaming.Failure.log/1` (`deps/req_llm/.../streaming/failure.ex`) emits `[warning] Streaming provider/API request failed: status=403, provider_code="forbidden", …`.
  The child outlives its test, so when the network is slow/blocked the line lands OUTSIDE ExUnit's per-test `capture_log` window and leaks into the `mix test` console (a fast response is absorbed by a later test's capture window — the leak is network-timing-dependent, not assertion-related).
  With no key at all the call fails BEFORE HTTP (`[error] Provider failed to build streaming request …`, no `Streaming provider/API request failed` line).
  Five tests can fire such requests.
  The two `settings_live_test.exs` "LLM connection test" tests ("test_llm handler routes through remote node when remote_config is true" / "… proceeds when remote_config is false") are quiet by construction: their profile fixture uses `model: ""`, which is still truthy for the handler's `if model do` branch (so the found-profile path still runs and `llm_test_status` still reaches `:testing`) but makes `SystemCheck.guarded_llm_test/3` return `{:error, "No LLM model configured"}` before any network I/O.
  The three `welcome_live_test.exs` "LLM connection test" tests ("typed API key is saved before the connection test runs" — always, it saves its own key; "testing state renders the spinner"; "test_llm handler starts the test (unit-style)") STILL issue real requests, because `resolve_welcome_model_value/4` never yields the `""`/`%{}` short-circuit value and neither handler has an app-env runner seam — silencing them cleanly needs a production-side `:llm_test_runner` seam in `lib/`, not a test-only change.
  Note the first settings test's NAME is misleading: the handler derives `remote?` from `current_node != node()` (not the `remote_config` assign), so with `current_node: node()` it exercises the LOCAL branch.
- **Running this subtree's tests**: use `mix test apps/evo_dash/test` (or an explicit file path). ⚠️ The directory form `mix test apps/evo_dash` silently produces NO output and runs nothing in this repo — always point at `apps/evo_dash/test`. App-wide/umbrella runs are ORDER- AND TIMING-SENSITIVE (pre-existing): the `async: false` suites here repeatedly terminate/restart the GLOBAL `EvoGit.Store`/`EvoGit.TaskRegistry`, and a leaked shared-hub snapshot can break an unrelated later mount (e.g. the sidebar's `Layouts.group_tasks_by_project/2` crashing on a junk atom left by a hub round-trip test that never cleans up). So an occasional single failure in a DIFFERENT suite is not necessarily caused by the file under test — re-run the failing file standalone (and with a couple of seeds) before suspecting a regression.

- **Async policy (per-suite)**: the PAGE suites here are all `async: false` except `agents_live_test.exs` (like `projects_live/project_test.exs`); the pure support-module unit tests are `async: true` (`project_flow_test`, `state_persistence_test`, `platform_info_test`, `settings_live/config_io_test`, `settings_live/model_profile_helpers_test`, `system_live/charts_test`, the `home_live/*` and `agents_live/*` unit tests). Each sync page suite touches at least one BEAM/process-wide mutable resource with no per-test seam:
  - **XDG_CONFIG_HOME** (process-wide env; `EvoGit.Config.config_dir/0` → `EvoGit.Platform.config_dir/1` reads it — there is NO config-dir app-env/process-dict override; an app-env override would NOT help since app env is itself global). Two concurrently-running modules that set it clobber each other. Used by: `projects_live_test`, `settings_live_test`, `review_live_test` (2 remote describes), `home_live_test`, `system_live_test` (several describes), `welcome_live_test`, `settings_live_agents_test`, `welcome_complete_live_test`, `projects_live/project_test`. Every other setter is `async: false` (the one `async: true` setter, `evo_dash_web/controllers/task_export_controller_test.exs`, saves/restores it around its 3 `?node=` tests).
  - **`EvoGit.Store` / `EvoGit.TaskRegistry` GLOBAL singleton**: `tasks_live_test` + `home_live_test` terminate + isolate + `restart_child` it; `projects_live_test` + `review_live_test` insert fixture rows (unique ids, on_exit cleanup) into the SHARED store. Terminating the singleton conflicts with any concurrent store reader/writer.
  - **`EvoGit.AgentScheduler` global state**: `settings_live_test` + `system_live_test` call `pause()`/`resume()`; `home_live_test` calls `update_config(model_profiles: [])` (restored in on_exit).
  - **App-owned ETS `:evogit_sched_meta` / `:evogit_agent_state`** (owned by `:evo_git`, survive tests): `home_live_test` seeds per-id rows and does a GLOBAL per-`task_id` sweep.
  - **Global hubs**: `EvoDash.ActiveTasks` (ConnCase resets it per test; `review_live_test` + `home_live_test` ASSERT its contents incl. `== :empty`; EVERY page mount writes the shared local `{nil, node()}` key); `EvoDash.UpdateStatus` (`system_live_test` resets); `EvoDash.ChatHistory` (`home_live_test` resets — would also conflict with the `async: true` `evo_dash/chat_history_test.exs`, which resets it too).
  - **App-env seams** (`Application.put_env` + on_exit delete): file-unique keys are cross-module safe; SHARED keys conflict — `:platform_os_override` is used by `system_live_test` + `settings_live_test` + the `async: true` `platform_info_test`; `:directory_picker` / `:directory_picker_module` by `projects_live_test`.

- **`agents_live_test.exs` is `async: true`**: it seeds the global `:evogit_sched_meta` / `:evogit_agent_state` ETS rows under a UNIQUE per-test agent id (`System.unique_integer([:positive]) + 10_000_000`, exposed via `agent_id/0` reading the process dictionary), so no two concurrent modules collide.
- Unlocking the remaining sync page suites needs production changes (an injectable per-process config dir in `EvoGit.Platform.config_dir/1`); the `XDG_CONFIG_HOME` env var and the local `EvoDash.ActiveTasks` hub key are process-wide and shared.
- **Mount/patch async-load settle**: `projects_live_test` + `review_live_test` + `settings_live_test` spawn a supervised `EvoDash.TaskSupervisor` child at mount whose late result apply can clobber test-driven state — they funnel every mount through a file-local settle helper (see `apps/evo_dash/test/CONTEXT.md` → "Notes for Agents — mount/patch async-load settle").
